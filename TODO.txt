# TODO

## Current priorities

### Tagging

- Close tag docs / release readiness:
  - Add performance notes for large FEN / puzzle batches.
  - Add migration notes for renamed or canonicalized tags.
  - Keep `wiki/tag-reference.md` synchronized with emitted families as detectors change.
  - Run the full regression suite after the tag pass.
- Continue the remaining detector backlog from `wiki/tagging-implementation-plan.md`:
  - Broaden fixture coverage for tag families that still rely mainly on ad hoc tests.
  - Triage the remaining named mate patterns; implement only predicates that are safe from final position alone.
  - Document excluded or history-dependent mate names instead of forcing weak detectors.
  - Continue tactical motif work: discovered checks, double checks, trapped pieces, overloads, deflections, clearances, sacrifices, quiet moves, zwischenzug, perpetual-check, and windmill candidates.
  - Normalize any remaining engine-enriched eval, PV, candidate, threat, and mate-in-N tags.

### CLI consistency

- Continue standardizing position input across older commands:
  `--fen`, positional FEN where sensible, `--input`, `--pgn`, and `--stdin`.
- Continue standardizing machine/script output across older commands:
  `--format`, `--json`, `--jsonl`, `--output`, `--no-header`, and `--fields`.
- Reduce remaining raw `System.exit` / direct stack-trace handling in older command paths:
  `ConfigOps`, `EngineOps`, `EvalOps`, `EvalCommand`, `BuiltInEngineCommand`,
  `PositionViewCommand`, `TagsCommand`, `PuzzleTagsCommand`, `TagTextCommand`,
  `PuzzleTextCommand`, `PuzzleSupport`, `MineCommand`, `GenFensCommand`, and
  `ThreatsCommand`.
- Keep command handlers thin: orchestration in CLI packages, chess logic in core / engine packages.
- Add shell completion for bash, zsh, and fish.
- Add broader CLI snapshot/golden tests for help output, table output, JSON output, and common error messages.
- Add `position show` and `selftest` only if they still look worthwhile after the current command set settles.
- Tighten defaults so common commands work well without excessive flags.

### Repo quality

- Add or document a one-command local workflow layer (`just` or `Makefile`) for build, release, install, and smoke tests.
- Add optional `pre-commit` hooks for shell scripts and Java formatting.
- Consider `jlink` / `jpackage` for optional self-contained distributions.

## Recently worked off

- `wiki/tag-reference.md` exists and is linked from the user docs.
- Command examples for `fen tags` and `puzzle tags` are present in the wiki.
- Exact `MOVE` aggregate tags are implemented in `chess.tag.MoveFacts`.
- Conservative terminal `CHECKMATE` attributes and high-confidence mate patterns are implemented in `chess.tag.Checkmate`.
- `MOVE` and `CHECKMATE` tags have direct regression coverage in `TaggingRegressionTest`, `ParserRegressionTest`, and `testdata/tags/static.tsv`.
- Initial native workbench regression coverage exists in `WorkbenchRegressionTest`.
