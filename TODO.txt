# TODO

## Current priorities

### Java Code Quality And Packaging

Goal: get the Java workspace clean under the VS Code SonarQube/SonarLint
session and keep source organization uniform. This is the foundation before
large UI, MCTS, and backend work keeps growing the codebase.

- Use VS Code SonarQube/SonarLint as source of truth:
  - Use `scripts/inspect_java_diagnostics.sh`, `reports/java-diagnostics-triage.md`,
    and the VS Code Problems/SonarQube views before and after each cleanup
    batch.
  - Repeat scans until no Java errors or warnings remain.
- Keep naming/package structure uniform:
  - Rename vague or inconsistent classes only when the new name clarifies
    ownership or behavior.
- Improve Java documentation:
  - Add proper multiline Javadoc to classes, public/protected methods,
    constructors, fields, and constants.
  - Document package-private types and members when they define important
    internal contracts or non-obvious behavior.
  - Include parameter, return, and thrown-exception tags where they clarify the
    contract.
  - Keep comments useful: explain purpose, invariants, ownership, threading,
    lifecycle, units, and edge cases rather than restating obvious code.
  - Keep Javadocs current during refactors and verify generated docs do not
    surface stale or misleading comments.
- Clean up and deduplicate code:
  - Use SonarQube/SonarLint duplicate-code and complexity findings to identify
    repeated logic.
  - Consolidate repeated Workbench UI styling, board rendering, command parsing,
    output formatting, and backend-selection code into shared helpers where the
    abstraction is genuinely clearer.
  - Remove dead code, stale compatibility shims, unused helpers, and unused
    imports found during cleanup.
  - Split oversized methods/classes only when it improves ownership,
    testability, or readability.
  - Avoid churn-only refactors; each cleanup should either remove duplication,
    reduce complexity, or fix a real static-analysis issue.
- Verify:
  - Full compile passes.
  - Focused regressions pass for touched areas.
  - VS Code SonarQube/SonarLint Java diagnostics are clean.

### Workbench UI Foundation

Goal: unify Workbench layout behavior and styling so all current and future
panels inherit the same controls, resize handles, scrollbars, and visual rules.

- Manual layout-control pass:
  - Check narrow, normal, and wide Workbench windows.
  - Exercise open, split, dock, detach, collapse, restore, close, and keyboard
    focus flows.
  - Fix any remaining unclear tab/layout controls with clear tooltips,
    accessible names, action ids, disabled states, and focus/hover feedback.
- Unify Workbench styling:
  - Audit toolbar buttons and inspector panes for unstyled or inconsistently
    styled native Swing defaults.
  - Reuse existing custom styling helpers wherever possible instead of adding
    one-off paint/styling code.
  - Consolidate duplicate components or helper methods that do the same visual
    job in slightly different ways.
  - Keep styling centralized in `Theme`, `Ui`, and dedicated reusable UI
    components so future panels inherit the same look automatically.
- Verify:
  - Workbench regression coverage for any changed tab/layout behavior.
  - Manual pass confirms focus, restore controls, and remaining custom controls
    are consistent across the shell.

### Workbench MCTS Graph And Live Search

Goal: keep the current MCTS table/details workflow and add a viable graph view
for the full tree being calculated, while making live search/NN visualization
advance incrementally instead of jumping from initial to final state.

- Preserve and improve the table workflow:
  - Keep the root move table as a first-class view; it is useful for scanning,
    sorting, comparing visits/scores, and inspecting candidate moves.
  - Keep selected-node details and board preview synchronized with table
    selection.
  - Add a clear view switcher for `Table` and `Graph` rather than replacing the
    table with the graph.
- Build the zoomable node/branch graph:
  - Add a custom Swing `JComponent` canvas for the explored MCTS graph.
  - Draw connected parent/child branches with highlighted PV and selected line.
  - Add graph style modes:
    - `Boards`: each node is a small chessboard thumbnail.
    - `Moves`: each node is a compact box/card with algebraic move text,
      visits, score, and key metrics.
    - `Hybrid` if it proves useful: board thumbnails for selected/PV nodes and
      compact move boxes elsewhere.
  - Implement zoom, pan, fit-to-view, selected-node centering, omitted-node
    badges, and stable viewport across refreshes.
  - Keep large trees bounded with depth/node/min-visit filters while making
    omitted branches obvious.
  - Keep graph selection synchronized with the table, selected-node details, and
    board preview.
- Share MCTS state with the Network tab:
  - Replace or retire the Network tab's older local MCTS worker.
  - Let Network follow-leaf visualization observe `MctsSession`.
  - Keep stop/pause/resume coherent when both MCTS and Network tabs are open.
- Restore incremental live NN visualization during search:
  - Fix the regression where live search only shows the initial and final NN
    frames.
  - When Follow leaf/live NN visualization is enabled, the search should advance
    in visible steps: choose next leaf, compute its NN snapshot, show that
    visualization, wait until the UI has actually rendered or acknowledged the
    frame, then continue to the next step.
  - Use explicit render backpressure instead of unbounded publish/coalescing:
    the UI must not skip all intermediate frames, but it also must not freeze
    the EDT.
  - Keep a bounded frame rate or "visualize every N leaves" setting so users can
    choose between speed and seeing each branch calculation.
  - Show progress/status while the worker is waiting for the UI frame, and make
    pause/stop respond promptly even during this wait.
  - Add regression/manual checks that Follow leaf shows multiple intermediate
    NN frames during a search, not only start/end.
- Strengthen backend/reuse behavior:
  - Validate missing/slow/unavailable non-classical backends.
  - Improve subtree reuse for known next positions and clean reset for unknown
    positions.
  - Surface backend/model errors without killing the tab.
- Verify:
  - Regression coverage for Network observing `MctsSession`.
  - Tree canvas zoom/pan/selection math.
  - Bounded rendering on large synthetic trees.

### Workbench NN Views And Runtime

Goal: finish NN board consistency and runtime diagnostics after the first
orientation/runtime pass.

- NN board cleanup:
  - Add a reusable collapsible chessboard section wrapper for dense NN views.
  - Keep collapsed layouts stable and focus-safe.
  - Keep NNUE, CNN, BT4, and OTIS board placement, empty/error states,
    overlays, coordinates, focus rings, and hit regions consistent.
  - Avoid visible board captions/helper text on small board surfaces; use
    tooltip and inspector details instead.
- Dashboard runtime diagnostics:
  - Expand `Network Runtime` beyond the current compact summary.
  - Show Java2D/OpenGL status, model availability, loaded backends,
    GPU/native availability, NN cache status, and readable model-path summaries.
- Verify:
  - Headless paint tests for collapsible board behavior.
  - Manual smoke with MCTS running, Follow leaf enabled, and NNUE Trace visible.

### OTIS CUDA Backend

Goal: add an optimized CUDA implementation of the OTIS architecture and iterate
on it until it is as fast as practical while staying numerically correct.

- Implement the CUDA backend:
  - Add native CUDA kernels for OTIS inference, including input preparation,
    sheaf/Laplacian-style propagation, trunk blocks, policy logits, WDL/value
    output, and any required reductions.
  - Add JNI bindings and Java backend selection consistent with the existing
    NN CUDA/native backend patterns.
  - Support clean CPU fallback when CUDA is unavailable, model loading fails, or
    the current platform cannot load native libraries.
  - Keep memory ownership explicit: model upload, device buffers, batch-size
    assumptions, cleanup, and error reporting.
- Optimize through measured variants:
  - Start with a straightforward correct CUDA implementation.
  - Build and benchmark alternative kernels/layouts for the expensive stages:
    fused kernels, shared-memory tiling, coalesced access patterns, reduced
    host/device copies, persistent uploaded weights, and batched evaluation if
    it helps MCTS.
  - Compare CPU, naive CUDA, and optimized CUDA paths on the same positions and
    model files.
  - Pick the fastest implementation that preserves accuracy; do not trade away
    correctness for small speed gains.
  - Record benchmark hardware, driver/runtime version, model shape, batch size,
    latency, throughput, and numerical max/mean error.
- Accuracy and parity:
  - Add CPU-vs-CUDA parity tests for representative FENs.
  - Check policy logits, WDL/value outputs, intermediate tensors where exposed,
    and top-move ordering.
  - Define acceptable tolerances for float differences and fail loudly outside
    them.
  - Include edge cases: empty-ish boards, tactical positions, endgames,
    castling/en-passant rights, black-to-move orientation, and malformed model
    handling.
- Integrate with Workbench/CLI:
  - Add config/status reporting so Dashboard/Runtime diagnostics show OTIS CUDA
    availability, loaded backend, device name, and fallback reason.
  - Let OTIS-backed MCTS and Network views use the CUDA backend when selected
    and available.
  - Keep command-line evaluator/backend selection explicit and scriptable.
- Verification:
  - Compile native CUDA code and Java code from a clean checkout.
  - Run CPU-vs-CUDA parity tests.
  - Run OTIS MCTS smoke tests with CUDA enabled and disabled.
  - Benchmark several candidate implementations and keep the fastest accurate
    one, with results saved under `reports/` or documented in the wiki.

### Position Description Text

Goal: improve `position describe` and the Workbench Describe tab, then prepare
the future T5 text model.

- Improve classical descriptions:
  - Review golden outputs for repetition, weak ordering, and awkward phrasing.
  - Improve fact ranking across side to move, material, king safety, pawn
    structure, tactical motifs, threats, candidates, and likely plans.
  - Add optional engine eval/PV enrichment when budgets allow, while keeping the
    default cheap and deterministic.
- Improve Workbench Describe:
  - Make engine selection, detail level, regenerate, freeze current position,
    copy/export, status, and source-signal display feel complete.
  - Keep T5 unavailable status clear until trained weights exist.
  - Ensure generation remains non-blocking while following the board.
- Prepare T5/training:
  - Finalize model-path config and runtime diagnostics for local T5 weights.
  - Serialize `PositionDescriptionInput` into a compact prompt/feature format.
  - Export deterministic training rows with FEN, structured signals, classical
    text, and metadata.
  - Use LLM-distilled targets later to make the generated text shorter, less
    repetitive, and more natural.
  - Add filters for illegal FENs, contradictory tags, hallucinated moves,
    repeated sentences, excessive length, and missing tactical facts.
- Verify:
  - More golden tests for tactical, strategic, quiet, endgame, and malformed-FEN
    positions.
  - Training export schema tests.
  - Workbench non-blocking generation tests beyond tab registration.

### Tag Engine Revamp

Goal: extend chess.tag into a richer, explanation-friendly static tagger whose
`FAMILY: key=value` output is the stable input format for the describe pipeline,
the T5 tag->text model, and a future explanation layer. Every tag stays
derivable WITHOUT an external engine (internal AlphaBeta/Mcts/MateProver,
chess.eval.Evaluator, chess.eval.See, and the bundled ECO book only). Vocabulary
is documented in src/chess/tag/SCHEMA.md; keep it in sync.

Status (this effort, all verified green via scripts/run_regression_suite.sh):
- 22 checkmate patterns in chess.tag.Checkmate (back_rank/smothered/corner/
  support/double_check + arabian/epaulette/anastasia/david_and_goliath/damiano/
  scholars/swallows_tail/dovetail/hook/opera/lawnmower/blackburne/greco/
  kill_box/reti/anderssen/mayet). Each golden-fixtured in testdata/tags/static.tsv,
  allow-listed in TaggingRegressionTest, and cross-collision-probed against the
  others before integration. mate_in_N via MateProver. Tactics: rooks_on_7th and
  a move-proven double_check.
- [DONE] chess.eval.See -- Static Exchange Evaluation (see / seeGreaterEqual),
  verified 14/14 by the workflow battery and 9/9 by an independent battery:
  x-ray/battery reveals, en passant, promotion captures, and king-defends even
  trades all correct. Not yet wired into tags.
- Rejected/dropped: morphy (collides with greco), pillsbury & suffocation (FENs
  not actually mate), h_file/boden (verifier-rejected), relative_pin (dup of
  absolute pins). A rogue subagent once wrote an unverified suffocation detector
  straight into live source -- removed; watch for agents editing source instead
  of returning detector text.

Verification workflow (keep using it): one subagent per pattern/tactic
constructs a real FEN + detector by compiling/running probes; an independent
skeptic recompiles and hunts misfires; only zero-misfire, fires-on-target
detectors integrate. Same-batch detectors are cross-collision-probed against
each other's FENs (the morphy/greco collision was caught this way). Port the
agent's detector source VERBATIM (hand-simplified ports failed to fire), and
re-derive must_contain from real Generator.tags output.

Next (in order):
- [DONE] Game-analysis evolution of the tagger (MOVE/LINE/VARIATION/GAME
  families for move-transition, multi-move tactics, multi-variation tactics, and
  whole-game narrative). Full plan in tag/GAME_ANALYSIS.md. Key insight: the
  multi-move tactics rejected statically (deflection, zwischenzug, windmill,
  combination) become groundable by REPLAYING a given move sequence - the line is
  the proof, no search. SHIPPED: chess.tag.MoveEffect (Layer 1), chess.tag.game.
  LineAnalyzer (Layer 2), chess.tag.game.VariationAnalyzer incl. tactic_shared
  (Layer 3), chess.tag.game.GameAnalyzer + analyze(Game) (Layer 4), all golden-
  tested. CLI: `fen tags --pgn <file> --analyze-game` emits whole-game analysis
  JSON. Deferred (need engine/cross-sibling grounding): MOVE_EFFECT quality=
  grade; VARIATION refutes/only_move/transposition_to; LINE zwischenzug/windmill/
  clearance/interference. The FACT:tactical->TACTIC: round-trip cleanup is NOT
  done and NOT required (it is load-bearing in Motifs, not dead code).

- [DONE/PLATEAU] Remaining chessfox checkmate nets: final batch (pillsbury,
  balestra, corridor, legal, max_lange) yielded 0 confirmed - pillsbury fired
  on non-mate positions (checker simply capturable); the others did not produce
  a clean distinct static frame (overlap opera/greco/damiano or are dynamic like
  legal). Checkmate-pattern vocabulary has plateaued at 22 named nets; further
  ones need move-sequence/dynamic detection, not single-frame geometry. Stop
  here rather than ship ungrounded tags.

- [DONE] chessfox tactics (search-gated): decoy integrated+tested+green (forcing sac, forced reply, MateProver/SEE-proven follow-up). greek_gift/deflection rejected (unsound non-forced lines); interference/clearance/zwischenzug skipped (need deeper search).

- [DONE] chessfox tactics (search-gated): decoy (forcing sac + forced reply +
  MateProver/SEE-proven follow-up) integrated, tested, green. greek_gift &
  deflection REJECTED by adversarial verifier (unsound: non-forced lines);
  interference, clearance, zwischenzug SKIPPED as not deterministically
  groundable without deeper search. These are correct conservative outcomes.

- [DONE] chessfox tactics (static batch): battery, x_ray, back_rank_weakness,
  f7_weakness in chess.tag.tactical.Motifs (grounded geometry/attacker-count),
  allow-listed, golden test + fixtures, suite green.

- [DONE] THREAT beyond promotion: type=mate (mate-in-1 + MateProver), type=
  king_attack (legal check into grounded weak shelter; suppressed when mate
  exists); chess.tag.Threats wired into Generator (non-promote tags only, to keep
  the existing promotion path + ctx flags). Golden test + fixture; suite green.
  ORIGINAL NOTE (resolved): THREAT beyond promotion: DESIGNED + adversarially VERIFIED (verdict=confirm:
  compiles, every threat grounded, mate threats proven, all moves legal, quiet
  positions clean). BUT the build agent wrote chess.tag.Threats to an isolated
  worktree that did NOT persist and returned a DESCRIPTION instead of source, so
  the artifact was lost (src/chess/tag/Threats.java is an 11-byte stub; out/ has a
  stale Threats.class with enum ThreatType{PROMOTE,MATE,MATERIAL,KING_ATTACK},
  promotion-only). Generator currently emits promotion threats via its own
  addPromotionThreatTags, NOT Threats.addThreats. TO FINISH: re-run a workflow
  that RETURNS full source in the result (forbid disk writes), or hand-build
  chess.tag.Threats.addThreats(List,Position) from this spec:
    * type=mate: m1 via copy().play(m).isCheckmate() severity=immediate; longer via
      MateProver.proveMate (Proof.bestMove():short, mateMoves():int) severity=soon
      target=mate_in_N; require bestMove in legalMoves().
    * type=king_attack: only when no mate; legal move with after.inCheck() &&
      !after.isCheckmate() AND concrete enemy-king shelter weakness (open/half-open
      file via frame-invariant pawn scan, or >=2 attacked ring squares); else none.
    * type=material: SKIP (needs search to ground).
    * preserve promote threats; field order type=,side=,severity=,move=/square=/target=;
      SAN via SAN.toAlgebraic(pos,move) with Move.toString fallback; de-dup.
    Wire Threats.addThreats into Generator.tags(); add golden test + fixture; build
    src/chess/**+src/utility/** only (external play/ tree is broken). LESSON: workflow
    build-agents must RETURN source in the result, never write repo files (their
    worktree writes do not persist).

- [DONE] IDEA family: chess.tag.Ideas.addIdeas emits grounded IDEA tags - type=
  win_material (highest-SEE legal capture), promote (legal promotion move),
  king_safety (king on open file + ring attackers, no move), space (countable
  piece-advance majority). Every IDEA cites a legal move or a re-derivable board
  fact; adversarially verified (4/4 grounding criteria) + independent exit-code
  battery. Wired in Generator.tags(); test allow-list (assertIdeaFields) +
  fixture added; full suite green.
- [DONE] CAND note enrichment: Generator.addCandidate now emits a grounded
  note="checkmate|wins material|promotes|check|" via candidateNote(...) (SEE-backed
  for captures). Suite green.
- [DONE] loses_material motif: SEE-backed tactic integrated in Motifs (addLosesMaterial),
  with a Generator pass-through for already-structured TACTIC tags; only flags LEGAL
  winning captures (pin-trap safe). Tag: TACTIC: motif=loses_material side=<loser>
  move=<uci> piece= square= see=<cp>. Test + fixture + SCHEMA + validator allow-list
  updated; full suite green.
- SEE wiring decision (resolved empirically): do NOT redefine `hanging` via See.
  Replacing isHangingPiece's "attacked && zero-defenders" with "some enemy
  capture has See.see > 0" compiles + passes fixtures but FAILS
  testDefendedAttackedPieceIsNotHanging: a DEFENDED queen captured by a lesser
  piece has positive SEE (defender loses material), which is "loses material",
  not "hanging/undefended". Keep `hanging` = undefended (current contract intact)
  and add a NEW SEE-backed motif `loses_material`/`loose` (enemy capture with
  See.see > 0 on a non-pawn, defended-or-not) via the verified workflow, with its
  own fixtures. Then SEE also feeds CAND note= ("wins material") and the
  explanation reason= fields. See.java is in place and battery-verified.
- Explanation-friendly enrichment: fill CAND note=, strengthen THREAT
  (type=mate|material|tactic|king_attack, severity, move=), emit the IDEA family
  (id reserved in Literals, not emitted yet), add grounded reason=/detail=
  fields. This is the core of the T5/explanation goal.
- Chessfox tactics via the verified workflow: static-confirmable first (battery,
  x_ray, back_rank_weakness, f7_weakness, pawn_fork, absolute/relative-pin
  refinement), then bounded-search ones (deflection, decoy, interference,
  clearance, zwischenzug, greek_gift) gated by a fixed budget with explicit
  provenance, SEE-backed where material is involved.
- Remaining chessfox mates as they yield (pillsbury, balestra, max_lange,
  corridor, legal, railroad, blind_swine, suffocation); many are dynamic, not
  single static frames, and will be rejected -- that is correct.
- Deferred cleanup: remove the FACT: tactical="..." -> TACTIC: motif=... string
  round-trip in Generator/GeneratorSupport (emit structured tags directly).

### Documentation Site

Goal: finish the GitHub Pages docs revamp and make the generated site ready to
publish.

- Publish-ready follow-through:
  - Commit regenerated docs output with generator changes when the GitHub Pages
    update is desired.
  - Smoke-test the generated site locally.
  - Run link/image checks for internal pages and generated assets.
  - Check desktop and mobile layouts for readable line length, non-overlapping
    navigation, and usable tables/code blocks.
- Keep the style restrained:
  - Maintain the neutral palette with one accent color across generated pages,
    manual HTML, and PDF output.
  - Reduce duplicated pages and stale generated fragments.
  - Keep command reference pages scannable with stable anchors and concise
    examples.
  - Keep source-of-truth content in `wiki/` where possible.
