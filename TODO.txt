Improve tagging
Add agentic commands:
- apply-move / fen-after: resulting FEN after applying a move (UCI or SAN)
- move-validate: legality check with reason on illegal
- uci-to-san / san-to-uci: move notation conversion
- legal-after: list legal replies after a move
- play-line: apply a move sequence; optional intermediate FENs
- fen-normalize: normalize and validate FEN fields
- position-diff: diff two FENs (piece/turn/castling/EP)
- pgn-slice: extract plies from PGN to FENs
- analyze-batch: analyze FEN list -> JSONL with caps
- bestmove-batch: bestmove for FEN list -> JSONL with caps

CLI and command interface improvements:
- Standardize global flags across commands: `--format`, `--quiet`, `--verbose`, `--color`, `--output`, `--stdin`.
- Standardize position input across commands: `--fen`, positional FEN where sensible, `--file`, `--pgn`, and `-` for stdin.
- Standardize output modes everywhere: human-readable text, stable tables, `--json`, and `--jsonl`.
- Standardize exit codes and error behavior: short user-facing errors by default, stack traces only with `--verbose`.
- Unify command naming around a small verb set such as `list`, `show`, `validate`, `convert`, `play`, `analyze`, and `benchmark`.
- Reduce overlapping command names and aliases; keep one canonical command path per action.
- Add shell completion for bash, zsh, and fish.
- Expand help output with examples, related commands, and grouped command categories.
- Add a central command metadata registry so help, docs, aliases, completion, and validation come from one source of truth.
- Improve composability for scripts: consistent stdin/stdout behavior, `--no-header`, `--fields`, and batch-friendly JSONL output.
- Add `position show`, `position diff`, `engine benchmark`, `engine compare`, and `selftest` as high-value workflow commands.
- Add CLI snapshot/golden tests for help output, table output, JSON output, and common error messages.
- Tighten defaults so common commands work well without excessive flags.
- Keep command handlers thin: orchestration in CLI, chess logic in core/engine packages.


fix gui situation
fix tagging situation

Repo quality improvements:
- Reduce direct `System.exit` and raw stack-trace printing in command handlers where practical so command logic is easier to test and reuse.
- Add contributor docs for coding style, test commands, release process, and expected Java version.
