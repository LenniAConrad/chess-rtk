/**
 * Offline PGN-backed study projects and editable study-tree helpers.
 *
 * <p>The package keeps PGN as the canonical chess content format and stores
 * only CRTK-only project state in optional sidecars.</p>
 */
package chess.study;
