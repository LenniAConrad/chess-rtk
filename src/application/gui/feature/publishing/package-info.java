/**
 * Publishing feature seam. This package owns the feature-facing view contract
 * and dependency ports while the existing Swing panel is migrated behind a
 * legacy adapter.
 */
package application.gui.feature.publishing;
