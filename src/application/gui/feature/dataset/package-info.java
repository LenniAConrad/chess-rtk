/**
 * Dataset feature seam. This package owns the Dataset view contract and
 * dependency ports while the existing Swing panel is kept behind an adapter.
 */
package application.gui.feature.dataset;
