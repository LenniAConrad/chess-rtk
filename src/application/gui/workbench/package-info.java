/**
 * Native Swing command and analysis workbench.
 *
 * <p>The implementation is intentionally split into feature subpackages:
 * board rendering, command building, dashboard/session state, draw controls,
 * game helpers, human-versus-engine play, network visualisation, publishing,
 * relation overlays, reusable UI controls, editor layout, and window
 * orchestration.</p>
 */
package application.gui.workbench;
