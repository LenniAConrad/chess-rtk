/**
 * Native Swing command and analysis workbench.
 *
 * <p>The implementation is intentionally split into feature subpackages:
 * board rendering, command building, dashboard/session state, game helpers,
 * network visualisation, publishing, reusable UI controls, editor layout, and
 * window orchestration.</p>
 */
package application.gui.workbench;
