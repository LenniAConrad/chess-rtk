/**
 * Main GUI window implementation layer.
 *
 * <p>Contains the composed window stack (base/history/engine/layout), board widgets,
 * dialogs, and PGN navigation primitives that are tightly coupled to runtime GUI state.</p>
 */
package application.gui.window;

