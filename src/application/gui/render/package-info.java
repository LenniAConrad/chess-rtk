/**
 * Swing cell renderers for GUI lists and tables.
 *
 * Groups custom renderers for history, PV, and report rows so UI-specific painting stays separate from window logic.
 */
package application.gui.render;
