/**
 * GUI data models and lightweight records.
 *
 * Provides command specs plus shared value objects (history rows, engine/cache payloads, palette commands) so rendering and controllers can exchange data without dragging UI state.
 */
package application.gui.model;
