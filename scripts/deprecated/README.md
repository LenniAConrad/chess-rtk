# Deprecated local scripts

This directory is for old one-off helper scripts, experiments, and generated
script caches that are no longer part of the supported automation surface.

The directory contents are ignored by git except for this README. Keep active
install, release, validation, and documented workflow scripts in `scripts/`.
