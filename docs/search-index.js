window.CRTK_SEARCH_INDEX = [
  {
    "title": "Overview",
    "href": "index.html",
    "category": "Start Here",
    "text": "ChessRTK (`crtk`) is a deterministic Java 17 chess research toolkit. One shared rules core powers the CLI, the desktop Workbench, engine workflows, puzzle mining, dataset export, board rendering, and PDF publishing. Start here Need the full setup path? Read [Getting Started](getting-started.md), [Build & Install](build-and-install.md), and [Configuration](configuration.md). The Short Version **One shared core.** FEN, SAN, UCI, legality, Chess960, perft, tags, diagrams, and books all use the same implementation. **Scriptable CLI.** Commands follow `crtk <area> <action>` and keep stable text, JSON, and JSONL output. **Desktop Workbench.** The Swing app wraps the same core for board analysis, play, command forms, datasets, publishing previews, and neural-network views. **Research workflow.** Mine puzzles, drive UCI engines, export ML tensors, and render native PDFs without Maven, Gradle, LaTeX, or glue scripts. Common Paths Documentation Map For bug reports, start with [Troubleshooting](troubleshooting.md). Include the command, input FEN or file, Java version, operating system, and engine protocol TOML when an external engine is involved."
  },
  {
    "title": "Getting Started",
    "href": "getting-started.html",
    "category": "Start Here",
    "text": "Getting Started One chess core backs everything in `crtk`: move generation, make/undo, attack detection, FEN/SAN/UCI parsing, Chess960, perft. The CLI, the desktop Workbench, dataset exports, and PDF books all call into it, which is why they never disagree about a position. The goal here is to get you from a fresh checkout to commands you'll actually use. Build or install, confirm the environment with `doctor`, then walk a short tour: print a board, list legal moves, count positions, search, open the Workbench. What you can do in five minutes Print any position from a FEN, in the terminal or as an image. List, convert, and apply moves in UCI or SAN, including full move lines. Count positions exactly with `engine perft` and validate the move generator with `engine perft-suite`. Search positions with the built-in engine, with no external process. Drive everything visually from the native Swing Workbench. Most of this runs against nothing but the core: positions, moves, perft, forced-mate search, and the built-in engine all work on a bare checkout. You only need Stockfish or LC0 for the UCI-backed commands \u2014 `engine analyze`, `engine bestmove`, `engine compare`, and puzzle mining. The neural evaluators (NNUE, LC0 CNN, OTIS) and T5 text summaries are optional too; they read local files under `models/`. Requirements The build is `javac --release 17` and nothing else \u2014 no Maven, no Gradle. Licensed GPL-3.0-only. Option A: install the launcher (Debian/Ubuntu) `./install.sh` compiles the sources, drops a `crtk` launcher on your `PATH`, and registers the Workbench as a desktop application. Ask it to, and it will also fetch optional model files and probe for GPU backends. From here on, commands look like `crtk <area> <action> [options]`, and the rest of this guide uses that form. Option B: build from source (no Maven, no Gradle) Skip the launcher and compile straight to an output directory, then run the main class. If you want the runnable JAR form, package it after compiling: You can also let the regression runner do the compile-and-package step: Wherever this page says `crtk`, `java -jar crtk.jar` works after the JAR exists, and `java -cp out application.Main` works after the classes are compiled. See [Build and install](build-and-install.md) for the full matrix, including GPU backends. Check your environment with doctor `crtk doctor` inspects your runtime version, configuration file, engine protocol, engine pool, output directory, and local model artifacts in one pass. Run it once after installing, and again whenever something behaves unexpectedly. A healthy run looks like this. Missing optional models show up as warnings, not failures \u2014 `ok-with-warnings` is still a pass: In CI, where a warning should fail the build, use `crtk doctor --strict` and it exits non-zero on any warning. The command shape Every command is a noun group plus an action. For anything structured, reach for a named flag rather than a positional. Help is available at every level, from the top down to a single action: The [Command cheatsheet](command-cheatsheet.md) lists every area and action on one page. First commands tour 1. Print a position `fen print` draws the board and the facts that go with it: side to move, castling rights, checkers, and the legal moves. `--startpos` gives you the standard opening; otherwise pass `--fen \"<FEN>\"`. For a vector board instead of text, `crtk fen render -o board.svg` writes an SVG; `crtk fen display` opens a live window. 2. List and apply moves `move list` enumerates the legal moves; pick the notation with `--format uci`, `--format san`, or `--format both`. `move after` applies one move; `move play` runs a whole line: Any of these takes `--json` or `--jsonl` when you want to pipe the result somewhere else. 3. Count positions exactly with perft `engine perft` walks the legal move tree to a given depth and counts the leaves. Since every command shares one move generator, a wrong count anywhere means a bug everywhere \u2014 which is exactly why these numbers are the project's ground truth. Depth 3 from the start position is 8902 nodes, no more and no less. To check the generator against a suite of known-good positions: Both take `--gpu`, which uses a native GPU backend when one is installed and falls back to the CPU when it isn't. 4. Search with the built-in engine `engine builtin` runs in-process \u2014 no subprocess, no socket, and the same single-thread budget always yields the same result. `--format summary` gives you something readable; bound the search with `--depth`, `--max-nodes`, or `--max-duration`. Out comes the chosen move, its score, the node count, and the principal variation in both UCI and SAN. When you want a proven forced mate rather than an evaluation \u2014 no neural net involved \u2014 reach for `engine mate`: For real playing strength, point `crtk` at Stockfish or LC0 and use `engine bestmove` or `engine analyze` (see [Configuration](configuration.md)): 5. Open the Workbench The Workbench is the same core behind a desktop GUI: an interactive board, play-vs-engine, command forms, batch jobs, dataset tools, publishing previews, puzzles, and neural-network visualizers. ![ChessRTK Workbench analysis view](../assets/screenshots/workbench-analysis.png) Pass `crtk workbench --fen \"<FEN>\"` to open it on a specific position. See [Workbench](workbench.md) for the full walkthrough. Verify the checkout Before you trust a build, or before you send changes upstream, the regression suite is the quick check. For a wider pass before publishing: Where to go next [Build and install](build-and-install.md) \u2014 full build matrix, GPU backends, and model setup. [Workbench](workbench.md) \u2014 the native desktop application in depth. [Command cheatsheet](command-cheatsheet.md) \u2014 every area and action at a glance. [Command reference](command-reference.md) \u2014 complete flag-level documentation. [Configuration](configuration.md) \u2014 engine protocols, model paths, and defaults."
  },
  {
    "title": "Build & Install",
    "href": "build-and-install.html",
    "category": "Start Here",
    "text": "Build and Install There is no build system to learn here. One `javac` invocation compiles the whole project \u2014 no Maven, no Gradle, nothing fetched from the network at build time. From a fresh checkout you are a few commands away from a working `crtk`: install a JDK, compile, optionally package a JAR, optionally run the Linux installer, and confirm with `crtk doctor`. Native GPU backends are the one moving part that lives elsewhere; they are optional and covered in [GPU Backends](gpu.md). What you get A single shared chess core: legal move generation, make/undo, attack detection, FEN/SAN/UCI handling, Chess960, and perft validation \u2014 the same core behind the CLI, the Workbench, and every evaluator. A deterministic noun-verb command-line interface (for example `crtk move list`, `crtk engine bestmove`, `crtk fen tags`). Optional external UCI engine integration (Stockfish, LC0) and optional local model weights for the built-in evaluators. The same sources compiled against the same `--release 17` target produce the same classes, and no command leans on hidden global state, so a run today and a run next month agree. See [Getting Started](getting-started.md) for your first commands. Requirements A JDK is the only hard dependency. FEN tooling, move generation, perft, tagging, dataset export, PDF publishing \u2014 all of it runs with nothing else installed. External engines and model weights buy you the workflows that need them, and nothing more. Honest note on neural-network fidelity The bundled evaluators (NNUE, LC0 CNN, OTIS) are real, deterministic position evaluators, but they are not bit-exact reimplementations of the upstream engines. The BT4 path is the rough one \u2014 simplified and still experimental. Read them as research-grade evaluators rather than stand-ins for native LC0 inference; for the strongest play and analysis, point crtk at an external UCI engine. See [LC0 Integration](lc0.md). Debian/Ubuntu packages Minimal install to build and run: Add a UCI engine only if you want external-engine analysis or puzzle mining: Reach for the optional tooling if you plan to run `./install.sh` or build the CUDA JNI backend: Notes: `git` clones the repository on a fresh machine; `ca-certificates` lets the installer fetch model weights over HTTPS. `nvidia-cuda-toolkit` is large, and you only need it to build the optional CUDA JNI backend under `native/cuda/`. ROCm/HIP and oneAPI builds bring their own vendor toolchains. See [GPU Backends](gpu.md). Build from source Two lines from the repository root. Make a directory for the classes, then compile every source file into it: Keep `--release 17`. It pins both the language level and the platform API surface, so a newer JDK still emits Java 17-compatible classes that behave the same. Drop the flag and compile against a newer API and you can hit spurious errors that have nothing to do with your code. Run straight from the compiled classes: Package a runnable JAR Bundle the compiled classes into a self-contained `crtk.jar`: The rest of this site writes commands as `crtk`, which is the launcher the installer drops on your `PATH`. Wherever you see it, `java -jar crtk.jar` does exactly the same thing. Build a self-contained Linux runtime bundle When you need a portable Linux app image that carries its own Java runtime, run the release-only jlink/jpackage script: It compiles the source with `javac --release 17`, packages `crtk.jar`, derives the required Java modules with `jdeps`, builds a minimal runtime with `jlink`, and creates an app image with `jpackage`. The staged bundle includes a root `./crtk` launcher that runs from the bundle directory, so relative `config/` and documentation paths resolve the same way they do in a checkout. The script writes: As part of the build it runs `./crtk version` from the produced bundle with an empty `PATH`, proving the launcher is using the bundled runtime rather than a system `java`. Verify the build Three commands tell you whether everything is wired up: `crtk version` prints version metadata; add `--json` for a machine-readable object: `crtk doctor` walks the runtime, the config file, the engine protocol, the configured engine instances, the output directory, and the local model artifacts, then summarizes the result as `ok` or `ok-with-warnings`: A missing optional model is a warning, not an error, because the core CLI runs fine without it. In CI you usually want the opposite stance \u2014 any warning should fail the check \u2014 so pass `--strict` and `doctor` exits non-zero: `crtk help` lists every area and action. Append a command to drill into one: Linux installer (Debian/Ubuntu) If the manual steps above feel like one too many, `./install.sh` collapses them into a single command that takes a fresh checkout to a fully wired-up install. It: optionally installs OpenJDK 17 and Stockfish via `apt-get` optionally downloads NNUE, LC0 CNN, and official LC0 BT4 model artifacts into `models/` compiles the sources with `javac --release 17` and builds `crtk.jar` installs a launcher at `/usr/local/bin/crtk` that runs from this repository installs a `ChessRTK Workbench` desktop entry that launches the Swing workbench (`crtk workbench`) without opening a terminal optionally builds the CUDA, ROCm/HIP, and oneAPI JNI backends when their vendor toolchains are present Any `models/*.bin`, `models/*.pb.gz`, and `models/*.nnue` files it downloads are local artifacts, and git ignores them. If a `crtk` command already exists on `PATH` and already points at this checkout, the installer reuses it and only replaces the Java build (`out/` and `crtk.jar`). That update path does not need sudo unless you also choose a package install, a native toolchain install, or a launcher/desktop rewrite. Run the installer, then verify: After install, open `ChessRTK Workbench` from your applications menu, or launch the same desktop app from a shell: The Workbench gives you a board, play-vs-engine, command forms, batch jobs, dataset and publishing previews, puzzles, and neural-network visualizers. See [Workbench](workbench.md). Installer flags Left alone, the installer detects what toolchains are present and acts on them. These flags take that decision out of its hands. Common combinations: None of the native GPU backends are required \u2014 build none of them and perft and OTIS quietly run on the CPU instead. See [GPU Backends](gpu.md) for `engine gpu`, `engine perft --gpu`, and the toolchain requirements. Update Fast-forward an existing checkout and rerun the installer in one step: Rebuild and reinstall the current checkout without pulling new commits: Notes: `scripts/update.sh` refuses to pull when the worktree has local changes, so an in-progress edit is never clobbered. When the installed `crtk` command already targets this checkout, `scripts/update.sh --no-pull --no-models --no-cuda --no-rocm --no-oneapi` rebuilds the jar behind that command without reinstalling the launcher. Anything it doesn't recognize passes straight through to `./install.sh` \u2014 for example `./scripts/update.sh --no-cuda`. Uninstall Remove the launcher, the desktop entry, and local build artifacts (`out/`, `crtk.jar`, `native/*/build/`): Useful flags: Regression checks after a build The same runner CI uses is yours to run locally, which is the point \u2014 what passes on your machine passes in the pipeline. From the repository root: Focused suites you can run individually: The perft smoke suite checks the in-process core move generator against stored truth values; it never launches Stockfish or any other external UCI process. Tune its depth and parallelism with environment variables: When a change you made depends on a configured external UCI engine, make the engine suite demand one rather than skip silently: Any time you touch move generation, FEN validation, SAN, make/undo, or Chess960 setup, run the full internal perft validation \u2014 that is where subtle correctness bugs hide. You can also exercise the perft path directly through the CLI: Adding `--gpu` to `engine perf"
  },
  {
    "title": "Use Cases",
    "href": "use-cases.html",
    "category": "Start Here",
    "text": "Use Cases Five very different jobs run on one set of rules here. Analyzing a position, mining tactics, building training tensors, publishing a book, scripting an agent \u2014 all of them reach through the same legal move generator, the same make/undo, the same FEN/SAN/UCI/Chess960 layer. Nothing forks its own slightly-wrong notion of a legal move. That is the whole point, and it's why the output is reproducible no matter which door you come in through. The page is sorted by who you are: find the persona that fits, take the recipe, follow the links when you want the depth. ![ChessRTK pipeline overview](../assets/diagrams/crtk-pipeline-overview.png) Every command reads as `crtk <area> <action> [options]`, noun then verb, and leans on explicit flags (`--fen`, `--input`, `--output`, `--format`) rather than positional guesswork \u2014 which is what keeps a script stable when you come back to it months later. The `fen`, `move`, and `position` primitives run in-process and ask for no engine at all. The `engine` area is where Stockfish or LC0 plug in, alongside a built-in searcher and a forced-mate prover. Personas At A Glance Chess Researchers Start with the primitives \u2014 they tell you what's legal, what a move does, what the position is \u2014 and reach for search only when you actually need it. The `fen`, `move`, and `position` commands run entirely in-process and need no configured engine, so they answer instantly and identically every time. When you want depth, the `engine` area adds UCI analysis, the built-in searcher, and a choice of evaluators: classical/static, NNUE, LC0 CNN, OTIS. A word on neural fidelity: the LC0/OTIS evaluators are honest research evaluators, not bit-exact LC0 reproductions. The BT4 path in particular is simplified and still experimental, so read its numbers as approximate rather than authoritative. Read next: [Getting Started](getting-started.md) [Command Cheatsheet](command-cheatsheet.md) [In-House Engine](in-house-engine.md) [LC0 Networks](lc0.md) ML Dataset Builders Raw PGN goes in one end, labeled tensors come out the other, and you never have to leave the toolkit to stitch the steps together. Pull FENs from games, enrich them with bounded engine analysis or puzzle mining if you want labels, then export either deterministic tensors (`record dataset npy|lc0|classifier`) or labeled JSONL rows (`record export training-jsonl`, `puzzle-jsonl`, `puzzle-elo-jsonl`). The reproducibility is not incidental: because one core normalizes every FEN, a dataset built today and the same dataset rebuilt next month are byte-for-byte identical. Read next: [Datasets](datasets.md) [Review to Study](review-to-study.md) [Filter DSL](filter-dsl.md) [Piece and Position Tags](piece-tags.md) [Outputs and Logs](outputs-and-logs.md) Players Reviewing Their Games Export a PGN, run bounded review with your configured engine, and let crtk write both the per-ply verdict stream and drillable study units. The review JSONL tells you what happened on every mainline ply; the study JSONL and Record sidecar keep only mistakes and blunders that should become practice material. Read next: [Review to Study](review-to-study.md) [Outputs and Logs](outputs-and-logs.md) [Configuration](configuration.md) [Desktop Workbench](workbench.md) Puzzle-Book Authors Your own games are full of tactics you missed. This path digs them out, verifies and tags them, and hands you a finished PDF \u2014 interior and cover both \u2014 without a LaTeX toolchain anywhere in sight. `puzzle mine` runs bounded UCI analysis over FEN or PGN seeds and sorts the results through a Filter DSL: the keepers land in `*.puzzles.json`, the rest in `*.nonpuzzles.json` so you can audit what got rejected and why. `book collection` turns the keepers into a print-ready manifest and PDF; `book cover` produces a matching cover sized to the binding you chose. Read next: [Book Publishing](book-publishing.md) [Mining Puzzles](mining.md) [Filter DSL](filter-dsl.md) [Tag Reference](tag-reference.md) Engine Testers A move generator is either correct or it isn't, and perft is how you settle the argument. `engine perft` and `engine perft-suite` walk the shared generator directly \u2014 Chess960 castling and en-passant corners included, the places where most implementations quietly go wrong \u2014 with native GPU acceleration when it's available and a CPU fallback when it isn't. `engine compare` pits two UCI protocols against the same positions, `engine benchmark` measures raw movegen throughput, and `engine mate` proves forced mates by brute force, no neural net involved and no chance of a hallucinated win. Read next: [Quality and Testing](quality-and-testing.md) [Architecture](architecture.md) [Development Notes](development-notes.md) [In-House Engine](in-house-engine.md) AI-Agent And Automation Builders An agent driving `crtk` wants two things a human can shrug off: machine-readable output and a guarantee the call will end. So pass explicit `--format`, take `--json` / `--jsonl` from the primitives, and put a leash on every engine call with `--max-duration` or `--max-nodes` \u2014 an unbounded search is a hung agent. When throughput matters, the batch commands (`engine analyze-batch`, `engine bestmove-batch`, `batch run`) chew through many FENs or commands in one invocation instead of paying startup cost per call. And since identical inputs yield identical output, you can cache aggressively and verify cheaply. For multi-step workflows, hand `batch run` a script with one `crtk` command per line: Read next: [AI Agents and Automation](ai-agents.md) [Command Reference](command-reference.md) [Command Cheatsheet](command-cheatsheet.md) [Example Commands](example-commands.md) Prefer A Desktop UI? Same core, with a window around it. The native Swing **Workbench** gives you board editing, play-vs-engine, command forms, batch jobs, datasets, logs, publishing previews, puzzle browsing, and neural-network visualizers \u2014 the CLI's behavior, made clickable for the times you'd rather see than type. Read next: [Desktop Workbench](workbench.md) [Getting Started](getting-started.md) Setup And Health Checks Five minutes spent here saves an afternoon of confusing failures later. `crtk doctor` checks the runtime, your config, the engine protocols, and the local artifacts, then tells you what's missing before a command does it for you obliquely. `crtk config show` prints the settings as actually resolved \u2014 which is the version that matters when something disagrees with what you thought you set. Read next: [Build and Install](build-and-install.md) [Configuration](configuration.md) [Troubleshooting](troubleshooting.md) [FAQ](faq.md)"
  },
  {
    "title": "FAQ",
    "href": "faq.html",
    "category": "Start Here",
    "text": "Frequently Asked Questions These are the questions that come up before anyone reads the manual: what crtk is, what it expects from your machine, what it deliberately leaves out, and how the parts connect. Every command and flag below is real and copy-pasteable. For broader orientation see [Getting Started](getting-started.md), [Command Reference](command-reference.md), and the [Glossary](glossary.md). What is ChessRTK? ChessRTK (\"crtk\") is a command-line workbench for chess research, automation, and content production. One shared chess core does the unglamorous, exacting work \u2014 legal move generation, make/undo, attack detection, FEN/SAN/UCI parsing, Chess960, perft \u2014 and everything else is built on top of it: external-engine analysis, a built-in search, several position evaluators, puzzle mining, deterministic tagging, machine-learning dataset export, and native PDF book publishing. It ships as a single `crtk.jar` and is driven entirely by `crtk <area> <action>`. Run `crtk help` to see every area. Is ChessRTK a chess engine? It contains one, but that is the smaller half of the story. `engine builtin` (alias `engine java`) is an in-process searcher, and `engine mate` is a brute-force forced-mate prover that needs no neural network at all. When you want genuinely strong analysis you hand the position to an external UCI engine through `engine analyze`, `engine bestmove`, and friends. crtk is the research platform around those engines, not a replacement for them. Do I need Stockfish or LC0? Not for the core. FEN, move, perft, tagging, the built-in engine, and the forced-mate prover all run in-process with nothing else installed. An external UCI engine \u2014 Stockfish, LC0, whatever you trust \u2014 only enters the picture for the UCI-backed commands: `engine analyze`, `engine bestmove`, `engine threats`, `engine compare`, `engine uci-smoke`, the `*-batch` variants, and `puzzle mine`. Describe your engine once in a protocol TOML, then pass it with `--protocol-path` or let it default from `crtk config show`. Does ChessRTK support Chess960? Yes, and not as a bolt-on \u2014 the support lives in the shared core, so every tool inherits it. Enumerate the 960 standard openings by Scharnagl index, draw random ones, mine Chess960 puzzles, and run the full move and FEN toolkit on any of them. Castling and move generation follow Chess960 rules where they differ. Is the output deterministic and reproducible? This is a design goal, not a happy accident. The shared core, perft, tagging, the single-thread built-in search, and the dataset and export pipelines return the same output for the same input, which is what makes results worth diffing, caching, and regression-testing. `engine perft-suite` checks move generation against stored reference node counts. The one place determinism stops is the external UCI engine: its output depends on its own threads, hash, and node or time budgets, so pin those budgets when you need byte-stable engine results. Does ChessRTK need a GPU? No. CPU is the default and the floor. The native backends \u2014 CUDA, ROCm, oneAPI \u2014 optionally accelerate perft and the OTIS/LC0 evaluators, and when none is available crtk quietly falls back to CPU rather than failing. `engine gpu` reports what actually loaded on your machine; the `--gpu` flag opts perft into the accelerated path. What neural networks are supported? Several evaluators are selectable through `engine eval`, `engine builtin`, and the workbench: classical/static, NNUE, an LC0 CNN, and OTIS. T5 natural-language models drive position summaries through `fen text` and `puzzle text`. A caveat worth stating plainly: these are usable evaluators, not bit-exact reproductions of the upstream engines, and the BT4 path especially is simplified and experimental. Drop local weight files under `models/` and point at them with `--weights`, or `--model` for the T5 models. Does book publishing require LaTeX? No \u2014 no LaTeX, TeX, or external typesetting toolchain of any kind. The `book` commands render print-ready PDFs natively: dense puzzle collections with `book collection`, annotated studies with `book study`, diagram sheets with `book pdf`, full interiors with `book render`, and matching covers with `book cover`. What Java version does ChessRTK require? Java 17 \u2014 that exact language and class-file level. There is no Maven or Gradle anywhere in the build, so a JDK and `javac` are all you need. See [Build and Install](build-and-install.md) for the full procedure. How is ChessRTK licensed? Free and open-source software under the **GPL-3.0-only** license. The full text is in `LICENSE.txt` at the repository root. Is there a GUI? Yes. `crtk workbench` (alias `crtk gui`) opens a native desktop Swing application over the toolkit: an interactive board, play-vs-engine, command forms for the CLI areas, batch jobs, dataset tools, logs, publishing previews, puzzle views, and neural-network visualizers. Play mode uses in-process opponents \u2014 alpha-beta or MCTS with local evaluators \u2014 while the live-analysis controls drive a configured external UCI engine. What is a `.record` file? It is crtk's reusable analysis-record JSON format, and the hub of the data pipeline. `record` commands summarize records (`record stats`, `record tag-stats`, `record analysis-delta`), merge, filter, and split them (`record files`), export them to other formats (`record export plain|csv|pgn|puzzle-jsonl|puzzle-elo-jsonl|training-jsonl`), and turn them into ML tensors (`record dataset npy|lc0|classifier`). Most record and puzzle commands take a Filter DSL through `--filter` to pick rows. How do I check my setup? `crtk doctor` inspects your Java version, configuration, engine protocol, and local artifacts in one pass. Add `--strict` to exit non-zero on warnings \u2014 useful when the check runs in CI. See also [Troubleshooting](troubleshooting.md) and [Configuration](configuration.md) for deeper diagnostics."
  },
  {
    "title": "Troubleshooting",
    "href": "troubleshooting.html",
    "category": "Start Here",
    "text": "Troubleshooting Most things that go wrong with crtk go wrong in one of seven places: the launcher or `PATH`, the runtime version, an external UCI engine that was never configured, a perft mismatch, a build error, missing model files, or a native GPU backend that failed to load. This page walks each one by symptom and gives the fix. Two commands diagnose nearly all of them in seconds, so start there. Start here: two diagnostic commands Both are read-only and deterministic. They report what crtk actually resolved on this machine, not what you assume it did. `crtk doctor` \u2014 checks the Java version, the resolved config and engine protocol files, the engine instance count, the output directory, and whether local model artifacts exist. It prints `ok`, `ok-with-warnings`, or an error, then lists each warning. `crtk config validate` \u2014 validates `config/cli.config.toml` and the referenced engine protocol TOML and reports the first problem it finds. `crtk doctor --json` and `crtk config show --json` \u2014 emit stable machine-readable objects for scripts, issue reports, and the Workbench setup/health surface. A healthy machine prints something like this. Warnings \u2014 a missing optional T5 model, say \u2014 are non-fatal: `doctor` reports `ok-with-warnings` and everything else keeps working. In CI or scripts, `crtk doctor --strict` turns warnings into failures \u2014 it exits non-zero the moment one is present. Add `--json` when the caller needs the `crtk.doctor.v1` status, warning/error arrays, and native backend matrix without scraping text. See [Configuration](configuration.md) for what each value means and [Build and Install](build-and-install.md) for installation details. Launcher or `PATH` issues **Symptom:** `crtk: command not found`, or the `crtk` launcher runs an old build. Start with `which crtk`. If nothing prints, the wrapper is not on your `PATH`. The installer places the launcher for you. Re-run `./install.sh` from the repository root (add `--no-desktop` to skip the desktop entry), then open a new shell so the updated `PATH` takes effect. Installing with `./install.sh --no-launcher` skips the wrapper deliberately \u2014 invoke the toolkit directly, e.g. `java -jar crtk.jar doctor` from the repo root. Every `crtk <area> <action>` in these docs maps one-to-one to `java -jar crtk.jar <area> <action>`. If `crtk` runs but acts stale, check `crtk version`. Rebuild (see \"Build errors\" below), then verify again. Java version errors **Symptom:** `UnsupportedClassVersionError`, `class file has wrong version`, or crtk refuses to start. crtk targets Java 17 and is compiled with `javac --release 17`, so it runs on that release or anything newer \u2014 21 is fine, as the `doctor` output above shows. See what you actually have with `java -version`. If it is older than 17, install a 17+ JDK/JRE and make it the default, or point `JAVA_HOME` at it. The `Java:` line from `crtk doctor` reports the version crtk resolved. When the launcher and your shell disagree on which runtime they pick, that line is where the disagreement shows. A stock JDK is all you need to build and run. No Maven, no Gradle, no third-party dependency to resolve. UCI engine not configured **Symptom:** an `engine` command that needs an external engine fails to start a process, hangs, or reports a missing binary. This affects `engine analyze`, `engine bestmove` (and `bestmove-uci` / `bestmove-san` / `bestmove-both`), `engine threats`, `engine compare`, `engine analyze-batch`, `engine bestmove-batch`, and `engine uci-smoke`, plus any tagging command run with `--analyze`. `crtk config validate` reads the engine protocol TOML named by your config and flags a missing or non-executable binary \u2014 usually the whole story. That protocol TOML names the engine and its `path = ...`. Confirm the path exists and is executable. If the engine lives on your `PATH`, `which stockfish` or `which lc0` tells you where. To debug against a specific protocol without touching your default, point one command at it with `--protocol-path /path/to/engine.toml` (short form `-P`). Isolate the wiring with the smallest possible search: To take the external engine out of the picture, use commands that never spawn a UCI process: `engine static` and `engine eval` (local evaluators), `engine builtin` / `engine java` (built-in searcher), `engine mate` (forced-mate prover), `move list`, `engine perft`, and `engine perft-suite`. If those run and `engine analyze` does not, the fault is in the external-engine configuration, not the chess core. See [Configuration](configuration.md) and [LC0](lc0.md) for protocol setup. Perft mismatches **Symptom:** `engine perft-suite` reports a failing row, or `engine perft` returns a node count that disagrees with a known reference. `engine perft-suite` checks stored truth values against crtk's own move generator \u2014 or the native GPU backend when `--gpu` is set. No Stockfish, no external UCI process. A mismatch therefore implicates the core move generation, or on the GPU path, the device backend. Re-run the failing position on its own and break it down per root move: When a GPU row fails, separate the device backend from the core logic: run the position once with `--gpu --divide --split <n>`, then again without `--gpu`. A correct CPU run alongside a wrong GPU run pins the bug to the native backend. After code changes, mismatches usually trace to FEN parsing, Chess960 castling metadata, en-passant handling, SAN/UCI move application, make/undo, or attack detection. One chess core backs every command, so a single regression in any of those surfaces in many places at once. Rule out a malformed input first with `crtk fen validate --fen \"<FEN>\"`. For a fast end-to-end check of the generator's throughput and correctness on the start position, use `crtk engine benchmark --startpos --depth 5`. Build errors **Symptom:** `javac` errors, or the build produces a jar that does not run. Build against the Java 17 release target. A bare newer JDK without `--release 17` can throw spurious errors; the supported path is `javac --release 17`. The installer does this for you \u2014 re-run `./install.sh` from the repository root to compile and assemble `crtk.jar`. There is no `pom.xml` and no `build.gradle`. If you are looking for one, you are on the wrong path: the build is plain `javac` driven by `install.sh`. Once it builds, confirm the artifact runs. `java -jar crtk.jar version` prints the version; `crtk doctor` reports `ok` or `ok-with-warnings`. The native GPU libraries (CUDA / ROCm / oneAPI) are a separate, optional compile step behind the installer flags below. A failure there leaves the pure-Java build intact, and crtk falls back to CPU on its own. See [Build and Install](build-and-install.md) for the full procedure. Model files missing **Symptom:** a command that needs local neural-network weights warns that default weights are missing and falls back to a classical heuristic, or a T5 command cannot find a model. Model binaries stay out of git and out of a fresh clone, so a new checkout reports them missing until you fetch them. This is expected, not a fault. `crtk doctor` names each expected-but-missing model in its warnings \u2014 the `t5-model-path` line above is one. Fetch the default bundled weights in one step: That pulls the NNUE weights (`models/crtk-halfkp.nnue`), the ChessRTK LC0 CNN `.bin` weights, and the LC0 BT4 `.pb.gz` network into `models/`. The LC0 CNN and OTIS evaluators read their weights from `models/` and fall back to the classical heuristic when the files are gone. So on a fresh clone, `engine eval`, `fen display --ablation`, and `fen render --ablation` quietly use the classical backend until you fetch the weights. Override any default by naming weights explicitly: `engine builtin --nnue --weights /path/to/file.nnue`, `engine eval --lc0 --weights /path/to/file.bin`, or `engine eval --otis --weights /path/to/file`. Stockfish-format NNUE files work with the NNUE evaluator. For T5 natural-language summaries (`fen text`, `puzzle text`), "
  },
  {
    "title": "Cheatsheet",
    "href": "command-cheatsheet.html",
    "category": "Commands",
    "text": "Command Cheatsheet Every command here is one line you can paste, edit, and run. They share a single noun-then-verb shape (`crtk <area> <action>`) and a single chess core, so the same input gives the same output every time. Replace `\"<FEN>\"` with a real position, and keep the quotes \u2014 FENs contain spaces. When you need the full option list for any command, `crtk help <area> <action>` has it (try `crtk help engine bestmove`), as does the [Command Reference](command-reference.md). On a fresh checkout, run `crtk doctor` first. It tells you what works before you waste time on what doesn't. 60-Second Smoke Test Setup And Diagnostics FEN And Board Inspection Tags And Natural Language Tags name what a position contains \u2014 tactics, themes, structural features \u2014 and they come straight from the chess core. The optional T5 path turns those facts into prose. The text comes from the bundled simplified T5 path. The tags do not \u2014 they are derived from the position, never sampled, so they stay stable across runs. Moves `crtk fen after` and `crtk fen line` are aliases for `move after` and `move play` \u2014 same behavior, whichever noun you reach for. Engine Analysis And Search Three kinds of engine live here, and the verb tells you which. `engine analyze`/`bestmove`/`threats`/`compare` talk to an external UCI engine \u2014 Stockfish, LC0. `engine builtin`/`java` run the in-process built-in engine, no external process required. `engine mate` proves forced mates by brute force, and `engine static`/`eval` just score a position. A protocol TOML file tells crtk how to launch and talk to each UCI engine. Pass `--protocol-path engine.toml` to override the configured default for one run. Movegen Verification (Perft) The native backends (CUDA/ROCm/oneAPI) accelerate perft and OTIS. `--gpu` is safe to leave on: with no backend present it falls back to the CPU rather than failing. `crtk engine gpu` shows you what's actually available. Generating Positions `gen fens` is an alias for `fen generate`. Filters combine with AND, so each one you add narrows the output further. The full set \u2014 presets, move-state, count, and material \u2014 is in `crtk help fen generate`. Puzzle Mining `puzzle mine` hunts for tactical lines, then runs each candidate through a Filter DSL that decides what counts as a puzzle. The keepers land in `<root>.puzzles.json`; everything rejected goes to `<root>.nonpuzzles.json`, which is worth keeping \u2014 it doubles as a negative training set. Records, Stats, And Export ML Dataset Export Publishing (Native PDF, No LaTeX) Batch Runner Each non-comment line is one full `crtk` command. By default a failing line stops the run; `--keep-going` pushes past it and reports the failures at the end. Desktop Workbench The Swing Workbench is the CLI with a face on it: board play, command forms, batch jobs, dataset tooling, logs, publishing previews, puzzles, and the neural-network visualizers \u2014 all driving the same core. Agent-Friendly One-Liners When something downstream has to parse the output, reach for these: structured, stable, and free of anything an LLM or a script would have to guess at. See Also [Command Reference](command-reference.md) \u2014 full option-by-option documentation. [Getting Started](getting-started.md) \u2014 install, build, and first runs. [Example Commands](example-commands.md) \u2014 worked end-to-end recipes. [Configuration](configuration.md) \u2014 config files and engine protocols."
  },
  {
    "title": "Command Reference",
    "href": "command-reference.html",
    "category": "Commands",
    "text": "Command reference One command-line interface drives every feature of ChessRTK (\"crtk\"), and one chess core sits underneath all of it. A FEN that validates in `fen validate` parses the same way in `move list`, `engine perft`, the workbench, and every dataset exporter \u2014 same parser, same move generator, same notation, so the same input always yields the same output. This page lists every area, subcommand, and option, grouped so you can find a command, copy its example, and trust the result to reproduce on the next machine. Invocations follow `crtk <area> <action> [options] [args]`. From a build tree rather than the installed launcher, that becomes `java -jar crtk.jar <area> <action> ...` or `java -cp out application.Main <area> <action> ...`. For anything this page leaves vague, `crtk help --full` prints the authoritative spec, and per-command help is either `crtk help <area> <action>` or `crtk <area> <action> --help`. Adding or changing commands? Use the [CLI Command Guide](cli-command-guide.md) for implementation, help, docs, and regression requirements. Command style and global conventions The grammar is noun-verb: an *area* (the noun, such as `engine` or `record`) followed by an *action* (the verb, such as `bestmove` or `export`). A few areas add a second noun level, as in `record export plain` or `record dataset npy`. In scripts, spell out the full grouped form \u2014 it survives renames and reads clearly six months later. Structured values go through named flags. `--fen`, `--input`/`-i`, `--output`/`-o`, and `--format` mean the same thing everywhere they appear. When scripting, put options before free-form positional args, and guard anything that could be mistaken for a flag \u2014 a FEN, a SAN move, a negative number \u2014 with a leading `--`. Position commands take the FEN positionally or via `--fen`; `--startpos` gives the standard start, `--randompos` a reachable random legal position. `--verbose`/`-v` turns a failure into a full stack trace on nearly every command. The lists below leave it out unless it is the only option worth mentioning. Row-oriented commands share `--json`, `--jsonl`, `--no-header`, and `--quiet`. Engine-backed commands share one option block: `--protocol-path`/`-P`, `--max-nodes`/`--nodes`, `--max-duration`, `--multipv`, `--threads`, `--hash`, and `--wdl`/`--no-wdl`. Most of the surface never touches an external engine. The in-process commands (`fen`, `move`, `position`, `engine benchmark`, `engine perft`, `engine perft-suite`, `engine mate`, `engine builtin`, `engine java`, `engine static`) run start to finish inside the JVM. A configured UCI engine is required only for the workflows that delegate search: `engine analyze`, the `engine bestmove*` family, `engine analyze-batch`, `engine bestmove-batch`, `engine compare`, `engine threats`, `engine uci-smoke`, the `--analyze` tagging paths, and `puzzle mine`. Areas at a glance record A `.record` file is the canonical container for analyzed positions, puzzle trees, and engine PVs \u2014 JSON, or JSONL when you want streaming. Everything that produces analysis writes records; the `record` area is how you get them back out. It exports rows to text and tensor formats, merges, filters, and splits files, and summarizes what they hold. Spell out the grouped forms (`record export ...`, `record dataset ...`) in scripts. record export plain Convert a `.record` JSON array into Leela-style `.plain` blocks (mainline only by default). record export csv Convert a `.record` JSON array directly to CSV (no `.plain` output). record export pgn Convert a `.record` JSON array into one or more PGN games. record export puzzle-jsonl Export `.record` rows as puzzle JSONL annotated with LC0 policy values. The policy map comes from the network, so this one needs ChessRTK LC0 CNN `.bin` weights. record export puzzle-elo-jsonl Export verified puzzle records as JSONL with Elo-style difficulty tags. By default it scores the puzzle trees in-process; if you already have a scored ratings CSV, point at it with `--ratings-csv` and the export skips straight to a single pass. Takes multiple inputs and directories. record export training-jsonl Flatten one or more record files into one-position-per-line training JSONL. Labels fall into three tiers: a row matching the puzzle DSL gets `coarse_label=1`/`fine_label=2`; a row that shares a parent FEN with a puzzle gets `coarse_label=1`/`fine_label=1`; everything else gets `coarse_label=0`/`fine_label=0`. Engine metadata never reaches the model \u2014 it stays out of the feature columns by design. record dataset npy Convert a `.record` JSON array into NumPy tensors: `<stem>.features.npy` shaped `(N, 781)` float32 and `<stem>.labels.npy` shaped `(N,)` float32 (pawns). record dataset lc0 Convert a `.record` JSON array into LC0-style tensors: `<stem>.lc0.inputs.npy` `(N, 112*64)`, `<stem>.lc0.policy.npy` `(N, policySize)` one-hot, `<stem>.lc0.value.npy` `(N,)` in `[-1,1]`, plus `<stem>.lc0.meta.json`. record dataset classifier Convert one or more record files into tensors for the one-logit binary classifier: `<stem>.classifier.inputs.npy` `(N, 21*64)`, `<stem>.classifier.labels.npy` `(N,)` (`0.0` negative, `1.0` positive), plus `<stem>.classifier.meta.json`. Labeling without `--label-filter` follows whatever the records carry: an explicit `kind:\"puzzle\"`/`kind:\"nonpuzzle\"` wins if present, and the configured puzzle verification filter decides the rest. record files Merge, filter, and split record files. record stats Summarize a `.record` or puzzle JSON/JSONL dump (counts, top tags, top engines). record tag-stats Summarize tag distributions in a dump. record analysis-delta Export one JSONL row per record with evaluation-stability metrics: initial and final eval, delta type/value, fluctuation range, and time/depth to final value. fen Everything that begins with a FEN lives here: validation, normalization, generation, PGN conversion, Chess960 enumeration, board rendering, tagging, and text. They all share the same core, so a FEN normalized here is byte-for-byte the FEN every other command expects. fen validate Validate a FEN; prints `valid<TAB><normalized-fen>` on success. fen normalize Parse a FEN and print it back in the canonical form the core uses internally \u2014 useful when two FENs should be equal but a stray castling-rights or move-counter difference says otherwise. Same I/O options as `fen validate`. fen after / fen line `fen after` is an alias for `move after`, and `fen line` (also `fen play`) is an alias for `move play`. See [move after](#move-after) and [move play](#move-play) for the full option set. fen generate Write random legal FEN shards to disk, mixing standard and Chess960 starts. Filters stack with AND \u2014 every condition you name has to hold, so a tight combination can reject most candidates and lean on `--max-attempts`. `gen fens` is an alias for this command. Stage presets: `--stage NAME` (`endgame`, `late-endgame`, `king-pawn`, `minor`, `rook`, `queenless`), `--endgame`, `--late-endgame`, `--king-pawn-endgame`, `--minor-endgame`, `--rook-endgame`, `--queenless`, `--opposite-bishops`. Move-state filters: `--side white|black|w|b`, `--in-check`, `--not-in-check`, `--checkmate`, `--stalemate`, `--en-passant`/`--ep`, `--promotion`, `--underpromotion`, `--capture`, `--castle-rights`, `--legal-castle`. Count and material filters (each supports an exact value plus `--min-*`/`--max-*` forms): `--pieces N`, `--white-pieces N`, `--black-pieces N`, `--pawns`/`--knights`/`--bishops`/`--rooks`/`--queens N`, side-specific forms such as `--white-rooks N`/`--black-rooks N`, `--material N`, `--material-diff N` (White minus Black, cp), `--max-material-imbalance N` (max absolute difference, cp), `--legal-moves N`, `--fullmove N`, `--halfmove N`. fen pgn Convert PGN games into a FEN list (usable as seeds). fen chess960 Print Chess960 starting positions in Scharnagl index order. fen print Pretty-print a FEN as ASCII (board, metadata, and tags). fen display Render a board image in a window, with optio"
  },
  {
    "title": "Command Guide",
    "href": "cli-command-guide.html",
    "category": "Commands",
    "text": "CLI Command Guide This guide explains how to add or change a ChessRTK CLI command. The CLI is not a loose argument parser. It is a deterministic noun-then-verb command surface backed by one shared chess core, central command metadata, built-in help, regression tests, and generated documentation. Use this guide whenever you add a new command, rename a command, add a flag, change output, or expose a workflow through the Workbench command builder. Command Design Rules Never reintroduce legacy single-word commands like `moves`, `bestmove`, `eval-static`, `pgn-to-fens`, `mine-puzzles`, `record-to-csv`, or `stats-tags`. If an alias is unavoidable, make it a compatibility alias that resolves to the canonical command and prints canonical help. Source Files Common helper classes: Implementation Process **Name the command.** Pick the canonical area/action path and decide whether it belongs under an existing group. **Define flags and output.** Write down required inputs, optional flags, defaults, output format, failure modes, and whether JSON/JSONL is supported. **Add constants if reused.** Put shared command tokens and option names in `Constants.java`. **Write the command handler.** Add or update a focused class under `src/application/cli/command/`. **Register the command.** Add a `CliCommand.leaf(...)` or group entry in `CliRegistry.java` with usage, about text, detail help key, examples, and related commands. **Add the help block.** Add a marker and option section in `HelpCommand.java`; map the command's `detailHelpKey(...)` to that marker. **Update docs.** Update `wiki/command-reference.md`; update examples/cheatsheet/config docs when the command affects those surfaces. **Expose through Workbench if useful.** Make sure command forms can discover or represent it when the command is user-facing. **Add tests.** Extend `CLICommandRegressionTest` or a more focused regression class. **Run verification.** Compile, run CLI tests, run docs if docs changed, then run the recommended suite before publishing broad changes. Handler Pattern Command handlers are static methods that accept `Argv` and return by printing to stdout or throwing a failure. They do not call `System.exit`. Parsing rules: Call typed `Argv` accessors once for every flag you support. Call `positionals()` before `ensureConsumed()` only if the command accepts positional arguments. Read all non-position flags before `CommandSupport.resolveFenArgument(...)` or `CommandSupport.resolvePositionArgument(...)`; those helpers consume position input and call `ensureConsumed()`. Make sure `ensureConsumed()` runs after parsing. Call it yourself unless a shared resolver already did it. Reject mutually-exclusive inputs before doing engine/model/file work. Prefer `CommandFailure` for command-specific failures with a clear command label and exit code. Let `IllegalArgumentException` handle simple parser failures from `Argv`; `Main` maps it to exit code `2`. Default-on flag pattern: This accepts `--analyze` and `--no-analyze`, defaults to enabled, and rejects contradictory repeats. Registry Pattern Register every executable command in `CliRegistry.java`. The registry controls dispatch, summary help, contextual help, aliases, examples, related commands, and command form discovery. Registry rules: The leaf name is the canonical action token. `summary` is one short sentence fragment for grouped help. `usage(...)` is only the tail after the command path. `about(...)` explains the command in one or two direct sentences. `detailHelpKey(...)` must match the help marker mapping in `HelpCommand`. `example(...)` entries must be copy-pasteable and deterministic. `related(...)` entries are command paths without the `crtk` prefix. Use aliases sparingly and only under the same parent. Aliases are for compatibility, not new naming. Help Block Pattern For each command with options, update `HelpCommand.java`. Add a marker constant: Add it to `HELP_MARKERS`: Add a section to `HELP_FULL_TEXT`: Help block rules: The marker line must exactly match the mapped marker string. Option rows use stable spacing and short descriptions. The help block must list every public flag and short alias. If the command supports JSON/JSONL, say so. If a command has aliases, contextual help should still show the canonical command path. Run `crtk help <area> <action>` after the change and read it as a user would. Input Rules Do not parse FENs, SAN, UCI, PGN, TOML, JSON, paths, or durations by hand when a shared parser exists. Output Rules Text output: Keep line order stable. Keep columns and separators stable. Prefer tabs for simple machine-readable text rows. Do not print progress or warnings to stdout if stdout is the data stream. Include enough context for a human to understand the result without hidden state. JSON/JSONL output: Use `utility.Json`. Keep field names lowercase, stable, and explicit. Use JSON for one object or summary; use JSONL for rows. Do not mix human prose with JSON stdout. Add regression assertions for important fields, not just substring smoke tests. Errors: Use `CommandFailure` for domain failures: invalid combination, missing input, unreadable file, unsupported format. Use exit code `2` for usage/input failures unless an existing command family uses a more specific code. Keep stderr concise. Include `--verbose` only when stack traces or internal details are useful. Determinism Checklist Before shipping a command, verify: Same input and flags produce the same stdout. Row ordering is sorted or follows core deterministic order. Engine work is bounded by nodes, depth, duration, or explicit user budget. Random sampling is explicit, seeded, or named as random in the command. Output paths are deterministic or user-provided. JSON field order is stable enough for regression tests. No wall-clock timestamp appears unless the user requested a run log or artifact metadata. Optional engines/models report availability honestly and fail or fall back consistently. Workbench Command Forms The Workbench command builder mirrors the CLI. A new command should be easy to discover from the GUI when it is user-facing. Check these files when a command should appear in forms: The generated command preview must be the exact runnable command. Do not add UI-only defaults that the CLI cannot reproduce. Regression Tests Use `TestSupport.runMain(...)` for successful command checks and `TestSupport.runMainExpectFailure(...)` for failures. Minimum coverage: Verification Commands Compile: Run focused CLI tests: Check the new command manually: Regenerate docs after wiki changes: Before publishing a broad CLI change: Review Checklist A CLI command change is ready only if every answer is clear: Related Pages [Command Reference](command-reference.md) - every current command, action, and flag. [Command Cheatsheet](command-cheatsheet.md) - common commands by task. [Example Commands](example-commands.md) - end-to-end command recipes. [Architecture](architecture.md) - how the CLI sits over the shared core. [Development Notes](development-notes.md) - broader contributor conventions. [Quality and Testing](quality-and-testing.md) - regression suite details."
  },
  {
    "title": "Example Commands",
    "href": "example-commands.html",
    "category": "Commands",
    "text": "Example Commands Every recipe below is something you can paste into a terminal and watch run to completion: study one position, verify the move generator, mine puzzles from a PGN, export training datasets, publish a puzzle book, run batch analysis. They lean on a single shared chess core, so move generation, FEN/SAN/UCI handling, and tagging behave the same whether the call comes from the CLI, a batch script, or the desktop [Workbench](workbench.md). Same inputs and flags, same output \u2014 which is what makes these recipes worth saving and rerunning. The examples assume the `crtk` launcher is on your `PATH`. Running from compiled classes instead? Swap `crtk` for `java -cp out application.Main`. See [Getting Started](getting-started.md) for installation and [Command Reference](command-reference.md) for the full option list. Conventions Replace `<FEN>` with a real FEN string in double quotes. Put named flags before free-form positional arguments when scripting, and use `--` if a value could look like a flag. Engine-backed commands (`engine analyze`, `engine bestmove`, `puzzle mine`) use an external UCI engine resolved from your config or `--protocol-path`. Run [`crtk doctor`](troubleshooting.md) first if engine discovery is uncertain. Bound every engine search with `--max-duration` or `--max-nodes` so runs are predictable. Study One Position A position is worth looking at from several angles before you trust any single read of it. Pretty-print the board, list the legal moves in SAN, get a deterministic textual description, then hand it to a UCI engine. `fen print` and `move list` never touch an engine; they run in the core. `position describe` emits deterministic text from static analysis. Only `engine analyze` shells out \u2014 to the configured UCI engine \u2014 and prints the top three principal variations. Visualize and Tag the Same Position The same position, seen rather than read: open it in a window with overlays, write a board image to disk, then emit deterministic tags and a natural-language summary. `fen tags` produces a JSON tag set from the static tagger; add `--analyze --max-duration 2s --multipv 3` to fold engine output into the tags. `fen text` runs the T5 summarizer over those tags and returns a plain-English sentence. Treat that sentence as a readable gloss, not as analysis \u2014 T5 describes the tags it was given, it does not reason about the position. Verify the Core (perft-suite) Before you trust anything downstream, confirm the move generator counts correctly. The suite checks it against stored ground-truth node counts across standard, Chess960, promotion, en-passant, castling, and stress positions \u2014 no external engine involved. A clean pass means the move generator is sound on this build \u2014 and since every other workflow inherits its determinism from the core, this is the check worth running first. For one position with the full set of counters (nodes, captures, en-passant, castles, promotions, checks, checkmates), or a per-root-move breakdown, reach for `engine perft`: The `--gpu` form routes node counting through the optional native CUDA/ROCm/oneAPI backend, and quietly falls back to CPU when no usable backend is present \u2014 so it never fails just because the hardware isn't there. Confirm what you actually have with `crtk engine gpu`. See [In-House Engine](in-house-engine.md) and [Architecture](architecture.md) for details. Mine Puzzles From a PGN A PGN of games is a seam of tactics waiting to be cut out. crtk walks candidate positions, runs the configured engine, and gates each line through its Filter DSL quality system \u2014 what survives is a puzzle. Accepted lines land in `dump/pgn.puzzles.json`, rejected ones in `dump/pgn.nonpuzzles.json` \u2014 keep the rejects; they make excellent negatives for a classifier. Other seed sources follow the same shape: With a dump in hand, summarize it, turn it into PGN you can scroll through in any viewer, and look at how the tags break down: To merge or filter many mined files into one dump, use `record files` with the Filter DSL. See [Use Cases](use-cases.md) for the full puzzle-mining playbook. Export ML Datasets Records become training data once you encode them. The exporters all draw on the same FEN encoder, so a plane means the same thing in every format \u2014 no per-exporter drift in layout or labels to debug later. `dataset npy` writes a paired `puzzles.features.npy` + `puzzles.labels.npy`. `dataset classifier` takes both the puzzle and non-puzzle sources and writes plane inputs with 0/1 labels. `dataset lc0` emits LC0-style input, policy, value, and metadata tensors \u2014 pass `--weights` to compress the policy map. For text-pipeline training, export JSONL instead: One caveat on the LC0 path: it is a serviceable encoder/evaluator for generating datasets, not a bit-exact reproduction of LC0/BT4 inference. If your pipeline depends on matching Leela's numbers exactly, read [LC0 Integration](lc0.md) for the fidelity notes first; [Configuration](configuration.md) covers weight paths. Publish a Puzzle Book Analyzed records go straight to a print-ready PDF, drawn as native vectors. No LaTeX, no typesetting toolchain to install and placate. `book collection` builds a TOML manifest from the records, and when given `--pdf-output`/`--cover-output` renders the matching interior and cover in the same pass. A full render is slow enough that you'll want to catch bad layout, FENs, or solution lines before paying for it \u2014 `--check` does exactly that: For richer annotated studies, reach for `book study`. For a quick diagram sheet off a FEN list or PGN, `book pdf` is the short path: Batch Analysis A thousand positions analyzed one process-launch at a time is a thousand cold starts. The JSONL batch commands keep the engine warm across the whole list, which is why they're the fastest path for a FEN file: Both emit one JSON object per line \u2014 pass `--json` for a single array instead. To put two engines or protocols on the same positions and see where they disagree, use `engine compare`: Script Many Commands With `batch run` When a pipeline mixes everything \u2014 convert, mine, export, publish \u2014 you don't want it scattered across shell history. Put one `crtk` command per line in a file and let `batch run` drive it: `pipeline.crtk` is a UTF-8 file, one command per non-comment line: `--keep-going` presses on after a command exits non-zero, `--quiet` drops the command echo, and `--stdin` reads the script from standard input. A whole research run, captured in one file you can rerun next month and trust to do the same thing. See Also [Command Reference](command-reference.md) \u2014 every area, action, and flag. [Command Cheatsheet](command-cheatsheet.md) \u2014 terse one-liners by task. [Getting Started](getting-started.md) \u2014 install and first run. [Use Cases](use-cases.md) \u2014 deeper puzzle-mining and dataset workflows. [Workbench](workbench.md) \u2014 drive all of the above from the desktop GUI."
  },
  {
    "title": "Configuration",
    "href": "configuration.html",
    "category": "Commands",
    "text": "Configuration Most of crtk needs no configuration at all. FEN handling, `move list`, SAN/UCI conversion, `engine perft`, `engine perft-suite`, `fen tags` without `--analyze`, `engine static`, `engine mate`, the built-in search (`engine builtin`), and the Workbench Play tab run entirely in-process \u2014 no external program, no model file, nothing to set up. Configuration enters the picture only at the edges: when you drive an external UCI engine (Stockfish/LC0), load a neural-network evaluator, ask for T5 natural-language summaries, want opening tags, or tune puzzle mining. Settings live in plain TOML files under `config/` and weight files under `models/`. This page documents each of them, how a value is resolved when several sources disagree, and how to confirm a setup with `config show`, `config validate`, and `doctor`. At a glance Two terms used throughout: **CLI config** means `config/cli.config.toml`, the single file that holds defaults for the whole tool. **Engine protocol** means an `*.engine.toml` file that describes how to drive one external UCI engine. CLI config: `config/cli.config.toml` Loaded once at startup. If it isn't there, crtk writes it out with documented defaults, so a fresh clone is runnable before you've touched anything. Every key also has a compiled-in fallback: a malformed value logs a warning and reverts to that fallback rather than killing the run, which means a typo costs you a setting, not the whole session. Keys TOML allows underscores in numbers, so the shipped file writes `max-nodes = 50_000_000` \u2014 the digit grouping makes a node budget legible at a glance. Output root behavior The `output` value \u2014 and the `--output` flag on commands that write files \u2014 is read by its shape, not by a separate mode switch: A directory such as `dump/` collects timestamped files inside it. A file-like root ending in `.json` / `.jsonl`, e.g. `run.json`, becomes a stem: `puzzle mine` writes `run.puzzles.json` and `run.nonpuzzles.json`. An empty string `\"\"` sends output to stdout where the command supports it. Puzzle mining filters The four `puzzle-*` keys are [Filter DSL](filter-dsl.md) strings that decide whether an analyzed position survives as a candidate puzzle. Each reasons over per-PV facts \u2014 `eval`, `nodes`, `break=N` for the Nth principal variation \u2014 joined with `gate=AND` / `gate=OR`. The shipped defaults are calibrated for Stockfish-style centipawn evals: Eval scale, contempt, and pruning all differ between engines, so these thresholds are starting points to tune, not constants. The shipped `config/cli.lc0.config.toml` softens them (`eval>=150`, `nodes>=6000000`) for LC0's compressed evals. Any of the four can be overridden for a single run without touching TOML: The filters compare PV1 against PV2, so the mining protocol must report at least MultiPV 2. The shipped protocol files already set `multipv` to 2 in their `setup` block; drop below it and the second `leaf` clause has nothing to read. Switching to the LC0-tuned baseline `config/cli.lc0.config.toml` is a complete alternative baseline, not a patch: it points `protocol-path` at `config/lc0.engine.toml`, dials in LC0-appropriate filter thresholds, and drops to a single engine instance \u2014 one LC0 process already saturates a strong GPU, so spawning four buys you nothing but contention. Activate it by copying it over the active config: Per-run overrides A CLI flag always beats TOML, for that one invocation only. The common ones: `--protocol-path` / `-P PATH` selects a different engine protocol. `--max-nodes` / `--nodes N` and `--max-duration D` (e.g. `5s`) change the per-position budget. `--engine-instances` / `-E N` sets the mining pool size. `--multipv N`, `--threads N`, `--hash N`, `--wdl` / `--no-wdl` adjust the engine for one run. See [Command Reference](command-reference.md) for the full flag list per command. External UCI engine protocol: `config/*.engine.toml` An engine protocol file is the translation layer between crtk and one external UCI engine. It spells out the exact strings crtk sends \u2014 how to handshake, set options, push a position, and start a search \u2014 so a new engine is a config change rather than a code change. These files back `engine analyze`, `engine bestmove` (and its `-uci`/`-san`/`-both`/`-batch` variants), `engine threats`, `engine compare`, `engine uci-smoke`, `puzzle mine`, and `fen tags --analyze`. They have nothing to do with `engine perft`, `engine perft-suite`, `engine mate`, `engine builtin`/`engine java`, `engine static`, or the Workbench Play opponent, all of which run in-process. Shipped files: `config/default.engine.toml` \u2014 Stockfish-style defaults; expects `stockfish` on `PATH` unless you change `path`. `config/lc0.engine.toml` \u2014 LC0-style defaults; expects `lc0` on `PATH` unless you change `path`. Key fields Templates carry `printf`-style placeholders \u2014 `%d` for an integer, `%s` for a string, `%b` for a boolean \u2014 and at most one per template. The `setup` array holds your standing options \u2014 whatever you want sent once, every session. `config/default.engine.toml` turns WDL on, sets `Hash` to 4000 MB, `threads` to 4, and `multipv` to 2. `config/lc0.engine.toml` chooses a backend and cache size, plus a commented `WeightsFile` line waiting to pin a specific network. Pointing crtk at your engine crtk neither ships nor builds engines \u2014 you bring your own. Install one through your package manager (`apt-get install stockfish`, say) or compile it, then point `path` in the protocol file at the executable: Confirm the handshake before you lean on it: When LC0 runs as a UCI engine it loads its own `.pb.gz` weights through `WeightsFile`. That has nothing to do with crtk's `lc0-model-path`, which only feeds the in-process evaluator \u2014 two separate networks, two separate paths, easy to conflate. [LC0](lc0.md) lays out both workflows. Neural-network models: the `models/` directory The in-process evaluators read weight files directly \u2014 no UCI process, no subprocess at all. The binaries themselves aren't committed: `models/*.bin`, `models/*.pb.gz`, and `models/*.nnue` are git-ignored, since weights are large and version separately from code. The installer fetches the defaults: A few behaviors worth knowing: `engine eval --evaluator auto` reaches for the LC0 evaluator and drops to classical evaluation when no LC0 weights are present; `--lc0`, `--otis`, and `--classical` each force one backend. `engine static` is the classical evaluator, always, and needs no model. `engine builtin --nnue` fails loudly with a missing-weights error if neither `--weights` nor the default NNUE file is there \u2014 it will not silently substitute another evaluator. Workbench Play falls back to classical evaluation when a chosen model won't load, so a game can always start. Treat these as research evaluators, not bit-exact clones of the engines they're modeled on. The LC0 path reads crtk's compact `.bin` format instead of LC0's protobuf, and the BT4 transformer is simplified and experimental \u2014 it is not LC0-bit-equivalent, so don't expect matching output. For filenames, architectures, and download links, see the in-repo `models/README.md`; the evaluators that consume them are documented in [In-House Engine](in-house-engine.md) and [LC0](lc0.md). Optional ECO book: `config/book.eco.toml` An Encyclopedia of Chess Openings dictionary: it maps move sequences to ECO codes and opening names. An entry reads: With the file present, the tagging subsystem \u2014 `fen tags`, `puzzle tags`, and mined records \u2014 annotates positions reachable through book lines with tags like `eco: A00` and `opening: Amar Opening`. Without it, opening tags drop out and nothing else changes; the book is enrichment, not a dependency. Lines starting with `#` are comments. Resolution order Every setting passes through three layers, each overriding the last: Compiled-in defaults (see `src/application/Config.java`). `config/cli.config.toml` \u2014 auto-created with those defaults if absent. The CLI flag for the command, scoped to that one in"
  },
  {
    "title": "Outputs & Logs",
    "href": "outputs-and-logs.html",
    "category": "Commands",
    "text": "Outputs and Logs Nothing crtk prints needs to be scraped. That is the whole design: a default text form a human can read, a machine-readable **JSON** or **JSONL** form one flag away, file artifacts that land in a predictable place under `dump/`, and a documented **exit code** at the end of every run that a script can branch on. Treat this page as the contract behind those promises \u2014 what the formats are, how `--format` / `--json` / `--jsonl` pick between them, what each exit code means, where the session cache and logs live, and what `clean` reclaims. Determinism is what makes it worth the trouble: the same command on the same inputs returns the same bytes, so the output is safe to diff, pipe, and commit. Output formats at a glance The vocabulary of output shapes is deliberately small, and a word means the same thing wherever it appears. Learn it once. Notation and structure are independent axes. The move-notation formats (`uci` / `san` / `both`) decide how a move is spelled; the structured formats (`json` / `jsonl`) decide whether the result is wrapped as data. The two choices do not interfere with each other. Choosing a format Three mechanisms select output. A command's `crtk help <area> <action>` tells you which it honors, and that help is the authority when this page and your memory disagree. `--format` `--format` takes a named value and is the canonical selector once a command has more than two output modes: `move list` / `move uci` / `move san` / `move both`, and `engine bestmove`: `--format uci|san|both` (default `uci`). `engine builtin`: `--format uci-info|uci|san|both|summary` (default `uci-info`). `engine mate`: `--format summary|uci|san|both|json|jsonl` (default `summary`). `engine perft`: `--format detail|table|stockfish`. `position describe`: `--format text|json|jsonl|training-jsonl` (default `text`). `fen chess960`: `--format fen|layout|both` (default `fen`). `fen render`: `--format png|jpg|bmp|svg` (this selects the image encoding, not text). `--json` and `--jsonl` When the only real decision is text versus structured, two boolean flags cover it: `--json` emits a single JSON value \u2014 one array for list-style commands, one object for single-result commands. `--jsonl` emits JSON Lines: one self-contained JSON object per line, with no enclosing array. Where a command also takes `--format`, these are simply aliases for `--format json` and `--format jsonl` (as on `engine mate` and `position describe`). Batch commands lean the other way: `engine analyze-batch` and `engine bestmove-batch` default to `--jsonl`, because a stream you can process line by line beats one array you must hold in memory; pass `--json` when you want the whole thing collapsed. Script-friendly flags Most row-producing commands take a few presentation flags that keep a pipeline's input tidy: `--fields LIST` \u2014 restrict columns (e.g. `--fields uci`, `--fields both`) on `move` commands. `--no-header` \u2014 accepted for script-friendly consistency. `--quiet` \u2014 suppress non-row chatter where supported. `--output|-o PATH` \u2014 write to a file instead of stdout (on commands that produce a file or a redirectable stream). Stable machine-readable contracts The structured formats are for parsing, not for reading over your shoulder. Four guarantees make leaning on them safe: **Determinism.** Pure core operations \u2014 move generation, FEN/SAN/UCI conversion, perft, tagging \u2014 depend only on their inputs, so the same command yields the same JSON every time. Snapshot it once as a golden fixture and every later run is a diff away from caught. **One object per JSONL line.** Each line of a `--jsonl` stream parses on its own, so a result larger than memory is still a result you can process. `--json` is the all-at-once counterpart, for when you do want the whole thing in hand. **stdout for data, stderr for diagnostics.** Rows and JSON go to stdout; progress notes, warnings, and stack traces go to stderr. Redirect stdout alone (`> out.jsonl`) and the captured data stays clean. **Consistent vocabulary.** A `uci` field is always UCI, a `san` field is always SAN, and `eval`, `nodes`, and their kin carry the same meaning across every engine command. Engine-backed commands (`engine analyze`, `engine bestmove`, `engine eval`, `puzzle mine`) lean on an external UCI engine, a neural-network evaluator, or both. The *shape* of their output is stable; the *numbers* in it are not, because they answer to the configured engine and the search budget you gave it. crtk's networks are usable evaluators, but neither the LC0 CNN nor the experimental, simplified BT4 path is a bit-exact reproduction of upstream LC0 \u2014 do not expect identical scores. For output you can reproduce, pin the engine, the weights, and `--max-nodes` / `--max-duration`. See [Engines and Evaluators](in-house-engine.md) and [LC0 Networks](lc0.md). File outputs and the `dump/` directory Commands that write an artifact instead of streaming to stdout drop it in the directory `crtk config show` reports as `Output:` \u2014 `dump/` unless you have said otherwise. Change the root in `config/cli.config.toml` (see [Configuration](configuration.md)), or override a single run with `--output|-o`. Leave `--output` off and the name comes from the input file's stem, so the artifact lands next to its source by an obvious naming rule. Derived names from the input stem `review game` rows are schema-stamped as `crtk.review.ply.v1`. The optional study JSONL sidecar is `crtk.review.study_unit.v1`, and the Record sidecar is a `crtk.record.v2` JSON array whose `parent` is the pre-move FEN and whose `position` is the post-best-move FEN. See [Review to study](review-to-study.md) for the full contract. Multi-file tensor exports A tensor export is rarely one file. Dataset exporters take a single output *prefix* and fan out several companion files around it (see [Datasets](datasets.md)): `record dataset npy` \u2014 `<prefix>.features.npy`, `<prefix>.labels.npy` (default prefix `dump/<stem>.dataset`). `record dataset lc0` \u2014 `<stem>.lc0.inputs.npy`, `<stem>.lc0.policy.npy`, `<stem>.lc0.value.npy`, plus a `.meta.json` (default output prefix `dump/<stem>.lc0`). `record dataset classifier` \u2014 `<stem>.classifier.inputs.npy`, `<stem>.classifier.labels.npy`, plus a `.meta.json` (default output prefix `dump/<stem>.classifier`). Puzzle mining outputs `puzzle mine` keeps its books in pairs: a verified-puzzle set and the rejected set beside it, so you can audit what the gates threw out (see [Puzzle Mining](mining.md)): Directory or default output: `standard-<timestamp>.puzzles.json` and `standard-<timestamp>.nonpuzzles.json` (the prefix becomes `chess960-` with `--chess960`). A file-like root ending in `.json` or `.jsonl`: `<stem>.puzzles.json` and `<stem>.nonpuzzles.json`. `--output -` streams to stdout instead of writing files. Each file is one top-level JSON array, grown incrementally as positions clear the [Filter DSL](filter-dsl.md) gates rather than written all at once at the end. Each newly written record object carries a `schemaVersion` field with the current contract id, `crtk.record.v2`. Older record objects that do not carry the field remain valid input and are interpreted as `crtk.record.v1`; use `record validate --input FILE --strict` when you want malformed rows to fail loudly instead of being skipped by tolerant readers. Publishing outputs PDFs land under `dump/` like everything else, and no LaTeX toolchain is anywhere in the path \u2014 the renderer is native (see [Book Publishing](book-publishing.md)): `book render` \u2014 `dump/<stem>.pdf`. `book pdf` \u2014 `dump/<stem>.pdf`, or `dump/chess.pdf` when there is no input path. `book cover` \u2014 `dump/<stem>-cover.pdf`. `book collection` \u2014 `dump/<stem>.book.toml` (with optional `--pdf-output` / `--cover-output`). `book study` \u2014 `dump/<stem>.pdf`. Exit codes Every invocation returns a documented status. Branch on `$?` (or whatever your language calls it) and never parse text to learn whether a command succeeded \u2014 the exit code already said so. When the cod"
  },
  {
    "title": "Workbench",
    "href": "workbench.html",
    "category": "Desktop Workbench",
    "text": "Desktop Workbench The Workbench is the same chess core as the CLI, with a board in front of it. There is no second engine, no second evaluator, no parallel implementation that drifts out of sync \u2014 a position you analyze in the GUI, a command you assemble in a form, and a dataset you inspect in a panel all resolve through the code path you would hit from a shell. Reach for the Workbench when a task wants a live board, a preview pane, or a command you'd rather click together than memorize. Reach for the [CLI](command-reference.md) when you want scripting, CI, stable text output, or a batch run that doesn't need a human watching it. What you can do Analyze positions and PGNs on an interactive board with legal-move overlays, deterministic tags, an ECO opening tree, post-game review, endgame tablebase status, and study authoring. Play full games against in-process alpha-beta/classical preset opponents, with Custom controls for MCTS and neural-evaluator experiments. Run deterministic built-in engine gauntlets from the Engine surface. Build any `crtk` command as a guided form, with validation and a live preview of the exact command text. Run batch jobs over FEN lists and command scripts, then watch progress and collect artifacts. Load, validate, and chart exported datasets and records. Preview publishing output (diagrams, books, studies, collections, covers) before rendering a PDF. Practice and inspect mined puzzles interactively. Visualize neural-network internals for NNUE, LC0 CNN, BT4, and OTIS. Launch Once ChessRTK is installed, `ChessRTK Workbench` appears in your applications menu. The same window opens from a shell, through either the `workbench` area or its `gui` alias: If the launcher script isn't on your path, point Java at the built classes: There are only three launch options, and they match what the CLI reports: Registered views The Workbench currently registers eight top-level views. Older names such as Analyze, Play, Solve, Relations, Draw, Network, MCTS, Tree, Gauntlet, Batch, and Commands are modes or workflows inside these views, not separate top-level tabs. Board view Board is the shared position and game workspace. Its mode switcher hosts Analyze, Play, Solve, Relations, and Draw. The board sits in the middle, surrounded by the mode-specific tools for the current task: move navigation, the legal-move list, deterministic tags, an ECO opening tree, deterministic post-game review with retry jumps, study TOML authoring from the current line, endgame/tablebase-hit status, a board editor for arbitrary setups, puzzle practice, tactical-incidence overlays, and freehand annotation/export controls. Load a PGN in Analyze mode and you step through the game from the same shared board surface. ![Workbench analysis board](../assets/screenshots/workbench-analysis.png) The board sees the world the way the CLI does: identical move generation, identical FEN/SAN/UCI handling, identical Chess960 support. The tags on the board are the ones `fen tags` produces, down to the bit. Live analysis drives your configured external UCI engine \u2014 Stockfish or LC0 \u2014 the same way `engine analyze` does, MultiPV included. The PGN explorer doubles as a lightweight database workspace. It indexes player/opening/result metadata, filters games reaching the current board position, removes exact duplicates, copies selected games, and can copy a prep report for the visible games or named player query. Where to find the new analysis tools The command palette also exposes the same workflows by name: Opening tree, Review game, Author study, Endgame tablebase, PGN database, Player prep report, and Open engine gauntlet. Board / Play Board / Play runs a full human-versus-engine game from the start position, the current board, or a pasted FEN. The named preset bots all use alpha-beta search with the always-available classical evaluator; their Elo changes adjust the search budget and move sampling, not the backend. Choose Custom to expose the two backend selectors: **Search** chooses the move-picking algorithm: `Alpha-Beta` or `MCTS`. **Network** chooses the leaf evaluator: `Classical`, `NNUE`, `CNN` (LC0), or `OTIS`. Both opponents run in-process. Play never spawns an external process. `Alpha-Beta` is iterative deepening with a transposition table, null-move pruning, and quiescence, scoring leaves with the chosen evaluator. `MCTS` is the in-process Monte Carlo Tree Search over the chosen policy/value backend. The CLI's `engine builtin --search mcts` path uses the same family, while the default classical/NNUE CLI path uses alpha-beta; both stay inside the JVM rather than reaching for a UCI engine. The Network selector decides which local model loads: If a neural model is missing or won't load, Play drops back to classical evaluation rather than refusing to start \u2014 you get a game, just a weaker one. The LC0 CNN and BT4 paths are real, playable evaluators, but they are simplified and not bit-exact reproductions of upstream LC0/BT4; [LC0 networks](lc0.md) covers exactly where the fidelity stops. Important: Play is in-process only. Whatever external UCI engine you've configured (Stockfish, full LC0) backs the Workbench's live analysis and the engine command forms \u2014 never the Play opponent. Run view Run is the CLI rendered as forms. Pick an area and action \u2014 `engine bestmove`, `puzzle mine`, `record dataset npy`, `fen tags` \u2014 and the form sorts the inputs into required fields, mutually exclusive choices, and optional flags, validating as you go. The exact `crtk` command takes shape in front of you while you fill it in, ready to copy into a script or run on the spot. ![Workbench command controller](../assets/screenshots/workbench-commands.png) Since every form lands on a real CLI command, this doubles as a way to learn the surface area: assemble a command by clicking, read off its flags, then lift the generated text into a script for reproducible runs. The [Command Reference](command-reference.md) documents what each form mirrors. The `Review game` command template mirrors `crtk review game --to-study`: choose a PGN, keep the shared engine budget fields explicit, and run the CLI to produce review JSONL, study JSONL, and Record sidecars. The Workbench preview is the runnable command; later review panels consume those files instead of computing separate verdicts. The local PGN store also has first-class Run templates for the non-destructive database path: `pgn import`, `pgn find`, `pgn show`, and `pgn stats`. Use the command palette entries `PGN import`, `PGN find`, `PGN show`, `PGN stats`, and `Review game command` to jump directly to those templates. The destructive store-maintenance verbs, `pgn delete` and `pgn compact`, remain available through the `All CLI` template. Batch jobs Batch work now lives in Run as command templates and multi-line inputs. Sweep a FEN list through an engine command, or run a command script \u2014 one `crtk` command per non-comment line \u2014 exactly as `batch run` would. It tracks progress, breaks out per-row results, and gathers the output, so a large mining or export pass never has to leave the GUI. Anything long-running reports into Console and persists into Logs. Datasets The Datasets tab opens up whatever the export pipeline produced \u2014 the tensors from `record dataset npy`, `record dataset lc0`, and `record dataset classifier`, and the JSONL rows from the `record export ...-jsonl` family \u2014 and reports shapes, counts, label distributions, and charts. The point is to catch a broken or skewed training set here, before a trainer spends hours on it. See [Datasets](datasets.md) for the export formats. Publishing previews The Publish tab shows you the PDF before you pay for the render. It previews single diagrams (`book pdf`), puzzle collections (`book collection`), annotated studies (`book study`), book interiors (`book render`), and covers (`book cover`). The whole publishing stack is built in \u2014 no LaTeX, no external toolchain to install. See [Book publishing](book-publishin"
  },
  {
    "title": "Design Guide",
    "href": "workbench-design-guide.html",
    "category": "Desktop Workbench",
    "text": "Workbench Design Guide The ChessRTK Workbench is a dense desktop research tool, not a landing page and not a decorative demo. Design new UI as VS Code on macOS applied to chess tooling: compact editor chrome, clear hierarchy, quiet surfaces, high information density, and a small amount of restrained frosted-glass influence only where it improves orientation. The goal is that a researcher can scan a position, command, dataset, or engine run quickly and trust that the GUI is showing the same deterministic system as the CLI. This page is the standard for Workbench UI changes. It covers visual direction, layout, controls, copy, accessibility, and verification. Core Principles **Use the shared chess core.** UI affordances may be visual, but legality, FEN, SAN, UCI, tags, engine settings, and command text still come from the same code paths the CLI uses. **Use VS Code for structure.** Major Workbench areas behave like editor groups, sidebars, panels, command palette entries, and contextual status surfaces. **Use Apple for finish.** Controls should feel deliberate, legible, consistent, and platform-familiar: clear hierarchy, logical toolbar grouping, useful search, restrained materials, and accessible states. **Design for repeated work.** Prioritize scanning, comparison, low friction, and predictable controls over explanation-heavy screens. **Keep surfaces calm.** The app should feel like an editor/workbench: flat panes, clear dividers, compact toolbars, and cards only for real grouped content. **Centralize styling.** Add palette, spacing, typography, and control styling through the shared Workbench UI layer instead of local ad hoc painting. **Make states obvious.** Hover, focus, disabled, selected, error, running, complete, and missing states must be visible in both light and dark modes. **Keep output deterministic.** UI previews, command builders, exports, and generated artifacts must not depend on wall-clock noise or hidden random choices. Uniformity Contract Every Workbench change must satisfy these rules before it is considered finished. Do not ship a panel that looks \"close enough\" but uses local colors, raw Swing control styling, arbitrary spacing, or a new layout language. Reference Model The Workbench should not copy VS Code or macOS pixel-for-pixel. It should translate their useful rules into ChessRTK's Java Swing implementation. Use this decision rule: Workbench North Star The Workbench should feel like one integrated chess research instrument. It is not a loose collection of Swing panels. It is not a chess website, an IDE clone, or a neural-network demo. It is an editor-style desktop tool where the user can move from position, to command, to run, to artifact, to explanation without losing context. At first glance the app should look like this: The most important visual rule is that the content owns the screen. Chess boards should look like boards, datasets should look like analytics, runs should look like executable command records, and publishing should look like previewing a real artifact. Chrome should help the user orient, not compete with the work. Current Registered Shell The shipped shell currently registers eight top-level views. This is the source of truth for docs, command-palette routing, and split/duplicate behavior. Target Information Architecture The ideal Workbench has seven top-level surfaces after the local game-corpus surface ships and output views can be treated as Run companions. Until then, new design work should still place features in the homes below instead of adding new top-level tabs. Surface rules: Do not add a top-level tab when the feature is a mode of an existing surface. `Analyze`, `Play`, `Solve`, `Relations`, `Draw`, and `Board Editor` are Board modes. `Network`, `MCTS`, `Tree`, and `Gauntlet` are Engine Lab modes. `Commands`, `Batch`, `Console`, and `Logs` are Run modes or Run/output companion views. `Datasets` and `Publish` stay top-level because their primary artifacts are not positions or command output. `Library` is top-level because its primary object \u2014 a queryable game corpus (the local PGN store) \u2014 is neither a single position (`Board`) nor an ML artifact (`Datasets`); selecting a game routes it into `Board`. It is introduced for the forthcoming `crtk db`/`pgn` store and may stay behind a transitional tab until that store lands. `Dashboard` is the only global state surface. It should launch work, not duplicate full workflows. Command palette entries may preserve old names like `Open Play` or `Open Logs`, but they should route to the right surface and mode. When a feature seems to need a new tab, name its primary object first. If the object is a position, game corpus, engine/search state, command run, dataset, publication, or session status, it belongs in one of the seven surfaces above. Primary Object Model Every screen must make one primary object obvious. This prevents the app from becoming a stack of unrelated forms. The `WorkspaceHeader` should always answer: what object am I looking at, what state is it in, and what is the next primary action? What Each Surface Should Look Like Use these visual targets when adapting the current implementation. Perfect Workbench screens should pass this test: if the user sees a screenshot without reading the tab name, they should immediately know whether they are working with a board, engine search, command run, dataset, publication, or session dashboard. Content-Driven Layout Rules Different content types deserve different layouts, but the visual language stays the same. Do not force every surface into a card grid. The grid is right for dashboards and analytics; a board, command builder, graph, console, and publication preview each need their own central artifact shape. VS Code Structure Rules Treat the Workbench shell like a code editor around chess/research content. New Workbench UI must name which container it belongs to. If the answer is unclear, the feature is probably not ready for UI. Apple Interaction Rules Use Apple design as the behavior and polish layer. Foundational UI Principles The lecture notes add two overlapping checks that every Workbench surface should pass: the six UI basics \u2014 contrast, consistency, typography, color, visual hierarchy, and spacing \u2014 and the C-R-A-P mnemonic: contrast, repetition, alignment, and proximity. Apply them through ChessRTK's shared primitives, not through one-off styling. Use this quick pass before declaring a UI slice done: Visual Identity The target is VS Code on macOS, with the existing ChessRTK board and research surfaces layered into that shell. Do not introduce a one-off theme, gradient-heavy panel, oversized hero section, decorative orb/blob background, or marketing-card layout inside the Workbench. Harmony Rules Harmony means the user should feel that every tab belongs to the same application, even when the task changes from playing chess to inspecting tensors. If two panels solve the same UI problem differently, prefer the existing shared primitive. If no shared primitive fits, create one and migrate both panels to it. Banned Patterns And Required Alternatives Source Of Truth Use these files before creating a new primitive. If a panel needs custom painting, it should still consume `Theme` tokens and shared helpers. Local color literals are acceptable only for domain colors that are not theme roles, such as fixed annotation brush colors, and those should be documented by the owning class. Spacing And Sizing Use the spacing scale in `Theme`. Do not invent new spacing constants in feature panels unless a custom-painted surface has a real geometry need. Common component widths: If a label is wider than the chosen label lane, do not let it push the control column. Shorten the label, use a tooltip, or use `Ui.elide(...)` in custom painting. Layout Workbench screens are task surfaces. A good surface has one clear job, a compact header, a predictable toolbar, and content that can scan without horizontal wandering. Use"
  },
  {
    "title": "Built-in Engine",
    "href": "in-house-engine.html",
    "category": "Engines & Models",
    "text": "Built-in Engine Everything here runs inside the same process that issued the command \u2014 no UCI subprocess to spawn, no socket to configure, no background engine to outlive its job. The core built-in command (`engine builtin` / `engine java`) now defaults to deterministic alpha-beta for the classical and NNUE evaluators, while LC0 CNN and OTIS still default to the PUCT-style MCTS search that can use their policy priors. Around it sit a standalone exhaustive forced-mate prover (`engine mate`), one-shot position evaluation (`engine eval` / `engine static`), an opponent-threat analyzer (`engine threats`), and a move-generator benchmark (`engine benchmark`). All of them share crtk's one chess core, return the same answer for the same inputs and budget, and stop only where you tell them to \u2014 via explicit `--depth`, `--nodes`, and `--max-duration` limits. That combination is what makes it pleasant in scripts, CI, and research you need to reproduce. It will not out-play Stockfish or LC0, and it isn't trying to. The point is determinism and zero dependencies: a search that gives the same result every time and keeps working when no UCI engine is configured. When you need real playing strength, MultiPV, or large-scale mining, drive an external engine through `engine analyze` / `engine bestmove` instead \u2014 see [LC0 & external engines](lc0.md) and [Mining](mining.md). What runs entirely in-process `engine builtin` and `engine java` are the same command \u2014 `engine java` is an alias, so pick whichever reads better in your scripts. `engine mate` also answers to `engine find-mate`. Built-in search \u2014 `engine builtin` / `engine java` Search a single position with the default classical evaluator: Read a file with one FEN per line: Or expose the searcher as a minimal UCI loop, which is enough for local protocol tests: Choosing an evaluator Pass `--evaluator classical|nnue|lc0|otis`, or use the shortcut selectors `--classical`, `--nnue`, `--lc0`, `--otis`. The default is `classical`. Search defaults to `alpha-beta` for `classical` and `nnue`, and to `mcts` for `lc0` and `otis`; override it with `--search alpha-beta` or `--search mcts`. `--weights PATH` names an explicit NNUE, LC0, or OTIS model file; omit it and each neural evaluator falls back to its configured default path. Install those defaults with `./install.sh --models` \u2014 see [Build & install](build-and-install.md). The neural backends cost far more per node than the handcrafted one, so give them a real `--nodes` or `--max-duration` budget rather than the depth-derived default. Bounding the search Three flags decide how long the search runs, and nothing else does: For alpha-beta, `--depth` is the iterative-deepening target. For MCTS, `--nodes` is the playout budget directly; set `--depth` alone \u2014 with no `--nodes` and no `--max-duration` \u2014 and the command translates depth into a small playout budget rather than searching forever. Whenever you care about the exact budget, state it: pass `--nodes` or `--max-duration` explicitly. Before spending heuristic search budget, alpha-beta checks immediate root mates directly. If the root is already forcing, either in check or with at least one checking move, it also runs the bounded mate prover up to the default mate-in-four horizon, so short tactical wins are reported exactly as mate scores instead of as ordinary centipawn preferences. At the horizon, alpha-beta quiescence also checks mate-in-one before standing pat or searching captures. That keeps one-ply mates from being flattened into static centipawn scores when they sit just beyond the requested depth. The same mate-in-one guard is used by the deterministic root fallback returned when a very small node or time budget stops before depth one completes. The first quiescence ply also includes quiet checking moves. Deeper quiescence stays capture/promotion-only unless the side to move is in check, keeping the extra tactical lookahead bounded. Output formats `--format` accepts `uci-info`, `uci`, `san`, `both`, or `summary` (default: `uci-info`). If a slow evaluator burns through its budget before depth 1 even completes, `uci-info` falls back to a static evaluation and emits it as `info depth 0 ... pv ...` \u2014 you still get a move out of it. Forced-mate prover \u2014 `engine mate` `engine mate` answers one question without guessing: does the side to move have a forced mate within the given distance? It is an AND/OR proof search over legal moves, ordered to try forcing replies first and memoizing its proof bounds. No evaluator, no network \u2014 the verdict is proven, not estimated. Because the search is exhaustive within `--max-mate`, a positive result is a forced mate, full stop. Raise `--max-mate` to reach for longer mates; raise `--nodes` if the budget cut a proof short before it could finish. One-shot evaluation \u2014 `engine eval` and `engine static` Sometimes you want a number, not a search. These commands run a single forward evaluation and print the score \u2014 no tree, no playouts, no budget flags to set. `engine static` runs the handcrafted classical evaluator: `engine eval` selects among the neural and classical backends: Both commands also accept `--input PATH` to score one FEN per line. The evaluators The MCTS searcher and the one-shot eval commands draw on the same four evaluators, and the split matters. `classical` and `nnue` return centipawns; the searcher uses them for leaf values and move priors. `lc0` and `otis` carry a policy head as well as a value, which lets their policy steer root exploration during MCTS rather than only scoring leaves. A word on fidelity, plainly: crtk's networks are usable, deterministic evaluators, not bit-exact copies of the upstream engines. The LC0 path is a simplified CNN and the BT4-style path is experimental, so do not expect LC0/BT4 parity from either. The caveats are spelled out in [LC0 & external engines](lc0.md). NNUE internals The NNUE path is kept small and legible on purpose \u2014 you can follow every step by hand: The compact CRTK-format network uses HalfKP-style sparse features: 64 king buckets x 10 relative piece planes x 64 piece squares = 40,960 possible features. For each perspective, the active sparse features accumulate into a hidden vector, clipped ReLU runs, and the side-to-move and opponent activations pass through a single linear output layer to a centipawn score. The loader recognizes both the compact CRTK format and supported Stockfish `.nnue` files; an unsupported large Stockfish net falls back to full-rebuild evaluation. `--nnue` needs its weights. If `models/crtk-halfkp.nnue` is missing it fails loudly with a missing-weights error instead of quietly substituting a smoke-test net \u2014 so a wrong score never masquerades as a real one. Install the file with `./install.sh --models`. OTIS OTIS is crtk's tactical-sheaf evaluator. It emits both a move-policy distribution and a win/draw/loss head, so the policy can steer MCTS root exploration while the value scores leaves. Reach it through `engine eval --otis`, or behind the built-in MCTS search, and point `--weights` at an OTIS `.bin`. Like every crtk network it is a usable, deterministic evaluator, not a bit-exact reproduction of anything upstream. It is also the backend the optional native GPU libraries accelerate \u2014 see [GPU Acceleration](gpu.md). ![OTIS tactical-lattice mark](../assets/logo/otis/crtk-otis-lattice.svg) Opponent threats \u2014 `engine threats` `engine threats` answers the counterfactual: what could the opponent do if the move were theirs? Handy for tactical alerts and annotation. It is the one command on this page that reaches outside the process \u2014 it drives the configured external UCI engine, so it takes the standard analysis flags. Relevant flags include `--protocol-path|-P`, `--max-nodes`, `--max-duration`, `--multipv`, `--threads`, `--hash`, and `--wdl` / `--no-wdl`. Configure the engine protocol as described in [Configuration](configuration.md) and [LC0 & external engines](lc0.md). Move-generator benchmark \u2014 `engine ben"
  },
  {
    "title": "LC0 Networks",
    "href": "lc0.html",
    "category": "Engines & Models",
    "text": "LC0 Networks in ChessRTK There are two ways to put a Leela Chess Zero network to work here, and they answer different questions. Run upstream `lc0` as an **external UCI engine** against its own `.pb.gz` weights when you want LC0 itself, exactly. Or use the **built-in Java LC0 CNN evaluator**, which loads compact ChessRTK `.bin` weights and runs policy/value inference in-process on the CPU or an optional native GPU backend. The in-process evaluator sits on the same deterministic chess core as everything else, so the position you score (`engine eval --lc0`), search (`engine builtin --lc0`), visualize in the Workbench, or export as training tensors (`record dataset lc0`) is one and the same object. The catch worth stating up front: these are honest, usable evaluators, **not** bit-exact reproductions of upstream LC0. The transformer (BT4) path is more experimental still. When fidelity is the point, run real `lc0`. What follows: the CNN evaluator and its 112-plane input, the policy and value heads, the CPU and GPU backends, LC0 as an external engine, and dataset export. See [Configuration](configuration.md) for engine protocols, [In-House Engine](in-house-engine.md) for the built-in MCTS, and [AI Agents](ai-agents.md) for the evaluator overview. Two LC0 Workflows at a Glance Model weights are not committed to the repository. Local `models/*.bin`, `models/*.pb.gz`, and `models/*.nnue` files are gitignored. Fetch the defaults with `./install.sh --models`, or download them manually as described in `models/README.md`. Java LC0 CNN Evaluator ![LC0 CNN evaluator flow](../assets/diagrams/crtk-lc0-cnn.png) The CNN evaluator lives under `src/chess/nn/lc0/cnn/`: a forward pass over a Leela-style convolutional residual network with squeeze-and-excitation (SE) blocks, a policy head, and a WDL value head, written in plain Java with no native dependency. Sharing the one chess core buys something useful here \u2014 the `Position` you analyze, search, or render becomes the exact tensor the network sees, so the same input always produces the same output. The default ChessRTK LC0 CNN weights file is: The name spells out the shape: 112-plane input, 10 residual blocks of 128 trunk channels, an 80-channel policy head emitting 4,672 raw move logits, and a 32-channel value head emitting 3 WDL outputs. A heavier `30blocksx384` network ships alongside it. `chess.nn.lc0.cnn.Model.DEFAULT_WEIGHTS` defines the default path. Evaluate a Position `engine eval` runs one forward pass and prints the value and a policy summary. Choose the LC0 backend with `--lc0` and point `--weights` at a ChessRTK `.bin` file: The evaluator modes are `auto`, `lc0`, `otis`, and `classical` (default `auto`), and `--lc0`, `--otis`, and `--classical` are shortcuts for them. `--terminal-aware` folds mate and stalemate detection into the score, which matters when the network's opinion of a dead position is beside the point. For a classical-only baseline that needs no weights at all, reach for `engine static`. Drive the Built-in MCTS Search The same network can serve as the policy/value oracle inside the built-in MCTS engine. `engine builtin` accepts `--evaluator classical|nnue|lc0|otis` (default `classical`), with `--lc0` as a shortcut: `engine builtin` exposes `--max-nodes` (the MCTS playout budget; `0` removes the cap), `--max-duration` (e.g. `5s`), and `--format uci-info|uci|san|both|summary`. See [In-House Engine](in-house-engine.md) for how the search uses them. This evaluator also backs the Workbench Play tab's `CNN` opponent, and if the chosen model fails to load, Play quietly drops to classical evaluation rather than refusing to play. Inspect the Evaluator on the Board To see what the evaluator is reacting to, `fen display` can overlay its per-square ablation heatmap and name the backend that produced it: `fen render` takes the same `--ablation` and `--show-backend` flags but writes the overlay to an image file instead of a window. Input Planes (112 x 64) The encoder (`chess.nn.lc0.cnn.Encoder`) flattens a `Position` into a `float[112 * 64]` tensor in **channel-major** order: the first 64 values are plane 0, the next 64 plane 1, and so on. Within a plane, squares run `a1..h1, a2..h2, ..., a8..h8`. Every plane is written from the **side-to-move perspective**, so when Black is to move the board is rank-mirrored and colors are swapped \u2014 \"our\" pieces always sit nearest rank 1. That is LC0's convention, and copying it is what lets LC0-trained weights mean anything here. The 112 planes break down this way: **Fidelity note.** `Position` carries no move history and no repetition state, so all eight history blocks hold copies of the current board and the repetition planes stay zero. That is the deliberate compromise: the layout is a faithful LC0 *classical 112-plane* encoding of a single position, but the temporal and repetition signal a real self-play input would carry is simply absent. The network can't see what it was never given, which is why evaluations come out reasonable rather than bit-identical to upstream LC0. Policy and Value Heads The trunk splits into two heads, following LC0's structure. **Policy head.** A policy stem and an output convolution produce raw move planes \u2014 4,672 logits for the default network. The weights file carries a `policyMap` that gathers those raw planes into the compressed LC0 move-logit vector, so downstream code sees standard LC0-ordered move probabilities and never has to know about the raw plane layout. Out-of-range map indices count as zero. **Value head.** A value convolution feeds two dense layers and a softmax, yielding three **WDL** probabilities ordered `[win, draw, loss]` from the side-to-move perspective. The scalar the evaluator reports is `win - loss`, so positive favors the side to move. **SE blocks.** A residual block may carry a squeeze-and-excitation unit: global-average-pool each channel, run it through a small two-layer gate, then use the result to re-scale and shift the trunk activations before the residual add and ReLU. Whether a given block has one is read from the weights file, block by block. Along the way the forward pass captures named intermediate activations \u2014 stem, per-block ReLU, policy hidden/planes/logits, value conv/fc1/logits, WDL, scalar \u2014 which the Workbench neural-net visualizers draw. Backends: CPU and GPU Out of the box the evaluator runs on a deterministic CPU backend with no native code behind it. Optional JNI backends offload inference to a GPU when you have one. JVM system properties decide which is used: `auto` tries an available native backend and falls back to the CPU evaluator if none loads \u2014 the GPU is a bonus, never a requirement. The shared libraries are `liblc0_cuda.so`, `liblc0_rocm.so`, and `liblc0_oneapi.so`. Check what's actually available: `engine gpu` reports OTIS and native perft backend status too; the LC0 rows are the ones the `crtk.lc0.*` properties control. Building a Native Backend Each backend is its own CMake project. The CUDA one, for instance: Then run with the library on `java.library.path` and the backend pinned: See `native/cuda/README.md`, `native/rocm/README.md`, and `native/oneapi/README.md` for backend-specific build notes, and [Build and Install](build-and-install.md) for the overall build flow. LC0 as an External UCI Engine When you need LC0 to behave exactly as LC0, point an engine protocol at the real `lc0` binary and let crtk drive it over UCI. This is the path to take for the official BT4 transformer and for any analysis where the answer has to be trustworthy rather than merely fast. The installer's default model set includes the upstream network: That is an LC0 protobuf-gzip file, not a ChessRTK `.bin` \u2014 `lc0` loads it through its own `WeightsFile` option, set in your engine protocol TOML: Once the protocol resolves to `lc0`, the ordinary engine commands apply (`--protocol-path` selects a specific protocol): `engine compare` pits an LC0 protocol against another UCI engine via `--left-protocol` / `--r"
  },
  {
    "title": "GPU Acceleration",
    "href": "gpu.html",
    "category": "Engines & Models",
    "text": "GPU Acceleration Everything in crtk runs on the CPU, and most of it stays there. GPU acceleration is an **optional** add-on aimed at the two workloads that actually saturate a device: **perft** move-enumeration and **OTIS / LC0 neural-network inference**. It ships as native JNI shared libraries \u2014 CUDA for NVIDIA, ROCm for AMD, oneAPI for Intel \u2014 loaded at runtime if they happen to be on the path. When a backend is missing or refuses to initialize, crtk **falls back to the CPU** without comment and returns the same answer. You opt in deliberately: `--gpu` for perft, a backend property for the evaluators. The device only ever sees the heavy batches. What follows: reading backend status with `engine gpu`, running `engine perft --gpu` and `engine perft-suite --gpu`, building the native libraries under `native/cuda`, `native/rocm`, and `native/oneapi`, and how the runtime finds and links them. All of this is optional. If you only need the CPU toolkit, skip the page \u2014 nothing here is load-bearing for the rest of crtk. What GPU acceleration does (and does not) do The backends move arithmetic, not logic. Results are identical either way: perft node counts match to the digit, and the evaluators run the same model weights regardless of where the matrix multiplies land. **Accelerated on the GPU when a backend is present:** **Perft enumeration** \u2014 `engine perft --gpu` and `engine perft-suite --gpu` hand packed subtree batches to the device through the `perft_<vendor>` library. **OTIS inference** \u2014 `.bin` policy/WDL evaluation on the GPU, auto-selected by the OTIS model. **LC0 CNN inference** \u2014 `.bin` policy + value inference on the GPU. **LC0 BT4 inference** \u2014 compact BT4 `.bin` (magic `BT4J`) policy + value inference on the GPU. The BT4 path is simplified and experimental; treat it as a usable evaluator, not a bit-exact LC0 reimplementation. **T5 text generation** \u2014 the `fen text` / `puzzle text` summary pipeline can offload matrix multiplies (CUDA/ROCm run greedy decoding end-to-end on the device; oneAPI offloads GEMMs). **Always on the CPU (no GPU path):** legal move generation, make/undo, FEN/SAN/UCI parsing, the built-in alpha-beta/MCTS search (`engine builtin` / `engine java`), the forced-mate prover (`engine mate`), and the classical/NNUE evaluators. External UCI engines such as Stockfish or LC0 manage their own hardware independently of crtk's native backends. This is what makes the GPU safe to leave on in a test harness. A GPU run of `engine perft` returns the same totals as the CPU run, and `engine perft-suite` checks against the same expected counts whichever path it took. See [Quality and Testing](quality-and-testing.md) for how the perft suite serves as a regression gate. ![ChessRTK position toolbox: one shared chess core feeding evaluation and analysis](../assets/diagrams/crtk-position-toolbox.png) Check backend status: `engine gpu` Run this before anything else. It prints one line per backend across all three vendors, so a single glance tells you what is loaded and what can actually reach a device on this machine. Each line reports three fields: `loaded=yes|no` \u2014 whether the native library was found and linked into the JVM. `available=yes|no` \u2014 whether the runtime could initialize a usable device. `deviceCount=N` \u2014 number of devices the backend sees. Example output on a machine with one NVIDIA GPU and CUDA libraries on the path: `loaded=no` means the library never made it onto `java.library.path` (or the vendor-specific path env var was unset) \u2014 a placement problem, not a hardware one. `loaded=yes, available=no` is the more interesting failure: the library linked, but it found no usable device. That points at drivers or `deviceCount`, not the build. Add `--verbose` for more detail. GPU perft: `engine perft --gpu` and `engine perft-suite --gpu` Perft counts the leaf nodes in the legal move tree to a fixed depth \u2014 the standard way to check that a move generator is both correct and fast. The GPU path changes only how the work is divided; the count is the same. The division is a tradeoff you control. crtk expands the tree on the CPU down to a **split depth**, packs the resulting legal positions, and ships them to the device, which counts the remaining subtrees in parallel. `--split N` sets that CPU depth. Each subtree the device receives is one large, independent unit of work \u2014 good for the GPU, until a subtree is deep enough to exhaust device memory or stall. When that happens, raise `--split`: more of the tree expands on the CPU, the per-device subtrees shrink, and the batch fits. Run perft from the start position to depth 6, expanding three plies on the CPU before the GPU handles the rest: Run perft on a custom position with a per-root-move breakdown: With no native perft backend loaded, `--gpu` does nothing visible: the same perft runs on the CPU and returns the same totals. That interchangeability is the whole point \u2014 it lets the regression suite treat GPU and CPU runs as equivalent. The suite accepts the same two GPU flags, so you can validate a whole battery of known positions on the device: See [Quality and Testing](quality-and-testing.md) for the suite file format and how perft is used as a determinism gate, and [Command Reference](command-reference.md) for the full option list. GPU evaluators: OTIS and LC0 The neural-network backends are chosen by system property rather than CLI flag \u2014 the commands stay the same whether the work runs on a GPU or not. The default is `auto`: take a GPU if one is there, otherwise the CPU. So `engine eval --otis` and `engine eval --lc0` are unchanged, as are the OTIS/LC0 paths inside `puzzle mine`, `fen tags`, and `fen text` / `puzzle text`. You change where the math happens without touching how you ask for it. Force OTIS onto a CUDA device for an evaluation: Pin LC0 CNN inference to the CPU even when a GPU is present: One expectation to set straight: crtk's neural networks are usable evaluators, not bit-exact reimplementations of upstream LC0/BT4. The GPU and CPU paths agree because they run the same crtk weights \u2014 but those weights will not track a stock LC0 binary move-for-move, and the BT4 backend is simplified and experimental on top of that. See [LC0 CNN evaluator](lc0.md) and [T5 summaries](t5.md) for what these models actually deliver. Building the native libraries Each vendor backend lives in its own directory and builds with CMake. There are **no third-party Java dependencies** \u2014 JNI plus the vendor runtime, nothing else. Build the one that matches your hardware: All three want CMake 3.18+, a C++17 toolchain, and a full JDK 17+ \u2014 a JRE will not do, because the build needs `jni.h`. Linux is the most-tested target. Each directory keeps its own `README.md` with the full build, run, and troubleshooting matrix. CUDA (NVIDIA) ROCm (AMD) oneAPI (Intel) If your default compiler does not understand `-fsycl`, point CMake at one that does: If CMake cannot find JNI, set `JAVA_HOME` to your JDK root and re-run the configure step. On Windows, reach for a multi-config generator (for example `-G \"Visual Studio 17 2022\" -A x64`) and `--config Release`; outputs land under `build/Release/` with `.dll` extensions. How Java loads the native libraries Each backend loads through `System.loadLibrary`, which means the built libraries have to sit somewhere on the JVM's library path. The usual move is to point `java.library.path` straight at the vendor build directory: Discovery order, from most to least common: Add the vendor `build/` directory to `-Djava.library.path`. Copy the built library next to where you launch crtk (there is a small fallback that probes the current directory). Set the per-backend, per-vendor path env var for an explicit absolute path, for example `CRTK_OTIS_CUDA_LIB`, `CRTK_PERFT_ROCM_LIB`, or `CRTK_T5_ONEAPI_LIB`. Backend selection at runtime: LC0 CNN / BT4, OTIS, and T5 each read their `-Dcrtk.<area>.backend` property (`auto` by default) and auto-select a loaded GPU backend when `auto`. Perft uses `-"
  },
  {
    "title": "T5 Text",
    "href": "t5.html",
    "category": "Engines & Models",
    "text": "T5 Tag-to-Text Summaries Tags tell you what is on the board; they don't read like a human talking about it. The T5 path closes that last gap. Give it a position or a whole puzzle line, and crtk pulls tags \u2014 material balance, king safety, the tactical motif that matters \u2014 from the [one shared chess core](architecture.md), wraps them in a fixed prompt, and runs a sequence-to-sequence T5 model end to end to return two to four sentences of commentary. The whole runtime is self-contained, dependency-free, and runs in-process. Decoding runs on the CPU, with optional CUDA / ROCm / oneAPI backends that fall back to CPU automatically. It works, but it is a research feature \u2014 read [Status and quality](#status-and-quality) before you trust the prose. ![T5 tag-to-text pipeline](../assets/diagrams/crtk-t5-pipeline.png) What it does Converts deterministic position and puzzle-line tags into natural-language commentary. Runs two CLI commands: `fen text` for single positions or FEN files, and `puzzle text` for expanded puzzle PVs. Loads a custom `.bin` T5 export (weights, config, and a SentencePiece vocabulary in one file). Tokenizes with a built-in SentencePiece unigram tokenizer \u2014 no Python, no native ML runtime required. Generates greedily on the CPU, or on an optional GPU backend with transparent CPU fallback. The tags driving the model come from the same code behind `fen tags` and `puzzle tags`. The prompt tells the model to stay inside those facts, but generated prose is still experimental and can drift. The working T5 runtime is exposed through `fen text` and `puzzle text`. For strict, non-hallucinated position prose, `position describe` is classical-only and uses the deterministic path plus the `PositionDescriptionVerifier` guard described below. See [Tag reference](tag-reference.md) for the vocabulary. Pipeline at a glance Quick start Summarize one position: Emit machine-readable output (one JSON object with `fen` and `summary`): Summarize a batch from a FEN file: The model path defaults to `t5-model-path` in `config/cli.config.toml` (shipped value: `models/t5.bin`). The repository ships no weights, so `--model` has to point at a `.bin` export you bring yourself. See [Status and quality](#status-and-quality). Position summaries: `fen text` `fen text` tags a single position \u2014 or every FEN in a file \u2014 and feeds the result through the summarizer. Reach for it when you want commentary on one position and nothing more. The engine flags do nothing on their own; they wake up only under `--analyze`, which runs a UCI engine to fold eval and PV motifs into the tag set before the model sees it. Leave `--analyze` off and the tags come straight from the static position \u2014 no engine, fully deterministic. Puzzle-line summaries: `puzzle text` `puzzle text` takes a root puzzle FEN, walks out its principal variations, tags each step along the way, and summarizes the line as a whole. Here analysis is on by default \u2014 a puzzle without engine context is rarely worth narrating \u2014 so pass `--no-analyze` if you want to skip it. Its companion `puzzle tags` produces the same expanded positions as JSONL tag deltas and stops short of the text step \u2014 the cleanest way to see exactly what the summarizer is handed: The model file Everything inference needs lives in one file. The runtime loads a custom binary export (extension `.bin`) that bundles: A header magic `LTTT` and a format version (currently `1`). Model configuration: vocabulary size, model width, head dimension, FFN size, encoder/decoder layer counts, head count, relative-position bucket settings, and the special token ids (pad, EOS, decoder-start, unknown). Flags for gated-GELU feed-forward and layer-norm epsilon. The SentencePiece vocabulary: piece strings plus their unigram scores. All weight tensors, keyed by export name (for example `shared.weight` for the shared embedding table), stored as little-endian float32. This is crtk's own packaging format \u2014 not a Hugging Face checkpoint, not a `.safetensors` file. To use the feature you supply a `.bin` produced from a compatible T5-style encoder-decoder export. The repository bundles none. Tokenizer Encoding goes through a self-contained SentencePiece unigram tokenizer: Input is whitespace-normalized, with spaces swapped for the SentencePiece `\u2581` marker. A Viterbi search over the vocabulary picks the highest-scoring segmentation. A span with no matching piece collapses to the unknown token, so encoding never fails \u2014 it degrades. Decoding stitches the pieces back together and turns `\u2581` markers back into spaces. The tokenizer rides inside the `.bin`, so encode and decode share one vocabulary with no external tokenizer files to keep in sync. Prompt building Before tokenizing, crtk wraps the tag list in a fixed instruction prompt. The instructions are pointed: write a single paragraph of chess commentary, ground it in material, king safety, and the top tactical motif, mention only moves that appear in the tags, and don't talk about tags or input formats. Then the tags follow as bullet lines, closed by an `OUTPUT:` marker. With `fen text` and `puzzle text` you never touch this prompt \u2014 crtk derives the tags and assembles it. The shape matters when you train or fine-tune a model: the model has to have learned this tag-prompt format, or its output will drift. Grounding guard `position describe` has a verifier for any future model-backed prose, but it does not expose a T5 runtime today. `PositionDescriptionVerifier` checks generated text against `PositionDescriptionInput`: every SAN-like move mention must match an input candidate, side-to-move claims must match the FEN, directional eval claims must agree with the evaluation sign, and concrete material-surplus claims such as \"a rook up\", \"an extra knight\", or \"a piece up\" must match the extracted material. When a candidate text fails, callers can use `groundedOrFallback(...)` to return the classical description instead. This guard is intentionally conservative. It does not certify that model prose is insightful; it blocks concrete false claims before model text can replace the faithful classical description. The repository still ships no T5 position-description weights, and `position describe --engine t5` is not a supported runtime. Use `fen text` or `puzzle text` for the one working T5 path. Backends Generation defaults to the CPU. When their JNI libraries are present, the native backends take over decoding; when one fails to load or stumbles at runtime, crtk drops back to the CPU path without complaint. A missing GPU library should never sink a run, so it doesn't. Pick a backend with a JVM system property: These backends load the same way as crtk's other GPU paths (CUDA / ROCm / oneAPI), built from the `native/` sources. [Native GPU backends](gpu.md) covers the build and detection details; `crtk doctor` or `crtk engine gpu` will tell you what your machine actually offers. Low-level runner The CLI is the supported entry point. Underneath it sits a thin standalone main that takes raw tags on stdin \u2014 the shortest path to the model when you want to debug it directly: It reads tags either as newline-separated lines or as a JSON array of strings \u2014 exactly what `fen tags` emits, so the two pipe together without translation. Status and quality The T5 path is **experimental** \u2014 built for research and tinkering, not as a finished commentary engine: No weights ship with crtk. `t5-model-path` points at `models/t5.bin`, but that file is yours to provide from a compatible export. Output is only as good as the model behind it. Load a weak or mismatched export and you'll get generic, repetitive, or off-topic text \u2014 the runtime is faithful to whatever it's given. The runtime and `.bin` format are crtk's own simplified take, not a bit-exact reimplementation of any published checkpoint. Read the prose as a best-effort summary, never as ground truth. When you need descriptions that are grounded and can't hallucinate, reach for `position describe` or the raw `"
  },
  {
    "title": "Puzzle Mining",
    "href": "mining.html",
    "category": "Workflows",
    "text": "Puzzle Mining Mining is how a heap of positions becomes a labeled corpus of tactical and defensive puzzles. The `puzzle mine` command feeds seed positions to a pool of UCI engines, runs each candidate through a set of Filter DSL **gates**, and writes two parallel record files: the accepted puzzles and the rejected non-puzzles. Keeping both sides is deliberate. One run yields ready-to-use material for study books *and* the balanced positive/negative split a classifier wants. Nothing about the process is magic \u2014 you choose the seeds, the per-position node and time budgets, and the exact gate definitions, then read the result back with `record stats` and `record tag-stats`. Same inputs, same corpus. What follows traces the workflow end to end: seeds, the `puzzle mine` run, the gates, the output files, conversion to PGN, and summarizing. The gate language has its own home in [Filter DSL](filter-dsl.md). For turning accepted puzzles into tensors or books, see [Datasets](datasets.md) and [Book Publishing](book-publishing.md). ![Mate in one with the solution arrow, rendered by crtk](../assets/boards/crtk-board-mate.svg) *A mined tactical position, drawn by `crtk fen render --arrow a1a8`.* The pipeline at a glance **Seed** \u2014 produce candidate positions, either from PGN games (via `fen pgn`) or by random legal generation. **Search** \u2014 `puzzle mine` runs each seed through a pool of UCI engines, bounded by `--max-nodes` and `--max-duration`. **Gate** \u2014 the accelerate, quality, winning, and drawing Filter DSL programs decide what counts as a puzzle. **Write** \u2014 accepted rows go to `*.puzzles.json`, rejected rows to `*.nonpuzzles.json`. **Summarize / convert** \u2014 `record stats`, `record tag-stats`, `puzzle pgn`, and `record export` turn the dumps into reports, games, and datasets. ![ChessRTK puzzle mining gates](../assets/diagrams/crtk-mining-gates.png) Step 1 \u2014 Seeds Every mined puzzle begins life as a seed position, and there are two ways to get them. PGN games (via `fen pgn`) Real games are the richest vein: actual losing moves and actual defensive resources are sitting there in the movetext, already vetted by someone who had to play them. Point `puzzle mine --input games.pgn` straight at a PGN file, or pre-extract a FEN seed list with `fen pgn` when you want to inspect or reuse it. The seed file is a plain FEN list: One FEN per line becomes that row's `position`. A `--pairs` file stores parent/child FENs so the move that *created* the candidate is preserved. `--mainline` keeps only mainline positions; omit it to include variation positions too. Blank lines and lines beginning with `#` or `//` are ignored, so annotate your seed files however you like. Random legal positions Drop `--input` and `puzzle mine` invents its own positions. Use `--random-count <n>` for a fixed batch or `--random-infinite` to keep generating across a long, continuous run. Three caps keep the random expansion from running away: `--max-waves N` \u2014 cap the number of expansion waves. `--max-frontier N` \u2014 cap the frontier size per wave. `--max-total N` \u2014 cap the total number of processed positions. Add `--chess960` (or `-9`) to generate Chess960 starts and switch on Chess960 mode in the UCI protocol where the protocol template supports it. Step 2 \u2014 Running `puzzle mine` Everything else is preamble or cleanup; this is the run itself. `puzzle mine` reads seeds, fans searches out across an engine pool, applies the gates, and streams accepted and rejected rows to disk as it goes. Core options The engine and budget defaults come from your configuration; the flags above override them for one run. The two knobs that matter most pull in opposite directions: more `--engine-instances` buys throughput but costs CPU and memory, while a deeper `--max-nodes` buys puzzle quality at the price of speed. Smoke-check before a long job A mining run can occupy a machine for hours, so spend thirty seconds proving the toolchain works before you commit to it: `doctor` inspects Java, config, the protocol, and local artifacts; `engine uci-smoke` confirms the configured engine actually launches and returns a move, which is the one failure mode that wastes the most of your time when it surfaces an hour in. See [Getting Started](getting-started.md) for first-run setup. Step 3 \u2014 The gates A search result is not yet a puzzle. It becomes one only by surviving a sequence of Filter DSL programs \u2014 four of them, each overridable per run. The acceptance rule combines them: Clear `quality` and match either `winning` or `drawing`, and the row is written as a puzzle; anything else lands in the non-puzzle file. Override any gate inline to redefine what \"puzzle\" means for a given run: The full gate grammar \u2014 fields, operators, and worked examples \u2014 lives in [Filter DSL](filter-dsl.md). Step 4 \u2014 Outputs `--output` takes either a directory or a file-like root, and either way a single run leaves behind two parallel files. **Directory output** writes timestamped, named pairs: `standard-<timestamp>.puzzles.json` `standard-<timestamp>.nonpuzzles.json` Chess960 runs use `chess960-<timestamp>...` stems. **File-like output** reuses the stem you give: `--output dump/run.json` writes `dump/run.puzzles.json` and `dump/run.nonpuzzles.json`. `.jsonl` roots follow the same stem behavior. Each file is a JSON array that stays valid while rows are appended, so you can inspect a long run mid-flight and the downstream `record` commands can read it without any bespoke parsing. Step 5 \u2014 Summarize and convert A finished run is two files of records. The `record` and `puzzle` commands are how you read them, reshape them, and turn them into something else. Summarize with `record stats` and `record tag-stats` `record stats` summarizes a record file: counts, engines, and top tags. `record tag-stats` focuses on the tag distribution; `--top N` controls how many tags are listed. The tags come from deterministic position and tactic tagging; [Tag Reference](tag-reference.md) lists the vocabulary. Merge, split, and compare `record files` merges, filters, or splits record files; `--puzzles` / `--nonpuzzles` select one side, `--recursive` walks directories, `--max-records N` splits output. `record analysis-delta` compares parent/child analysis changes \u2014 useful when seeds carry parent FENs from `fen pgn --pairs`. Convert accepted puzzles to PGN `puzzle pgn` renders a puzzle dump as PGN games. Open them in any chess GUI and judge the quality with your eyes, which is still the best filter for a few minutes of spot-checking. Export for training and study Handing both the puzzle and non-puzzle files to `record dataset classifier` is the whole reason mining bothers to keep the rejected side: positives and negatives, balanced, in one step. See [Datasets](datasets.md) for the full tensor export surface, and [Book Publishing](book-publishing.md) for turning puzzle records into native PDFs with `book collection` and `book study`. Reproducibility checklist Given the same seeds, engine, protocol, budgets, and gate definitions, mining is deterministic \u2014 which is worth nothing unless you actually capture all five. To keep a run reproducible: Run `crtk doctor --strict` on the target machine and treat warnings as failures. Run `crtk engine uci-smoke` with the protocol, thread, and hash settings you intend to mine with. Mine a small batch first, check `record stats`, and open a few PGNs from `puzzle pgn`. Keep the config file, the protocol TOML, and the exact command line alongside the produced dumps. Save the seed file, the gate flags, and the engine protocol together. They are the recipe \u2014 anyone with the same inputs can regenerate the same puzzle corpus."
  },
  {
    "title": "Filter DSL",
    "href": "filter-dsl.html",
    "category": "Workflows",
    "text": "Filter DSL Engine analysis is a firehose of numbers \u2014 depth, nodes, evaluations, win/draw/loss estimates across several lines. The Filter DSL is how you ask a sharp question of that data and get back a single `true` or `false`. A filter is a tree of boolean checks over a position's principal variations (PVs), joined by a logical gate. The same machinery decides which analyzed positions become puzzles, picks rows out of `.record` files when you export or split them, and labels rows for ML datasets. It lives in `chess.uci.Filter`, round-trips through parse and serialize without loss, and is deterministic to the bit: identical filter, identical analysis, identical verdict. What follows is the reference \u2014 block structure, keys, predicates, operators \u2014 with worked examples you can drop straight into a flag or a config value. Where filters are used A filter is always a plain DSL string. It reaches a command from one of two places: **Defaults in `config/cli.config.toml`** \u2014 the four mining gates ship as TOML multi-line strings: `puzzle-quality`, `puzzle-winning`, `puzzle-drawing`, and `puzzle-accelerate`. Tune these to match your engine. **Per-command flags** \u2014 override or supply a filter at the command line. See [Mining](mining.md) for the mining pipeline, [Datasets](datasets.md) for export and dataset commands, and the [Configuration](configuration.md) page for the TOML keys. Mental model A filter is a **tree of blocks**. Each block does three things: Optionally picks a MultiPV line to inspect (`break=<n>`, where `1` = the best line / PV1, `2` = PV2, and so on). Evaluates its own predicates (e.g. `eval>=300`) against that line, and recursively evaluates any nested child blocks (`leaf[ ... ]`). Reduces every resulting boolean \u2014 predicates plus children \u2014 into one value using its gate (`gate=AND`, `gate=OR`, \u2026). The whole filter's verdict is the outermost block's verdict. A block reads the deepest available output at its chosen `break` index. A predicate that references an attribute the engine never reported fails rather than erroring \u2014 so a filter is exactly as strict as the data behind it, no stricter. Syntax Tokens within a block are separated by `;` (semicolons). Commas and spaces also work as separators, and whitespace is otherwise ignored. A nested block is written `leaf[ ... ]` \u2014 its contents are themselves a full filter. A predicate is `key` + operator + `value`, e.g. `nodes>=50000000` or `eval<=0`. Comparison operators are `>`, `>=`, `=`, `<=`, `<`. A minimal one-block filter: Read it left to right: on PV1, require depth of at least 20 **and** an evaluation of at least +3.00 pawns. Block keys These keys configure the block rather than test the position: Notes on `break=`: `break=1` reads PV1 (the engine's best line), `break=2` reads PV2, etc. If a block has predicates but no `break=` (or `break=0`), it defaults to PV1. A block with only nested `leaf[...]` children and no predicates of its own does not read any PV, so you can omit `break=` on those purely structural blocks. Notes on `null=` and `empty=`: `null=true` makes a block pass when its PV is absent \u2014 handy when a missing second line should read as \"no counter-line exists.\" `null=false` makes the same block fail. Set this deliberately on anything reading `break=2` or higher, because that line genuinely may not be there. `empty=` only bites on a block with no predicates and no leaves: it is the constant that empty block returns. Gates A gate folds the booleans a block produced \u2014 its predicate results, then its child results \u2014 into one value. `AND` and `OR` carry almost every filter you will ever write. The rest are here for completeness and to let any serialized filter round-trip exactly. Predicates A predicate tests one attribute of the selected PV with `>`, `>=`, `=`, `<=`, or `<`. `eval` values The right-hand side of an `eval` predicate takes three forms: **Centipawns** as a bare integer: `eval>=300` means +3.00 pawns or better. **Pawns** with a decimal point: `eval>=3.0` is the same as `eval>=300`; `eval<-0.5` is `eval<-50`. **Mate** with a `#`: `eval>=#3` (mate in 3 for the side to move), `eval<=#-2` (getting mated in 2). A leading `+` is optional (`#+3` == `#3`). Every mate score outranks every centipawn score, so `eval>=#5` matches any forced mate for the side to move regardless of distance. Comparisons also honor the engine's bound hints: a lower-bound score can only satisfy `>`/`>=`, an upper-bound score only `<`/`<=`, and `=` demands an exact, unbounded score. `chances` values `chances` tests the reported win/draw/loss triple. Its canonical form is three integers summing to 1000 (per-mille): `chances>=790/200/10`, `chances=1000/0/0`. Percentages work too \u2014 `win 79% draw 20% loss 1%`, or just `79 20 1` when the three sum to 100 \u2014 and are normalized onto the `/1000` basis. Worked examples Quality gate: demand real effort on the top two lines Don't trust a verdict the engine hasn't worked for: accept a position only after at least 50M nodes on both PV1 and PV2. This is the default `puzzle-quality` filter. Winning puzzle: one good move, no good alternative A good puzzle has exactly one answer. Here PV1 wins outright (+3.00 or better) while PV2 is at best equal \u2014 the signature of a forcing tactic where the second-best move goes nowhere. This is the default `puzzle-winning` filter. Drawing puzzle: only one move holds The mirror image of the winning gate. PV1 holds the position equal or better; every alternative collapses by 3 pawns \u2014 find the only defense. This is the default `puzzle-drawing` filter. Accelerate: reject obvious non-puzzles cheaply Deep search is the expensive part, so spend it only on candidates worth the cost. After a shallow two-PV pass, this gate discards anything that can't become a puzzle. It is the default `puzzle-accelerate` filter. The two nested `OR` blocks each say the same kind of thing \u2014 \"PV1 is unconvincing **or** PV2 is already strong\" \u2014 and either condition means the position has no unique best move. Mate-aware quality, on one line Not every filter needs a tree. A single block mixing predicate types does fine when you only want one deeply searched, decisive line \u2014 common when selecting records: Override a mining gate at the command line Filter records on export Reading and writing filters programmatically `Filter.FilterDSL.fromString(...)` parses a DSL string into an immutable rule tree; `Filter.toString()` serializes it back. The serializer drops `break=` when it is unbound (0) and writes each predicate in canonical `key`+operator+`value` form, so parsing and re-serializing reaches a fixed point \u2014 the second pass produces the same string as the first. A persisted filter \u2014 the one recorded alongside a mining run, say \u2014 often reads fuller than what you typed: explicit `null=`/`empty=`, a `break=` on every line. That is each block spelling out its effective configuration, not a different filter. The expanded form and your compact one mean exactly the same thing. Tips **Set `null=` on every counter-line block.** A `break=2`-or-higher block can land on a position with fewer PVs than you assumed; leaving the default in place silently fails it. Decide on purpose whether a missing line should pass (`null=true`) or fail (`null=false`). **Calibrate thresholds to your engine.** Centipawn scale, contempt, pruning, and network behavior all differ across Stockfish, LC0, and the built-in evaluators, so `+300` does not mean the same thing everywhere. The shipped defaults lean conservative. **Write pawns or `#`, not raw centipawns.** `eval>=3.0` and `eval>=#3` parse identically to their integer forms and are far easier to read back six months later. **Order accelerate gates cheap-first.** Put node and time checks ahead of evaluation checks so a fast reject short-circuits before the costly comparison runs. Related pages [Mining](mining.md) \u2014 the mining pipeline these gates drive [Datasets](datasets.md) \u2014 `record files`, `record export`, and `record dataset` filtering [Confi"
  },
  {
    "title": "ML Datasets",
    "href": "datasets.html",
    "category": "Workflows",
    "text": "Datasets The hard part of building a training set is rarely the math \u2014 it's trusting that the planes you feed the model are the ones you think you're feeding it. `record dataset` and `record export` close that gap. They read `.record` JSON (or the `*.puzzles.json` / `*.nonpuzzles.json` from [puzzle mining](mining.md)) and write NumPy `.npy` tensors, JSONL label streams, and JSON metadata, with no Python and no external toolchain in the way. The writers run on the same chess core as the CLI and Workbench, so a position's planes, labels, and tags are identical to what those tools display \u2014 there is no second encoding to drift out of sync. What follows: what each export emits, the layouts that are fixed, the inputs each command expects, and two pipelines run end to end. What You Can Export Dataset work splits along one question: do you want tensors or text? `record dataset KIND` writes binary NumPy tensors plus a metadata sidecar \u2014 reach for it when your training code wants feature arrays it can load and go. `record export FORMAT` writes rows you can read (JSONL, CSV, PGN, plain) \u2014 reach for it when you want to inspect, post-process, or hand structured text to your own featurizer. The shapes below describe crtk's own encoding. The LC0-style export borrows LC0's input shape for compatibility checks and experimentation; it is **not** a bit-exact reproduction of any official LC0 training pipeline. Inputs Every dataset command reads chess records. They tend to come from one of three places: `*.puzzles.json` and `*.nonpuzzles.json` written by `puzzle mine` (see [Puzzle Mining](mining.md)). `.record` JSON arrays from analysis pipelines. Merged, filtered, or split files produced by `record files`. Newly written record rows include `\"schemaVersion\":\"crtk.record.v2\"` so later tools can distinguish the on-disk contract explicitly. Historical rows without that field are still accepted and treated as `crtk.record.v1`, so existing mining and export dumps remain readable. Look before you export. `record stats` gives you record counts and engines, `record tag-stats` shows the tag distribution (the same tags from [Position & Piece Tags](piece-tags.md)), and `record analysis-delta` surfaces positions whose evaluation never settled. A skewed class balance or a pile of unstable samples is cheap to spot here and expensive to discover three epochs into training. Eval Regression Tensors: `record dataset npy` Reads a single `.record` JSON array and writes a flat numeric feature matrix with scalar evaluation labels. This is the export for value-head or regression work that doesn't care about a chess-specific plane layout. Output (with `--output training/run` as the prefix): `training/run.features.npy` shaped `(N, 781)` float32 `training/run.labels.npy` shaped `(N,)` float32 Labels are evaluations in pawns, clamped to a stable range \u2014 left unclamped, a single forced mate would swamp the regression target and the model would spend its capacity chasing outliers. Omit `--output` and the prefix defaults to `dump/<input-stem>.dataset`. LC0-Style Tensors: `record dataset lc0` Reads a `.record` JSON array and writes LC0-shaped input planes with one-hot policy targets, a value label, and a metadata file \u2014 the export for policy/value experiments and for sanity-checking the LC0-style feature encoding. Output (with `--output training/run`): `training/run.lc0.inputs.npy` shaped `(N, 112*64)` float32 `training/run.lc0.policy.npy` shaped `(N, policySize)` float32 `training/run.lc0.value.npy` shaped `(N,)` float32 in `[-1, 1]` `training/run.lc0.meta.json` recording the policy encoding, value scale, compression, and source metadata Each policy row is one-hot over the legal-move target. Without `--weights`, rows use the raw 73-plane policy size (`4672`). Point `--weights PATH` at an LC0 CNN `.bin` file and crtk reads that network's policy map, compressing each target down to the network's own policy size \u2014 the rows then line up with the model you mean to train or evaluate against, instead of forcing a remap later. Binary Classifier Tensors: `record dataset classifier` Reads one or more record files or directories and writes binary-labeled input planes \u2014 for a puzzle vs. non-puzzle split, or any yes/no question you can phrase as a filter. Output (with `--output training/run`): `training/run.classifier.inputs.npy` shaped `(N, 21*64)` float32 `training/run.classifier.labels.npy` shaped `(N,)` float32 `training/run.classifier.meta.json` recording sources, filters, class counts, and the positive-class weight Labeling tries three sources in order, stopping at the first that applies: `--label-filter <dsl>` marks matching rows positive and the rest negative. A record `kind` field of `puzzle` or `nonpuzzle`, when present. The configured puzzle-verification Filter DSL, when `kind` is absent. `--filter <dsl>` narrows which rows are eligible before labeling, `--recursive` walks input directories, and `--max-positives` / `--max-negatives` cap each class \u2014 the quickest way to a balanced smoke set without re-mining. The Filter DSL is the same gating language `puzzle mine` uses; [Puzzle Mining](mining.md) covers its syntax. Labeled Positions: `record export training-jsonl` Writes one chess position per JSONL line \u2014 for pipelines that would rather read structured text than unpack binary tensors. Accepts one or more files or directories. Labels follow a fixed coarse/fine scheme: `coarse_label=1`, `fine_label=2` \u2014 rows matching the puzzle DSL (`--filter`); these become `verified_puzzle`. `coarse_label=1`, `fine_label=1` \u2014 rows that share a parent FEN with a puzzle. `coarse_label=0`, `fine_label=0` \u2014 every remaining row. Engine and PV details stay out of the model input by default. `--include-engine-metadata` keeps them as metadata only, `--recursive` walks directories, and `--max-records N` stops after N rows. Omit `--output` and the path defaults to `dump/<input-stem>.training.jsonl`. Policy-Aware Puzzle Rows: `record export puzzle-jsonl` Writes puzzle rows carrying LC0 policy-map information. The map comes from a network, so `--weights` pointing at a ChessRTK LC0 CNN `.bin` file is required, not optional. `--puzzles` keeps only puzzle records, `--nonpuzzles` runs negatives through the same shape, and `--filter <dsl>` selects rows more finely. Difficulty-Graded Puzzles: `record export puzzle-elo-jsonl` Scores verified puzzles and writes one JSONL row each, pairing an Elo rating with the position tags \u2014 what you want when the dataset needs difficulty, not just a binary label. Accepts one or more files or directories. Scoring runs a tree-search pass, which makes this the heaviest export here by a wide margin. `--threads N` (defaulting to the available processors) and `--max-records N` keep it in check. Already have a rating CSV from an earlier run? `--ratings-csv PATH` reuses it for a one-pass re-export and skips the search entirely. Text Exports: plain / csv / pgn Sometimes you just need to look at the data, hand it to a spreadsheet, or open it in something that speaks PGN. `record export` writes those flat formats from a `.record` JSON array: `record export plain` \u2014 a `.plain` text dump; mainline only by default, `--sidelines` (alias `--export-all`) pulls in variations, `--csv` writes a CSV alongside it. `record export csv` \u2014 a spreadsheet-friendly CSV. `record export pgn` \u2014 one PGN game per record. All three take `--filter <dsl>` to select records and default their output to `dump/<input-stem>.<ext>`. Evaluation Stability: `record analysis-delta` A position whose evaluation thrashed throughout analysis carries a noisier label than its final number admits. `record analysis-delta` writes one JSONL row per record showing exactly that motion: initial and final eval, the delta's type and magnitude, the fluctuation range, and the depth and time at which the value finally settled. It's the cleanest way to find and drop unstable samples before they reach the model. Full Pipeline: Mine, Inspect, Train a Classifier Min"
  },
  {
    "title": "Review To Study",
    "href": "review-to-study.html",
    "category": "Workflows",
    "text": "Review To Study `crtk review game` turns a PGN mainline into two durable artifacts: a per-ply review stream and, when requested, study material for drillable mistakes. The command is meant to answer \"what did I get wrong, and what should I practice from this game?\" without adding a GUI-only review model or an ungrounded coach layer. Command External UCI review is the default. Use `--offline` only when you want the deterministic in-process alpha-beta path instead of a configured engine. The Workbench command builder has a `Review game` template that emits the same CLI path. It does not recompute verdicts in Swing; it previews and runs the real `crtk review game` command. Outputs With no output flags, paths are derived from the PGN stem under `dump/`. Override paths with `--output`, `--study-output`, and `--record-output`. Review Rows Every review row carries `schemaVersion: \"crtk.review.ply.v1\"`. The row includes game identity, PGN headers, ply number, pre-move FEN, played move, best move, eval before and after, centipawn or WDL loss, PV, grounded tag deltas, mistake category, recommended action, optional study-unit id, and a `repro` block. Rows are written as JSONL so long reviews can be streamed and diffed line by line. External UCI rows record `repro.deterministic: false`; offline rows record `true`. Study Units `--to-study` converts rows whose `recommended_action` is `drill_puzzle` into `crtk.review.study_unit.v1` rows. The first slice intentionally emits only mistakes and blunders, not every inaccuracy. The review row's `study_unit_id` is set only when `--to-study` is active and a matching study unit is emitted. Record Handoff The Record sidecar uses the existing `crtk.record.v2` shape so record, puzzle, mining, and publishing tools can read it without a new parser. For each study unit: Record provenance is still stringly typed because `Record` does not yet have dedicated game-id and ply fields. Keep linkage in the `META:` tags until the store/schema layer grows first-class provenance fields. Validation Use the schema commands to inspect the contracts: The regression suite pins the JSON shapes with `ReviewRowSchemaRegressionTest` and the end-to-end fake-UCI review fixture in `ReviewCommandRegressionTest`. Boundaries Review-to-study is not an accuracy score, not a prose coach, and not an SRS scheduler. It emits structured, reproducible artifacts. Later Workbench panels and spaced-repetition features should consume those artifacts instead of recomputing verdicts."
  },
  {
    "title": "Tagging",
    "href": "piece-tags.html",
    "category": "Workflows",
    "text": "Position and Piece Tags A position holds a lot of truths at once \u2014 who moves, who's up material, where the pawns lock, whether the king is exposed, what tactic is hanging in the air. **Tags** are how ChessRTK names each of those truths as a single structured string, so a position becomes a stream of facts you can sort, count, and reason over instead of a board you have to re-read. The same schema feeds three consumers: the [Filter DSL](filter-dsl.md) that gates puzzle mining, the dataset exporters, and the T5 summaries. Every tag traces back to one chess core and a fixed set of deterministic taggers, so the same FEN always produces the same tags. That is the property that makes them worth filtering on, diffing, and handing to an LLM as grounded evidence \u2014 the strings don't drift under you. Two commands emit tags: `fen tags` works on any FEN, FEN file, or PGN, and `puzzle tags` walks a puzzle's principal variations and tags every move. `record tag-stats` rolls the distribution up across a mined dataset. For the precise parser grammar, family list, field identities, and golden fixtures, see the [Tag Reference](tag-reference.md). This page is the conceptual overview. ![Tagging pipeline](../assets/diagrams/crtk-tagging-flow.png) Tag Shape Each tag is a family prefix, a colon, then ordered `key=value` fields: Real output from `crtk fen tags`: They're plain strings, but the key/value layout lets tools sort, dedupe, group, count, filter, and render them to text with no bespoke parser. Field order within a family never changes, so even a naive substring match against a tag holds up. Two Kinds of Tags Tags come in two flavors, **heuristic** and **engine-derived**, and the line between them is a reproducibility line. A heuristic tag depends only on the position in front of it. An engine-derived tag also depends on which engine ran, how long you let it think, and which build you used \u2014 change any of those and the tag can change too. Heuristic tags cover material, pawn structure, king safety, piece placement, space, development, mobility, and the tactical motifs the core can prove on its own \u2014 pins, hanging pieces, the forced mates it can read directly off the board. The engine-derived families (`META: source=engine`, `META: eval_cp`, `META: wdl`, `PV`, `CAND`, `THREAT`, and the analysis-backed `IDEA` and `TACTIC` rows) only show up once analysis is requested or supplied. `puzzle tags` analyzes by default; to hold it to the static taggers, pass `--no-analyze`: `fen tags` is static unless you add `--analyze`. The Taggers A fixed set of taggers produces every tag. Each owns one or more families and runs deterministically over the core's view of the position \u2014 no tagger reaches outside the board it was handed. Families come and go with the position. A symmetric opening produces no passed-pawn or back-rank tags; an endgame produces `ENDGAME` rows a middlegame never will. Absence is itself a signal \u2014 don't read a missing family as a bug. Commands Tag a single position Include the FEN alongside the tags: Tag a file or a PGN `--mainline` keeps PGN input to the main line; `--sidelines` follows the variations too. `--sequence` reads a FEN file as one ordered game rather than a bag of independent positions \u2014 relevant the moment you care about delta tags or move-to-move structure. Tag a puzzle's lines `puzzle tags` takes the engine's top moves, expands each into a principal variation, and tags every move along the way: `--multipv` is how many lines to expand (default 3), `--pv-plies` is how deep each one runs (default 12), and `--tag-multipv` is the MultiPV the engine uses while tagging the individual moves (default 1) \u2014 a separate knob, because you often want several candidate lines but only one engine opinion per position inside them. Enrich with engine analysis `--analyze` opens `fen tags` up to eval, WDL, threats, and the analysis-backed motifs. Pin the engine budget every time \u2014 an unbounded search makes the run depend on how busy your machine was, and that is the opposite of what tags are for: You pick the engine with `--protocol-path` (an engine protocol TOML) and bound it with `--max-nodes`, `--max-duration`, `--threads`, and `--hash`. See [Configuration](configuration.md) for protocol setup and [In-House Engine](in-house-engine.md) for the built-in evaluator. Delta Tags A move's meaning is mostly in what it changed. `--delta` on `fen tags` emits JSONL rows describing how the tag set shifts from a parent position to a child \u2014 the king that just lost its shelter, the pawn that became passed. That is precisely the \"what changed after this move\" evidence that move explanations, puzzle-line summaries, and training rows are built on. For FEN-pair input, each line holds a parent and child FEN: For PGN input, the parent/child relationships fall out of the parsed movetext, so you don't spell the pairs out yourself. To compare analysis changes across a whole record file rather than one pair at a time, reach for `record analysis-delta`. Tags to Natural Language The T5 commands read the tags and write a short prose summary from them. Because the prose is only as informed as the tags underneath it, the same static-versus-engine split carries straight through: no `--analyze`, no engine facts in the text. Add `--analyze` for engine-enriched text, and keep the limits explicit in batch jobs \u2014 the same reproducibility argument as before, only now it decides what the sentences say: When you want deterministic prose and no model in the loop, `position describe` renders the same facts to fixed text. See [T5 Summaries](t5.md) for the model setup used by `fen text` and `puzzle text`. Reading PIECE Tags `PIECE` tags speak to where a piece sits and how much it can do. The fields you'll see most: `tier` \u2014 placement tier: `very_strong`, `strong`, `slightly_strong`, `neutral`, `slightly_weak`, `weak`, or `very_weak` `activity` \u2014 mobility signal such as `high_mobility` or `low_mobility` `extreme` \u2014 strongest/weakest piece anchors for the whole board or one side (`strongest`, `strongest_white`, `weakest_black`, and so on) `side` \u2014 `white` or `black` `piece` \u2014 `pawn`, `knight`, `bishop`, `rook`, `queen`, or `king` `square` \u2014 algebraic square A tier is an auditable hint, not a verdict \u2014 a knight on a \"weak\" square can still deliver mate. When you're explaining a forcing sequence, let `TACTIC`, `CHECKMATE`, and `KING` outrank a generic placement tier; concrete beats positional when the king is in danger. Summarizing a Dataset `record tag-stats` counts how often each tag occurs across a record file or mined dump. Run it before you train or publish: it surfaces the class imbalance, the family that never showed up, and the handful of generic tags drowning everything else \u2014 the problems that are cheap to fix now and expensive to discover in a trained model later. For the broader file-level picture there's `record stats`, and the [Filter DSL](filter-dsl.md) turns those same tags into selection criteria for export and dataset building. Why a Stable Schema The schema is frozen on purpose. Freeze it once and the same strings can drive three very different consumers without anyone re-deriving the facts: **Filters** \u2014 the [Filter DSL](filter-dsl.md) matches on tags to gate `puzzle mine` and to select records for `record files`, `record export`, and `record dataset`. **Datasets** \u2014 `record dataset npy|lc0|classifier` and `record export training-jsonl` carry tags as labels and features. **LLM agents** \u2014 tags are deterministic and readable, so a model can quote them as evidence rather than re-deriving facts it's prone to get wrong. Same FEN, same engine, same limits, same tags. The whole design rests on that one guarantee. Related Pages [Tag Reference](tag-reference.md) \u2014 full grammar, family identities, and fixtures [Filter DSL](filter-dsl.md) \u2014 selecting positions and records by tag [Mining](mining.md) \u2014 where tags gate puzzle quality [Configuration](configuration.md) \u2014 configuring the analysis behind `--"
  },
  {
    "title": "Tag Reference",
    "href": "tag-reference.html",
    "category": "Workflows",
    "text": "Tag Reference A tag is crtk's answer to a recurring problem: how do you let a language model talk about a chess position without letting it hallucinate the moves? You don't hand it prose. You hand it facts. A tag such as `MATERIAL: balance=white_up_exchange` or `TACTIC: motif=fork side=white move=e4c5` is a short, parseable string that states only what is verifiably true of the board, derived from the same chess core that generates moves, converts FEN/SAN/UCI, and runs perft. Feed the tagger the same FEN twice and you get the same tags in the same canonical order, every time. This page is the catalogue: every family, every field, every value, so you \u2014 or a model downstream \u2014 can quote them without guessing. Tags come out of `fen tags` and `puzzle tags`, get summarized by `record tag-stats`, and feed both the classical `position describe` pipeline and the optional T5 summaries behind `fen text` / `puzzle text`. To produce tags from a FEN, PGN, or puzzle line, see [Command Reference](command-reference.md) and [Example Commands](example-commands.md). For the design behind the shared core, see [Architecture](architecture.md). What tags are A tag is a single line in one canonical format: The **family** is uppercase (`FACT`, `META`, `TACTIC`, ...) and names the category of fact. **Fields** are space-separated `key=value` pairs. A bare `FAMILY:` with no fields is allowed. Values containing spaces, quotes, or backslashes are double-quoted, with the quote or backslash escaped: `name=\"King's Pawn Game\"`. Nothing here reaches for an external UCI engine. Every tag is derivable from the position itself; the only \"engine\" inputs permitted are crtk's own bounded searchers \u2014 alpha-beta, MCTS, the mate prover \u2014 plus the static evaluator, static exchange evaluation (SEE), and the bundled ECO opening book. Those searchers run under a fixed node and time budget, which is what keeps bulk tagging both deterministic and cheap. An unbounded search would give you sharper tags and a different answer every run; that tradeoff isn't worth it here. No move is invented. If a downstream text generator names a move, that move already appears in a tag \u2014 `CAND: ... move=`, `PV:`, `THREAT: ... move=`, `TACTIC: ... move=`, or `MOVE: only=`. Moves in `CAND` / `THREAT` / `IDEA` are SAN; static-motif and `MOVE: only=` moves are UCI. Producing tags Most paths into the tagger differ only in what they read \u2014 a single FEN, a list, a PGN, a puzzle line \u2014 and converge on the same generator. Both `fen tags` and `puzzle tags` emit from `chess.tag.Generator`. `position describe` consumes the same tags to build deterministic prose; `fen text` / `puzzle text` hand them to the T5 tag-to-text model. The full flag list lives in [Command Reference](command-reference.md). Determinism and stability guarantees The whole point of tags is that you can trust them across runs, so the generator is built to a few hard rules. **Deterministic.** The same FEN always yields the same tags. A tag asserts what is true of the position and nothing more \u2014 no mood, no hand-waving evaluation. **Sorted and de-duplicated.** Output is ordered by family rank, then lexicographically within a family, with exact duplicates dropped. The sort is idempotent, so re-sorting canonical output changes nothing. **Stable canonical family order:** `FACT` \u2192 `META` \u2192 `MOVE` \u2192 `THREAT` \u2192 `CAND` \u2192 `PV` \u2192 `IDEA` \u2192 `TACTIC` \u2192 `CHECKMATE` \u2192 `PIECE` \u2192 `KING` \u2192 `PAWN` \u2192 `MATERIAL` \u2192 `SPACE` \u2192 `INITIATIVE` \u2192 `DEVELOPMENT` \u2192 `MOBILITY` \u2192 `OUTPOST` \u2192 `ENDGAME` \u2192 `OPENING` \u2192 `MOVE_EFFECT` \u2192 `LINE` \u2192 `VARIATION` \u2192 `GAME`. **Regression-locked.** A new `pattern=` or `motif=` value ships with a golden fixture under `testdata/tags/*.tsv` and is cross-checked against the existing detectors before it lands. That cross-check is the safeguard that stops a new detector from quietly rewriting old output. Family overview Some families fire on every position; others only when the board earns them. The \"Emission\" column tells you which. FACT \u2014 static board observations The things you could read off the board without thinking: status, check, rights, en passant, who holds the center. META \u2014 metadata and evaluation Side to move, phase, opening, and whatever the bounded evaluator could establish under its budget. MOVE \u2014 legal-move facts Generate every legal move, then count them by kind. The counts say a lot about the position before any evaluation runs. Each count field is `=<n>`. THREAT \u2014 grounded next-move threats What the side to move is threatening \u2014 but only when a concrete legal move backs the claim. Mate-in-1 falls straight out of direct play; anything longer has to be proven by the bounded mate prover before it earns the tag. A `king_attack` needs a legal check landing into a shelter that is demonstrably weak, and it stands down when a mate threat already covers the position. Material threats that would require search to confirm are left out on purpose \u2014 they can't be grounded, so they don't get a tag. CAND \u2014 candidate moves Only with `--analyze`. One tag per move the bounded search puts forward. Fields: `role=best|alt move=<SAN> eval_cp=<int> note=\"<short reason>\"`. `note` carries a grounded reason rather than a verdict: `checkmate`, `wins material` (a SEE-positive capture), `promotes`, `check`, or empty when none applies. PV \u2014 principal variation Emitted only with `--analyze`. IDEA \u2014 grounded plans Plans, but kept honest: each cites a legal move or a board fact you can re-derive. No speculation about what \"should\" happen. Fields: `side=<color> type=<type> move=\"<SAN>\" detail=\"<short grounded reason>\"`. TACTIC \u2014 tactical motifs The motifs a coach would point to. Each carries `motif=<id> side=white|black`, then whatever fields the motif needs to be specific: `move=<uci>`, `piece=`, `square=`, `detail=\"...\"`, and motif-specific extras like `targets=<piece@sq,...>` for a fork, `front=`/`behind=` for a skewer, `slider=`/`target=` for a discovered attack, `count=`/`squares=` for rooks on the seventh, or `see=<cp>` for material loss. CHECKMATE \u2014 mate identity This family fires once the game is already over \u2014 the board is mated, and these tags describe how. **Named patterns currently emitted:** `back_rank_mate`, `smothered_mate`, `corner_mate`, `support_mate`, `double_check`, `arabian_mate`, `epaulette_mate`, `anastasia_mate`, `david_and_goliath_mate`, `damiano_mate`, `scholars_mate`, `swallows_tail_mate`, `dovetail_mate`, `hook_mate`, `opera_mate`, `lawnmower_mate`, `blackburne_mate`, `greco_mate`, `kill_box_mate`, `reti_mate`, `anderssen_mate`, `mayet_mate`. A named pattern is a refinement, not a replacement: it sits alongside the generic geometry it specializes, so `anastasia_mate` shows up next to `support_mate` rather than instead of it. Each detector runs only on an already-mated board, ships its own golden fixture, and is cross-collision-probed against the rest \u2014 the same board fed to every detector \u2014 so two patterns never claim the same mate by accident. PIECE \u2014 piece roles and activity How each piece is doing \u2014 where it sits, how much it can do, and where it ranks against its peers. KING \u2014 king safety PAWN \u2014 pawn structure MATERIAL \u2014 material balance SPACE, INITIATIVE, DEVELOPMENT, MOBILITY, OUTPOST The slower-burning positional signals, each reduced to a side and a count where one exists. ENDGAME and OPENING `OPENING` tags appear only when the bundled ECO book matches the position \u2014 out of book, there is no honest code to emit. Dynamic game-analysis families These four families come from `fen tags --pgn FILE --analyze-game`. They are grounded by replaying a parsed game through the same chess core, so malformed or illegal branches are truncated instead of guessed. Tag deltas and identity Walking a game move by move, you care less about the full tag set than about what just changed. `fen tags --delta` gives you that. The trick is comparing tags by **semantic identity** instead of raw text, so editing a value reads as a single change rather than one remove pair"
  },
  {
    "title": "Book Publishing",
    "href": "book-publishing.html",
    "category": "Workflows",
    "text": "Book publishing Most chess-book pipelines bolt SAN rendering onto a TeX stack and hope the two agree. The `book` area skips that entirely: it writes print-ready books, covers, and diagram sheets straight to native vector PDF, with no LaTeX, no TeX distribution, and nothing to install beyond the runtime. Feed it a JSON or TOML manifest (or a `.record` dump) and it emits a finished PDF through the same chess core that backs every other command, so the SAN, the diagrams, and the figurine notation are the ones you already trust. Five subcommands span the path from raw material to a press-ready product. `book collection` turns a record dump into a dense puzzle book, `book study` renders deeply annotated studies, `book render` typesets a manifest into the interior, `book cover` builds the matching wraparound, and `book pdf` cuts quick diagram sheets. Each one takes a `--check` dry run that validates geometry, FENs, and solution moves before a single byte hits disk. Why native PDF matters **No toolchain.** `crtk book render -i book.toml -o book.pdf` produces a finished PDF on a clean machine with only Java 17 present. No LaTeX packages, no font installs. **Deterministic output.** A manifest renders byte-stable diagrams and SAN every time, which makes a book diffable and reviewable in version control like any other source artifact. **One shared chess core.** FEN parsing, legal-move validation, and SAN run the exact code paths behind `move`, `engine`, and `fen`. Solution lines are validated, not taken on faith. **Figurine notation.** Caption and table SAN is converted to figurine algebraic, so `Qxe8+` prints with a queen figurine and a multiplication sign where the capture is. Command map Typical render then cover flow Two commands cover the usual case: render the interior, then cut a cover from the same manifest. Hand the rendered interior to `book cover --pdf` and the cover command reads trim width, trim height, and printed page count directly out of the file instead of guessing them. The page count drives the spine width, so this is the difference between a cover that fits and one that does not. Without `--pdf`, the cover command works down a fallback chain: manifest `paperwidth` / `paperheight` for trim size, manifest `pages` for the count, and failing that an estimate from the puzzle grid and solution-table cadence. The further down that chain it goes, the more the spine is a guess. Pass `--pages` to pin the count yourself when you need the spine width to be exact. The `--check` dry run Every `book` subcommand accepts `--check` (alias `--validate`), which runs the full pipeline and writes nothing: `book render --check` validates the manifest, page dimensions, every FEN, and every solution line. `book cover --check` validates the same manifest and prints the calculated trim, spine, and full-cover dimensions so you can compare them against your printer's calculator before uploading. `book collection --check` and `book study --check` validate the generated model and layout without writing output. Wire `--check` into CI. It catches a malformed FEN or an impossible solution move while the cost is a failed build, not a printed page. `book render`: manifest to interior PDF This is the typesetter the rest of the area leans on. It reads a chess-book manifest in JSON or TOML, lays out the puzzle grid with solution tables interleaved through it, and writes the interior PDF. For reviewer or free-edition copies, watermark the PDF so a leaked file points back to whoever it went to. `--watermark-id` implies `--watermark` and stamps page-specific overlay text plus matching PDF metadata: Render manifest shape The renderer takes JSON or TOML; TOML tends to age better under hand-editing. The keys below are the ones it actually reads. Key manifest fields: `paperwidth` / `paperheight` are the trim dimensions, in centimeters. `innermargin`, `outermargin`, `topmargin`, `bottommargin` set the margins, also in centimeters. `tablefrequency` is how many puzzle pages pass between solution tables. `puzzlerows` and `puzzlecolumns` set the diagram grid per puzzle page. `pages` is what cover generation falls back to for spine width when no interior PDF is supplied. `blurb` reappears on the back cover; `imprint`, `dedication`, `introduction`, `howToRead`, and `afterword` are repeatable front- and back-matter paragraph blocks. Each `[[elements]]` entry needs a starting FEN in `position` and a SAN solution line in `moves`. `book cover`: matching cover PDF The cover reads the same manifest and prints to spec. Its geometry is a product of four inputs: trim size, binding type, interior paper, and printed page count. Change any one and the spine moves. Bindings `paperback`: wraparound back + spine + front cover with 0.125 inch bleed on every outside edge. `hardcover`: case-laminate cover with 1.5 cm wrap and 1.0 cm hinge allowance. `ebook`: front-cover-only PDF; spine and back cover are omitted. Interior paper and spine width Spine width is `pages * paper-thickness`, and thickness per page is set by the interior token: Before any print-on-demand upload, run `book cover --check` and reconcile the reported trim, spine, and full-cover values against your printer's own cover calculator. The two should agree to the millimeter; if they do not, fix it here rather than discovering it in a proof. `book collection`: record dump to dense puzzle book When the material already exists as mined data, this is the shortest route to a finished book. The source is a record JSON or JSONL dump whose rows each carry a starting `position` and an `analysis` block holding a principal variation. The command takes the first PV from each row, rewrites it as move-numbered SAN, writes a TOML manifest, and will render the interior and cover in the same pass if you ask it to. Leave `--subtitle` off and you get `\"<count> Chess Puzzles\"`. Leave the front-matter paragraphs off and the builder fills in a compact default introduction while keeping its native how-to-read and afterword text. A record with no usable position or PV is dropped, and the dropped count shows up in the summary, so a thin book is never silent about why. Record dumps come from the mining and dataset pipeline. See [Mining puzzles](mining.md) for how to produce them and [Datasets and exports](datasets.md) for the record export and filter options. `book study`: deeply annotated studies Reach for this when each entry is more than a puzzle: description text, comments, hints at several depths, a diagram for every move. Rather than collapse an entry to `position + moves`, it keeps the full `Composition` model, which is why the page becomes one large diagram wrapped in prose instead of a grid. It reads JSON or TOML, optionally writes back a normalized TOML manifest, renders the composition-style interior, and can build the matching cover in the same pass. Study manifest shape A study manifest is a `[[compositions]]` array, each entry pairing annotated text with a diagram backbone that stays aligned to it. Key study fields: `[[compositions]]` is the repeated annotated-entry list. `description`, `comment`, `analysis`, and `hintLevel1` through `hintLevel4` map straight onto the rendered text blocks. `figureFens` is the diagram backbone; `figureMovesAlgebraic`, `figureMovesDetail`, and `figureArrows` are parallel arrays that must stay index-aligned with it, or the captions drift off their boards. `pageSize`, `margin`, `diagramsPerRow`, `boardPixels`, `whiteSideDown`, and `showFen` govern the composition-PDF layout. An optional `id` per composition tags the entry for cross-referencing. `book pdf`: quick diagram sheets Not everything needs to be a book. `book pdf` lays out a plain sheet of diagrams from FENs, a FEN list, or a PGN. The three inputs are mutually exclusive: pick exactly one of `--fen`, `--input`, or `--pgn`. Regression checks Touch the publishing code or a manifest and run these headless tests; they exercise the native PDF paths end to end: Keep `-Djava."
  },
  {
    "title": "AI Agents & Automation",
    "href": "ai-agents.html",
    "category": "Workflows",
    "text": "Automation and AI Agents A program driving `crtk` should never have to guess. Every command is a noun-verb invocation (`crtk move list`, `crtk engine bestmove`, `crtk fen validate`) that takes its inputs through named flags, writes a stable line-based or JSON/JSONL shape to stdout, and exits with a code that means what it says. One chess core handles legality, notation, and search for the entire toolkit, so the FEN/SAN/UCI semantics an agent learns from one command hold everywhere else. What follows: where the machine-readable contracts live, how to keep a run from going open-ended, and how to push a whole plan through in a single process. ![Agentic command contracts](../assets/diagrams/crtk-agentic-commands.png) Why crtk Is Agent-Friendly **Deterministic outputs.** Move ordering, FEN normalization, perft counters, and the built-in search return the same answer for the same inputs and budgets. The core paths carry no hidden clocks and no randomized defaults. **Self-describing.** `crtk help --json` emits the entire command tree \u2014 paths, aliases, usage, flags, examples, and the exit-code taxonomy \u2014 as a deterministic `crtk.cli.catalog.v1` object. Discover the surface from the catalog instead of hard-coding it; `crtk help --json <area> <action>` scopes it to one subtree. **Stable contracts.** Most machine-facing commands take `--json` (one object or array) or `--jsonl` (one object per line); notation commands take `--format uci|san|both`. Parse a documented shape rather than scrape prose. **Explicit inputs.** Positions come in through named flags (`--fen`, `--input`, `--startpos`, `--randompos`, `--stdin`), never positional guesswork. When a free-form argument could pass for a flag, separate it with `--`. **Exit codes that mean something.** Success exits `0`; a bad FEN, a missing file, or a failed command exits non-zero. Errors go to stderr, so a failing run still leaves stdout clean for the parser. **Bounded work.** Engine and search commands take `--max-nodes`, `--max-duration`, and `--multipv`; batch and export commands take `--limit`, `--max-records`, and `--max-total`. Nothing forces an agent to trust an unbounded run. **One shared core.** Move generation is checked against stored perft truth positions (`engine perft-suite`), so a legality-sensitive workflow is correct without an external engine to vouch for it. Quickstart for an Agent The smallest useful loop \u2014 is this position legal, and what can I play from it: Warm Process: `crtk serve` For interactive agents and editor integrations, `crtk serve` keeps one JVM warm and exposes the same command dispatcher over localhost HTTP. It binds `127.0.0.1` by default, refuses non-loopback hosts, and exposes only `/rpc`, `/catalog`, and `/health`. Use `/catalog` for the same command contract as `crtk help --json`: Run commands through JSON-RPC by sending the exact argv tokens after `crtk`: The result contains `exitCode`, `stdout`, `stderr`, and the argv the daemon dispatched. The stdout text is the CLI's stdout byte-for-byte, so existing JSON parsers can keep using the command-specific contracts. Requests are serialized through one dispatcher thread because the underlying CLI writes to process-wide stdout and stderr. See [Local JSON-RPC daemon](serve.md) for endpoint details and safety boundaries. Python integrations can use the generated SDK instead of hand-writing JSON-RPC requests: Machine-Readable Output by Command These are the commands worth reaching for first, paired with the contract each one emits. Notation commands (`move list`, `move uci`, `move san`, `move both`, `move to-san`, `move to-uci`, `move after`, `move play`) and `fen validate`/`fen normalize` also accept `--no-header` and `--quiet` for script-friendly piping. Bounding Engine and Search Runs Cap every automated analysis. The budget flags are consistent across commands, so one agent-side policy covers all of them. **External UCI engine** (`engine analyze`, `bestmove`, `bestmove-batch`, `analyze-batch`, `compare`, `threats`): bound it with `--max-nodes` and `--max-duration` (`5s`, `500ms`), and pin `--multipv`, `--threads`, and `--hash` to keep the engine reproducible. Point at a protocol with `--protocol-path` (or `--left-protocol`/`--right-protocol` for `compare`). **Built-in search** (`engine builtin`, alias `engine java`): use `--depth`, `--max-nodes` (alias `--nodes`; `0` lifts the cap), and `--max-duration` (`0` means no time cap). It runs in-process; `--threads` is available for alpha-beta Lazy SMP or MCTS workers, while `--hash` is only for external UCI engines. Choose an evaluator with `--evaluator classical|nnue|lc0|otis`. **Forced-mate prover** (`engine mate`): bound it with `--max-mate` (default `4`) and `--max-nodes`. This is brute force, no neural-network evaluation \u2014 depth, not heuristics, is what costs you. **Mining / dataset / book** commands: cap volume with `--max-total`, `--max-waves`, `--max-frontier` (`puzzle mine`), `--max-records` (record exports), or `--limit` (`book collection`, `book render`). See [In-House Engine](in-house-engine.md) and [LC0 Integration](lc0.md) for engine setup and evaluator details. Batch Many Commands in One Run `batch run` reads a UTF-8 script, one `crtk` command per line. Blank rows and `#` comments are skipped, and the leading `crtk` token is optional. For a fixed plan, this beats shelling out repeatedly \u2014 the runtime starts once instead of once per call. Script behavior: Each command is echoed before it runs; `--quiet` suppresses the echo and trims output to the results. The run stops at the first command that exits non-zero. `--keep-going` continues through the remaining rows and still reports the failures. `--stdin` reads the script from standard input \u2014 handy when the agent writes the plan as it goes. Example `commands.crtk`: For large position sets, reach past `batch run` for the dedicated batch engine commands: they load the engine once and stream one JSON object per position. Scripting Recipes **Verify legality before acting.** Validate, normalize, then enumerate. **Advance a game without hand-editing FEN fields.** Let the core apply the moves; it tracks castling rights, en passant, and clocks for you. **Diff two candidate positions.** A structured field/square delta, not a string comparison that breaks on cosmetic FEN differences. **Tag positions deterministically** for labeling pipelines (no engine required unless you add `--analyze`): **Generate natural-language summaries** with the local T5 model (see [T5 Summaries](t5.md)): Move-Generation Checks in CI With one shared core, perft is the legality gate that matters. Run it after any change that could reach move generation \u2014 a regression here is silent everywhere else. `engine perft-suite` prints a progress bar, then a table with `No`, `Depth`, `FEN`, `Truth`, `Calculated`, `Speed`, and `Match`. A custom `--suite` TSV takes `name<TAB>depth<TAB>fen<TAB>nodes` rows, with shorter variants falling back to `--depth`. GPU acceleration is optional through `--gpu` with `--split <n>`, and quietly drops to CPU when no backend is present. See [GPU Backends](gpu.md) and [Quality and Testing](quality-and-testing.md). Record and Dataset Plumbing Data transformations stay explicit and reproducible because nothing is implicit: inputs and outputs are named, and exports emit stable JSON/JSONL or tensor artifacts. Dataset and JSONL exports: Puzzle mining (`puzzle mine`) writes `*.puzzles.json` / `*.nonpuzzles.json` and is gated by the Filter DSL. See [Puzzle Mining](mining.md), [Datasets](datasets.md), and [Filter DSL](filter-dsl.md). Game review (`review game --to-study`) writes `*.review.jsonl`, `*.study.jsonl`, and `*.study.record.json`. The review rows keep every analyzed ply; the study rows and Record file keep drillable mistakes. See [Review to Study](review-to-study.md). Setup Gates for Pipelines Run these as preflight checks before the real work, at the top of a CI job or agent session: `crtk doctor` \u2014 checks the runtime, config, proto"
  },
  {
    "title": "Architecture",
    "href": "architecture.html",
    "category": "Project",
    "text": "Architecture Most chess tools accrete a second, third, and fourth notion of \"what is legal\" as they grow, and then spend their lives reconciling them. ChessRTK (\"crtk\") refuses to. There is **one shared chess core**, and every part of the tool asks it the same questions. A FEN that is legal for `move list` is legal in exactly the same way for `engine perft`, `fen tags`, `record dataset npy`, `book render`, and the Swing `workbench`. Notation, search, evaluation, tagging, datasets, rendering, publishing \u2014 all of it sits on the single `chess.core` model of the board and the rules. That is why two commands never quietly disagree. This page maps the layers, names the real packages, and follows a command from a CLI flag down to its output. What \"one shared core\" buys you The bugs that hurt most in chess tooling are the consistency bugs. Let notation conversion believe one thing about castling rights and the move generator believe another, and the disagreement leaks into everything downstream: a SAN string, a perft count, a tagged puzzle, a training tensor, a printed diagram. None of them is obviously wrong on its own. Routing every question through one core closes that gap: Is this FEN legal, and what is its normalized form? Which moves are legal here, and what are their SAN and UCI spellings? What position results after this move or this whole line? Do perft counts match known reference positions (including Chess960)? What should the tagger, dataset exporter, renderer, and book generator all see? One implementation answers all of them, so `fen normalize`, `engine perft-suite`, and a rendered PDF are demonstrably talking about the same chess. ![ChessRTK position toolbox](../assets/diagrams/crtk-position-toolbox.png) Layer map The codebase is a stack, and the dependency arrows point one way. Lower layers know nothing of the ones above; higher layers reuse the core instead of reinventing chess inside themselves. Entry is `application.Main`, which dispatches into `application.cli`. Chess core (`chess.core`, `chess.struct`) Everything else stands on this. `Position` holds the mutable board state; `MoveGenerator`, `MoveList`, and `Move` produce and represent legal moves; `PositionRules`, `PositionMoveSupport`, and `PositionUndoState` carry make/undo and legality; `SlidingAttacks` and the attack helpers answer the check, pin, and threat questions. Notation lives in `Fen` and `SAN`, with `Setup` handling Chess960 start positions and the castling rules they imply. `chess.struct` defines the `Record`, `Game`, `Pgn`, and `Plain` shapes that the workflow layers pass around. Crucially, nothing here depends on engines, neural nets, or I/O \u2014 the core can be reasoned about, and tested, in isolation. Movegen tooling (`chess.debug`) Perft is how a move generator proves it is honest: count the leaves to a fixed depth and compare against numbers the chess world already agrees on. `Perft`, `SplitPerft`, and `PerftSuite` do exactly that, backing `engine perft`, `engine perft-suite`, and `engine benchmark`. The `chess.debug.gpu` package (`GpuPerft`, `NativePerftBackend`, `PositionCodec`) can hand the deep part of the tree to a native GPU library; `--split N` sets where the CPU stops expanding and the device takes over. Search (`chess.engine`) The built-in search never leaves the JVM, which keeps it dependency-free and trivially scriptable. `AlphaBeta` (with `AspirationWindow`, `NegamaxSetup`, `QuiescenceSetup`) is the default `engine builtin` search for classical/NNUE and plays the Workbench Play opponent; `Mcts` and `MctsUci` provide the policy/value Monte Carlo Tree Search used by LC0/OTIS and by the minimal UCI loop. `MateProver` and `ProofState` are the brute-force forced-mate prover behind `engine mate`. `EvaluatorBackend`, `SearchBackend`, and the `*Backend` classes (`CnnBackend`, `OtisBackend`, `Bt4Backend`) are the seam between search and whatever scores a position. Evaluation (`chess.eval`, `chess.classical`) One abstraction, several scorers behind it. `chess.eval` defines the evaluator interface (`Evaluator`, `Backend`, `Kind`, `Factory`) and ships `Classical`, `Nnue`, `Lc0`, and `Otis` implementations, plus `See` for static exchange evaluation. The classical scorer's heavier scanning routines live in `chess.classical`, kept separate so the hot loops stay readable. Together they back `engine static` (classical only) and `engine eval` (`auto`, `lc0`, `otis`, or `classical`). Neural networks (`chess.nn`, `chess.gpu`) Model loading and inference live under `chess.nn`: `chess.nn.lc0` (the CNN and BT4 paths), `chess.nn.nnue`, `chess.nn.otis`, `chess.nn.t5` (the natural-language summaries behind `fen text` and `puzzle text`), and `chess.nn.classifier`. `chess.gpu` (`BackendNames`, `SharedLibrarySupport`) decides which backend is live \u2014 `cpu`, `cuda`, `rocm`, or `oneapi` \u2014 and the native CUDA, ROCm, and oneAPI sources for OTIS and T5 sit right beside the in-process loaders that call them. Treat the neural networks as usable evaluators, not clones of their upstream namesakes. The LC0 CNN path works as a CNN evaluator; the BT4 path is simplified and experimental, and crtk makes no claim of bit-exact LC0 or BT4 parity. When you want the strongest play, point crtk at a real external engine through `chess.uci`. External engines (`chess.uci`) When you need real strength, this layer talks to it. `Engine`, `Protocol`, `Analysis`, and `Output` drive external UCI engines such as Stockfish and Leela Chess Zero \u2014 spawning the process, speaking the protocol, and parsing back the MultiPV, WDL, node, time, thread, and hash fields. It powers `engine analyze`, `engine bestmove`, `engine threats`, `engine compare`, the `*-batch` commands, and `engine uci-smoke`. Each binary is described by a protocol TOML file passed with `--protocol-path`, so swapping engines is configuration, not code. Workflow and I/O (`chess.io`, `chess.puzzle`, `chess.tag`, `chess.describe`, `chess.eco`) Above the core sit the products a research pipeline actually ships: `chess.io` \u2014 record readers/writers and exporters: `RecordPgnExporter`, `RecordDatasetExporter`, `RecordLc0Exporter`, `ClassifierDatasetExporter`, `PuzzleEloExporter`. These back `record export *`, `record dataset *`, and `record files`/`stats`. `chess.puzzle` \u2014 puzzle scoring and difficulty signals (`Scorer`, `Difficulty`, `Goal`) used by `puzzle mine`. `chess.tag` \u2014 deterministic position/tactic/theme tagging (`Generator`, `Emitter`, `Checkmate`, plus `core`, `material`, `move`, `pawn`, `piece`, `position` detectors) behind `fen tags` and `puzzle tags`. `chess.describe` \u2014 deterministic classical position descriptions behind `position describe`. `chess.eco` \u2014 ECO opening lookup. Rendering and publishing (`chess.images`, `chess.pdf`, `chess.book`) `chess.images.render` draws boards to PNG/JPG/BMP/SVG with arrows, circles, legal-move dots, and themes (behind `fen render` and `fen display`). `chess.pdf` is a from-scratch PDF writer \u2014 **no LaTeX, no external typesetter** \u2014 which means a clean machine produces the same book as a cluttered one. `chess.book` (`collection`, `study`, `render`, `cover`) assembles publication-ready puzzle books and diagram PDFs behind the `book` commands. Application layer (`application.cli`, `utility`) This is where the noun-verb grammar lives. `CliRegistry` maps each area (`fen`, `move`, `engine`, `record`, `puzzle`, `book`, `position`, `config`, `batch`, `doctor`, `clean`, `workbench`, `version`, `help`) to its `application.cli.command.*` handler. The consistency you feel using crtk comes from shared helpers \u2014 `EngineOps`, `EvalOps`, `ConfigOps`, `PathOps`, `Format` \u2014 rather than copied conventions. `utility.Argv` parses flags and `application.Config` resolves defaults, so `--fen`, `--input`, `--output`, `--format`, `--max-nodes`, and `--max-duration` mean the same thing everywhere they appear. Desktop workbench (`application.gui.workbench`) The native Swing workbench (`workbench`, alias `gui`) is deliberately thin: a UI ove"
  },
  {
    "title": "Quality & Testing",
    "href": "quality-and-testing.html",
    "category": "Project",
    "text": "Quality and Testing Because everything in crtk routes through one chess core \u2014 legal move generation, make/undo, attack detection, FEN/SAN/UCI conversion, Chess960, perft \u2014 a single regression suite can guard parsing, engine analysis, datasets, rendering, and PDF publishing in one pass. Correctness isn't a phase here; it's the product. The suite is a plain Bash script with named targets: `./scripts/run_regression_suite.sh <target>`. Each target is deterministic and builds with `javac --release 17` (no Maven, no Gradle). The same script drives your laptop and GitHub Actions, which is the whole point \u2014 a green local run is the same checks that gate CI, so it means a green CI run. Run commands from the repository root (`/home/lennart/Code/chess-rtk`). Examples below use `crtk` as shorthand for `java -jar crtk.jar`. The One Command You Run Most This is the pass to run before pushing. It builds, lints sources and shell scripts, then walks the core, CLI, engine, UCI, book, perft-smoke, and jar groups in order. Green means the change is ready to share. The `release` alias points at the same target, so cutting a release runs exactly these checks and nothing extra. One bar, used everywhere. Suite Targets Run a single group when you only touched one area and want a fast answer. The script announces each step as it goes (`==> build`, `==> lint`, `==> jar`, and so on), so a failure tells you immediately which stage broke. A few examples: No target falls back to `recommended`. An unknown target prints the valid list and exits non-zero, so a typo never quietly runs the wrong thing. Move-Generation Confidence with Perft Perft (performance test) counts the legal leaf nodes reachable at a fixed depth. The answer is one integer with a known correct value, which is exactly what makes it merciless: any change that touches legality, Chess960 castling, en-passant, or promotion either reproduces the count or it doesn't. `engine perft` walks one position. `--divide` (alias `--per-move`) breaks the total down by root move, which is how you bisect a mismatch to a single move; `--threads N` splits those root moves across workers. `engine perft-suite` checks a set of stored truth positions against the core move generator. Depth is clamped to `1..6` (default `6`). Point `--suite PATH` at a tab-delimited file to test your own positions \u2014 rows are parsed by column count, so several shapes work: Neither command touches Stockfish or any external UCI engine. Both exercise the core move generator and only that \u2014 what they measure is crtk's own legality, not someone else's. Optional GPU Perft When a native backend (CUDA, ROCm, or oneAPI) is built and loadable, perft hands leaf expansion to the GPU. When it isn't, crtk takes the CPU path without comment, so the same command works on any machine. `--gpu` requests the native backend; `--split N` is how deep the CPU expands before handing whole subtrees to the GPU \u2014 the knob that trades launch overhead against parallelism. Run `crtk engine gpu` to print the JNI backend status (`--verbose` for detail). See [GPU Acceleration](gpu.md) and [Build and Install](build-and-install.md) for setup. Tuning the Perft Smoke Run A perft smoke test is only useful if it fits the runner. Three environment variables scale the cost of `perft-smoke` to your hardware; a fourth decides whether the `uci` target insists on a live Stockfish. Engine and UCI Confidence Reach for these after touching external-engine setup or protocol handling. Confirm the configuration parses, then prove the round trip with the smallest search that can fail: `engine uci-smoke` launches the configured engine and runs a one-node search \u2014 just enough to prove the UCI handshake completes end to end. The `uci` suite target wraps it: the orchestration regression always runs, and the live smoke runs too when `stockfish` is on `PATH`. Set `CRTK_REQUIRE_STOCKFISH=1` and a missing Stockfish becomes a hard failure instead of a silent skip. That is precisely how CI runs it, because a test that quietly skips itself isn't a test. See [Configuration](configuration.md) for protocol TOML files and [LC0 and External Engines](lc0.md) for engine setup. Determinism Same inputs, byte-identical outputs, every run. This isn't a nicety \u2014 it's what lets a golden comparison mean anything and what lets a reported failure reproduce on the first try. The build is hermetic: `compile_sources` deletes `out/`, finds every `*.java` under `src/`, sorts the list, and compiles with a fixed `--release 17`. No discovery order to drift. Core checks compare against stored golden values \u2014 perft node counts, SAN strings, tag fixtures, JSON shapes \u2014 so any drift lands as a concrete diff, not a vague flake. `lint` runs `git diff --check`, which means a stray trailing space or a leftover conflict marker fails the suite outright. The random generators (`--randompos`, `fen generate`) break the rule on purpose: they draw fresh positions every run. Pin an explicit FEN in a regression instead of assuming a \"random\" input will recur across runs or machines \u2014 it won't. To lean on determinism directly, run a command twice and diff: What the Regression Groups Cover Each group maps to a cluster of focused regression classes under the `testing` package. A sample of what's behind each target: **Core** \u2014 `PositionRegressionTest`, `CoreMoveGenerationRegressionTest`, `SplitPerftRegressionTest`, `SANRegressionTest`, `Chess960SetupRegressionTest`, `ParserRegressionTest`, `TaggingRegressionTest`, `TagFixtureRegressionTest`, plus JSON, XML-security, install-script, source-header, and workbench-structure checks. **CLI** \u2014 `CLICommandRegressionTest`, `PGNRegressionTest`, book/cover/PDF command tests, and puzzle collection/study command tests. **Engine** \u2014 `BuiltInEngineRegressionTest`, `PuzzleDifficultyRegressionTest`, `BT4RegressionTest`, `T5RegressionTest`, `OtisBackendRegressionTest`, `GpuPerftRegressionTest`. **UCI** \u2014 `UCIRegressionTest`, plus the live `engine uci-smoke`. **Book** \u2014 `BookRegressionTest`, `ChessPDFRegressionTest`, `PDFDocumentRegressionTest` (run headless). A word on neural-network fidelity, stated plainly. The LC0 CNN, NNUE, and OTIS evaluators are usable research evaluators; the BT4 path is simplified and experimental. crtk does **not** claim bit-exact parity with upstream LC0/BT4, and the docs won't pretend otherwise. `BT4RegressionTest`, `T5RegressionTest`, and `OtisBackendRegressionTest` guard crtk's own deterministic behavior \u2014 they pin what crtk does, not what upstream does. Documentation Confidence These pages and the printable manual are generated from this same wiki source, which puts documentation inside the quality gate rather than beside it. Under the hood the `docs` target runs the two generators in sequence: After regenerating, eyeball the links and assets under `docs/` before publishing. See [Development Notes](development-notes.md) for the docs pipeline. Continuous Integration CI lives in `.github/workflows/ci.yml` and fires on every pull request, every push to `main`, and manual dispatch. It calls the same `run_regression_suite.sh` you run locally \u2014 there is no second CI-only code path waiting to drift out of sync with the one you can see. The workflow: Checks out the repo and sets up Temurin Java 17. Installs `shellcheck` and `stockfish` so lint and the live UCI smoke have what they need. Runs each target as its own step \u2014 `build`, `lint`, `core`, `cli`, `engine`, `book`, `docs`, `uci`, `perft-smoke` \u2014 so a red check names the failing stage directly. The packaged-jar smoke rides along inside `recommended`/`ci` locally. Pins the smoke run to `CRTK_PERFT_THREADS=2`, `CRTK_PERFT_DEPTH=4`, `CRTK_PERFT_SUITE_DEPTH=4`, and sets `CRTK_REQUIRE_STOCKFISH=1` on the UCI step so a missing engine breaks the build rather than passing on a skip. To reproduce the full CI gate in one local command: Environment Health Check Separate from the regression suite, `doctor` audits the environment around crtk \u2014 Java version, configura"
  },
  {
    "title": "Development Notes",
    "href": "development-notes.html",
    "category": "Project",
    "text": "Development Notes One rule shapes everything in this codebase: there is exactly one chess core, and everyone uses it. Commands, the desktop Workbench, the puzzle miner, the dataset exporters \u2014 all of them route legality, FEN/SAN/UCI, and Chess960 through the same code, so the same input produces the same output every time. Protecting that property is the thing to keep in mind as you read the rest of this page, which covers the repository layout, the build (and the one compiler flag you cannot skip), the regression suite, regenerating the docs site and manual PDF, re-rendering the diagrams, and the conventions the source follows. For end-user workflows see [Getting Started](getting-started.md) and the [Command Reference](command-reference.md). For the system map, read [Architecture](architecture.md). For build prerequisites and install paths, see [Build & Install](build-and-install.md). Prerequisites A JDK 17 or newer with `javac` on `PATH`. A newer JDK works fine; the build targets the 17 release regardless. `jar` (ships with the JDK) to package `crtk.jar`. Optional: `python3` for the docs/manual scripts, Graphviz (`dot`) for diagrams, a headless Chromium/Chrome for the manual PDF, and native toolchains (CUDA/ROCm/oneAPI) only if you build GPU backends. No Maven, no Gradle, no third-party dependencies. Everything compiles from `src/` with the stock JDK. Repository layout `src/application/` \u2014 CLI and Workbench `application.Main` is the `main-class` baked into `crtk.jar`, and not much else \u2014 a thin launcher that hands off to `application.cli`. `application.cli.CliRegistry` maps each area/action to a `CliCommand` implementation under `application.cli.command`. One class per command (`AnalyzeCommand`, `PerftCommand`, `Chess960Command`, and so on) keeps the dispatch table flat and each command testable in isolation. `application.cli.Constants` holds the canonical command tokens. The plumbing every command shares \u2014 engine setup, eval setup, record I/O, FEN/PGN parsing, output formatting, validation \u2014 lives in sibling classes like `EngineOps`, `EvalOps`, `RecordIO`, `PgnOps`, and `Format`, so a new command rarely has to reinvent any of it. `application.gui.workbench` is the Swing Workbench launched by `workbench` (alias `gui`): board view, play-vs-engine, command forms, batch jobs, dataset tools, logs, publishing previews, puzzles, and neural-net visualizers. Workbench UI changes should follow the [Workbench Design Guide](workbench-design-guide.md): use `Theme`, `Ui`, shared layout primitives, and focused Workbench regressions instead of local styling copies. `src/chess/` \u2014 the shared core and everything built on it `chess.core` is the canonical rules engine: `Position` (mutable state, FEN round-trip, `play(...)`/`undo(...)`), `MoveGenerator` (pseudo-legal/legal generation, attack and pin detection), `MoveList` (compact encoded `short` moves), `Fen`, `SAN`, and `Setup` (standard, Chess960, and random starts). Note what is absent: there is no separate `bitboard` package, because the bitboard-backed code *is* the core. `chess.debug` holds the perft runners. Plain perft lives in `Perft`, the stored-truth regression suite in `PerftSuite`. The optional GPU path expands frontier positions on the CPU through `SplitPerft`, then hands the heavy node counting to a native backend. `chess.uci` is the external-engine protocol layer. It drives Stockfish/LC0 for `engine analyze`, `engine bestmove`, `engine threats`, `engine compare`, MultiPV, and the batch commands, and it hosts the Filter DSL that gates puzzle mining. `chess.engine` contains the alpha-beta search behind the default classical/NNUE `engine builtin` path and Workbench Play, plus the MCTS search used by LC0/OTIS and the minimal UCI loop. `chess.eval` is the in-process evaluator facade for `engine eval` and `engine static`. `chess.nn` holds the neural-network evaluators: an LC0 CNN forward pass, the simplified BT4 attention path, the OTIS policy/WDL evaluator, and the T5 text models behind `fen text` and `puzzle text`. Each backend ships a pure-Java path plus optional CUDA/ROCm/oneAPI acceleration that falls back to the CPU when the native library is missing. `chess.puzzle`, `chess.tag`, `chess.io`, `chess.book`, `chess.pdf`, and `chess.images` cover puzzle mining, deterministic tagging, record/dataset export, native PDF publishing (no LaTeX), and board rendering. The networks are usable evaluators, not bit-exact reimplementations. crtk does **not** claim LC0/BT4 parity, and the BT4 path is simplified and experimental. Say so plainly in code comments and docs; an honest caveat beats a misleading benchmark. Building with `javac --release 17` The canonical build is whatever `install.sh` does, which is two commands: compile every source under `src/`, then package a runnable jar. Then run it: You can also run without packaging, straight from the classes directory: The `--release 17` gotcha (do not skip this) **Always compile with `--release 17`, never with a bare JDK 21 default.** Build against a newer release and you will get errors that point at nothing real \u2014 the MCTS session code is a repeat offender \u2014 because the compiler is resolving against a newer class library and language level than the source was written for. `--release 17` pins both the language level and the API surface to what the project targets and what CI uses. So when the editor lights up red, run a clean command-line `javac --release 17` and believe it over the IDE. IDE markers also go stale mid-edit. When in doubt, compile from scratch instead of trusting a single inline complaint. Running the regression suite `scripts/run_regression_suite.sh` compiles the sources and runs the `src/testing/` checks. There is no test framework: each test is an ordinary class with its own `main(...)`, invoked as `java -cp out testing.<Name>`. That keeps the dependency count at zero and makes any failing test runnable on its own. Available suite selectors: A few environment overrides are worth knowing: `CRTK_PERFT_THREADS`, `CRTK_PERFT_DEPTH`, `CRTK_PERFT_SUITE_DEPTH`, and `CRTK_REQUIRE_STOCKFISH` (set it to `1` when you want a missing Stockfish to fail the run rather than quietly skip). For a correctness check that needs no script at all, run the built-in perft regression directly: It checks stored node counts against the shared core move generator \u2014 the fastest way to know a movegen change did not quietly break something. Regenerating the docs site and manual The Markdown under `wiki/` is the source of truth. The HTML under `docs/` is generated from it, so edits there get overwritten \u2014 change the Markdown. Static site This reads `wiki/*.md` and writes the static site into `docs/`: a top nav, a landing hero, a left sidebar with per-page tables of contents (built from the `##`/`###` headings), a search index (`docs/search-index.js`), and a light/dark theme toggle. Page order and grouping come from the `NAVIGATION` table near the top of the script. Anything not listed there still ships \u2014 it lands in a trailing reference group rather than vanishing without a trace. Manual PDF This reuses `build_docs_site` to assemble one continuous `docs/manual.html`, then renders `docs/chessrtk-manual.pdf` through a headless Chromium/Chrome (`--headless --print-to-pdf`). It probes `PATH` for `chromium`, `chromium-browser`, `google-chrome`, `google-chrome-stable`, or `chrome`; if the PDF step fails, the usual cause is that none of those is installed. Run `build_docs_site.py` \u2014 or this script, which calls it \u2014 after any `wiki/` edit so the site and manual stay in step. Regenerating diagrams The architecture diagrams are Graphviz sources in `assets/diagrams/*.dot`, each with its rendered `*.png` checked in alongside it. They are all `digraph`s, so `dot` renders them: Regenerate every PNG at once: Reach for `neato` only if you write a graph that wants a spring or radial layout; everything currently checked in is a directed-graph layout, so `dot` is the right tool. Reference a render"
  },
  {
    "title": "Releasing",
    "href": "releasing.html",
    "category": "Project",
    "text": "Releasing ChessRTK A release is one runnable jar, a thin launcher, and \u2014 if you want the GPU \u2014 a native library beside it. There is **no Maven or Gradle**: `javac --release 17`, `jar`, and CMake produce every artifact, full stop. That austerity is the point. The same sources through the same three tools yield the same `crtk` binary, which is what lets the packager ship checksums and mean them. This page is for cutting a release. If you are setting up a development machine instead, start with [Build and Install](build-and-install.md); for the end-user install story, see [Getting Started](getting-started.md). What ships in a release A release is a self-contained directory, tarred and gzipped. The Linux CUDA packager stages this layout: When `lib/liblc0_cuda.so` is present, the launcher adds `-Djava.library.path=lib` on its own, so an extracted release finds the GPU without any further wiring. Model weights stay out by default: they are large, and they live in a separate model repository. One honest note on the networks. The bundled evaluators (NNUE, LC0 CNN, OTIS, T5) are usable, not bit-exact reimplementations of their upstream engines. BT4 in particular is simplified and experimental. No release claims LC0 or BT4 parity, and you should not read one into it. Versioning The `version` command is the single source of truth. Release checks and scripts read it, never a constant pasted somewhere else: For CI and release tooling, ask for JSON. It also reports the runtime it is running on: `crtk version` accepts these flags: Artifacts are tagged `vX.Y.Z`. The packager takes the version as its own argument (see below); omit it and it falls back to `git describe --tags --always`, then to a UTC date stamp outside a checkout. The one rule when cutting a release: the git tag, the bundle filename, and `crtk version` must agree. A mismatch there is the bug nobody notices until a user reports it. The Java constant behind `crtk version`, the text/JSON command output, and the repository metadata in `package.json` are checked together by `CLICommandRegressionTest`, so release metadata drift fails in the normal CLI suite instead of waiting for a packaging run. Build the runnable jar Everything else hangs off the jar, so build it through the regression suite \u2014 the same path CI takes, which is the point of building it that way locally: That compiles every source with `javac --release 17`, packages `crtk.jar` under main class `application.Main`, and runs a smoke check (`crtk --help`, plus `workbench`/`gui` help). The two underlying commands, if you ever want them by hand: `--release 17` is not optional. It pins both the bytecode target and the standard-library surface the compiler will admit, which is what makes the build reproducible across JDK versions \u2014 you can compile on a newer JDK and still ship an artifact that only depends on 17. Build the Linux self-contained runtime bundle For a CPU-only Linux bundle that does not require a system JDK/JRE at runtime, use the jlink/jpackage script. It still compiles with `javac --release 17` and packages `crtk.jar` first; the bundled runtime is only a release artifact layered on top of that jar. Prerequisites are the stock JDK packaging tools: `javac`, `jar`, `jdeps`, `jlink`, and `jpackage`. The script derives the Java module set from `crtk.jar`, adds TLS/locale support, writes a jpackage app image, and adds a root `./crtk` launcher that changes into the bundle directory before dispatching. That keeps the relative `config/`, `wiki/`, and `models/README.md` paths consistent with the normal checkout launcher. Outputs land under `dist/`: The script smoke-tests the produced root launcher with an empty `PATH`: If that prints the expected version, the app is using the bundled runtime rather than a `java` binary from the host. This is still Linux-only groundwork; Windows/macOS launchers and installers remain separate release work. Build the Linux (x86_64) + CUDA release artifact One script does the whole pass: `scripts/make_release_linux_cuda.sh` builds the jar, builds the CUDA JNI library, stages the bundle, and writes the tarball and checksums. Prerequisites: A Java 17+ JDK on `PATH` (`javac`, `jar`) CMake 3.18+ (for CUDA language support) CUDA toolkit (`nvcc`) An NVIDIA driver at runtime to actually use the GPU Build and package a tagged release: To fold local model weights into the bundle (gitignored, and absent from a normal release): Drop `--version` and it derives one from `git describe --tags --always`. Miss any of `javac`, `jar`, `cmake`, or `nvcc` and it fails early and says which \u2014 the packager will not quietly produce a CPU-only bundle. A release labeled CUDA should contain CUDA. The graceful CPU fallback belongs to `install.sh`, not here. Outputs land under `dist/`: Smoke-test the artifact From the extracted directory, three commands tell you the launcher resolved and the GPU path is live: `crtk engine gpu` lists the native backends it found and says whether the CUDA library actually loaded; if it did not, crtk falls back to the CPU path without complaint. For what the accelerated commands look like (`engine perft --gpu`, OTIS evaluation), see [GPU Backends](gpu.md) and the [Command Reference](command-reference.md). Other native backends The packager only ships CUDA, but two more backends exist: ROCm for AMD and oneAPI for Intel, under `native/rocm/` and `native/oneapi/`. Those are `install.sh`'s job (see below), not the packager's. All three are optional, and the design assumes none of them. With no native library loaded, every command still runs on the CPU. What the backends buy you is speed on `engine perft` / `engine perft-suite` and OTIS evaluation \u2014 and they all defer to the same in-process chess core, so they accelerate the work without ever disagreeing about the answer. Pre-release checklist Before you tag, run the full suite once. It builds, lints, and exercises core movegen, the CLI, engines, UCI, book/PDF publishing, perft, and a jar smoke test: If the release touched move generation or notation, go deeper on perft \u2014 that is where off-by-one bugs hide: You can also run the suites one at a time: `build`, `lint`, `core`, `cli`, `engine`, `uci`, `book`, `docs`, `perft-smoke`, and `jar`. The `ci` suite adds a full docs-site rebuild on top. Then walk the checklist: Confirm a clean working tree: `git status --short`. Run `./scripts/run_regression_suite.sh recommended` and ensure it passes. Update docs that changed with the release \u2014 `README.md`, [Command Reference](command-reference.md), [Build and Install](build-and-install.md), and any new-feature pages. Confirm `crtk version` reports the version you intend to tag. Verify the release will bundle `LICENSE.txt` (the packager always copies it). GitHub release workflow Complete the pre-release checklist above. Tag and push the version: `git tag -a v1.0.0 -m \"v1.0.0\"` then `git push origin v1.0.0`. Build the artifact: `scripts/make_release_linux_cuda.sh --version v1.0.0`. Create a GitHub Release for tag `v1.0.0` and attach `dist/crtk-v1.0.0-linux-x86_64-cuda.tar.gz` and `dist/SHA256SUMS`. Install, update, and uninstall scripts Most users run crtk straight from a checkout via the repo's helper scripts. [Build and Install](build-and-install.md) documents them in full; what follows is the maintainer's view \u2014 exactly what each script does, and what it refuses to do. `install.sh` `./install.sh` builds crtk from the current checkout and wires it into the system. It: Checks for Java 17+ and `javac`, offering to install OpenJDK 17 if missing. Optionally installs Stockfish for UCI-backed analysis. Optionally downloads model weights into `models/` (prompted, or forced with `--models` / skipped with `--no-models`). Compiles sources with `javac --release 17` and packages `crtk.jar`. Builds optional native backends and installs a launcher at `/usr/local/bin/crtk` plus a \"ChessRTK Workbench\" desktop entry. Backend selection flags: `--amd` and `--intel` are aliases for `--rocm` and `--"
  },
  {
    "title": "Glossary",
    "href": "glossary.html",
    "category": "Project",
    "text": "Glossary Chess research has its own dense vocabulary, and crtk borrows freely from all of it: the notations players use to write moves, the algorithms engines use to choose them, the network pieces that score positions, and a handful of formats crtk invented for itself. Because everything sits on one shared core, a term means the same thing at the CLI, in a batch script, and in the Workbench. The definitions below are deliberately terse. When one term deserves a chapter, the cross-link points to it. Terms Related pages [Command Reference](command-reference.md) \u2014 every area and action in full, including the `fen` and `move` tools for FEN, SAN, UCI, and Chess960 [In-house Engine](in-house-engine.md) \u2014 where MCTS, the evaluators, WDL, and MultiPV come together [Puzzle Mining](mining.md) \u2014 the Filter DSL and gating, applied rather than defined [Datasets](datasets.md) \u2014 the record format and what you can export it to"
  },
  {
    "title": "Support",
    "href": "support.html",
    "category": "Project",
    "text": "Support When crtk misbehaves, the good news is that it almost always misbehaves the same way twice. Determinism means a problem reproduces from one command plus its input, so a report that hands a maintainer that command rarely needs a follow-up. The self-checks below resolve the usual suspects \u2014 runtime version, config, protocol TOML, engine path, movegen \u2014 on their own, which is why they come before the issue tracker. Start Here: Self-Diagnose Start here, because most of what looks like a bug is an environment or configuration problem these three commands will name for you. `crtk doctor` inspects the runtime, configuration, engine protocol files, external engine availability, and local artifacts. By default it tolerates warnings; `--strict` turns them into a non-zero exit, which is what you want in CI: For bug reports, automation, or the Workbench setup card, prefer `crtk doctor --json` and `crtk config show --json`. They return stable `crtk.doctor.v1` and `crtk.config.v1` objects instead of requiring text scraping. No `crtk` launcher on your PATH? Replace `crtk ...` with `java -jar crtk.jar ...` (or `java -cp out application.Main ...`). Always tell us which form you used. Run the Regression Suite Run the recommended suite from the repository root before you report anything. A clean pass narrows the search: your build and core are sound, so whatever you hit lives somewhere more specific. A failure here is the opposite of a dead end \u2014 it is the single most useful thing you can put in a report. The suite compiles with `javac --release 17`, lints, and exercises the core, CLI, and engine paths. When you already know roughly where the trouble is, a narrower target is faster: Targeted Self-Checks Match the check to the symptom. Each command is deterministic, so its output drops straight into a bug report. External UCI engine problems When `engine analyze`, `engine bestmove`, `engine compare`, `engine threats`, or `puzzle mine` can't get a word out of Stockfish or LC0, the protocol TOML is the usual culprit. Smoke-test it: Move generation, notation, or perft problems Everything else stands on movegen, so if a move looks wrong, rule out the foundation first: Native GPU backend problems A GPU run that quietly falls back to the CPU is the common failure here. Confirm the JNI status, then compare the two paths so you can see whether the native CUDA/ROCm/oneAPI backend actually engaged: Neural-network and evaluator problems For evaluator selection, weights, or model-path trouble, drive the backends directly. One expectation to set before you report a discrepancy: crtk's networks are workable evaluators, not bit-exact reproductions of LC0/BT4. The BT4 path is simplified and still experimental, so \"the numbers don't match upstream LC0\" is the design, not a defect. What to Include in a Bug Report The fastest report is the one a maintainer can reproduce in a single command. Aim for that. Include: The **exact command line** you ran, verbatim (with the area and action, e.g. `crtk engine bestmove ...`). The **full input**: the complete FEN string, a PGN snippet, or the shape of the input file (`--input` / `--stdin`). The **expected behavior** versus the **actual output**, including the full stack trace (re-run with `--verbose` to capture it). Your **Java version** from `java -version`. Your **operating system** and version. Which **invocation form** you used: the `crtk` launcher, `java -jar crtk.jar`, or `java -cp out application.Main`. For UCI engine issues: the **engine path and protocol TOML** contents (`--protocol-path`), plus output from `crtk engine uci-smoke --verbose`. Whether **`./scripts/run_regression_suite.sh recommended` passes** on your checkout. For GPU issues: the output of `crtk engine gpu --verbose`. Gathering this once beats trading questions for a week. Where to File It Open issues on the project's GitHub issue tracker. Search first \u2014 perft mismatches, engine-protocol questions, and known operational quirks are often already tracked or documented, and the answer may be sitting in a closed thread. ChessRTK is GPL-3.0-only. Security and Privacy Notes crtk runs locally and deterministically, so a good report rarely needs anything sensitive. Before pasting into a public issue: Do not share private engine binary paths, proprietary model/weights locations, private dataset paths, or unpublished book manuscripts unless they are genuinely needed and safe to share. Replace private paths with equivalent local examples (for example, a public Stockfish path or a sample FEN) wherever possible. Where to Look Next Before filing, check whether the question already has a home:"
  },
  {
    "title": "Wiki Index",
    "href": "wiki-index.html",
    "category": "Project",
    "text": "ChessRTK Wiki Everything in ChessRTK (\"crtk\") runs through one chess core. A single legal move generator, one make/undo, one attack detector, one FEN/SAN/UCI/Chess960 codepath \u2014 and every command above it inherits the same answers. That is what makes the noun-verb CLI (`move list`, `engine bestmove`, `puzzle mine`) agree with the search, the tagger, the datasets, and the GUI down to the last bit. On top of the core sit external UCI analysis (Stockfish, LC0), built-in alpha-beta/MCTS search with a forced-mate prover, classical/NNUE/LC0/OTIS evaluators, position and tactic tagging, optional T5 summaries, puzzle mining driven by a Filter DSL, ML dataset export, native PDF publishing with no LaTeX, optional GPU backends for perft and OTIS, and a Swing desktop Workbench. This page is the index. Pick a section and follow the one-line descriptions to the page that goes deeper. Pages document what the code actually does. Licensed GPL-3.0-only. Other Reading Formats The same content reaches you three ways, depending on whether you want to browse, read offline, or check the source of truth: **Website**: this wiki with sidebar navigation and a wider reading layout at `https://LenniAConrad.github.io/chess-rtk/`. **PDF manual**: one offline handbook, produced by ChessRTK's own PDF pipeline \u2014 the publishing code eating its own cooking. **Built-in help**: `crtk help`, `crtk help <area>`, or `crtk help <area> <action>`. When the prose and the binary disagree, the binary wins. If the `crtk` launcher is not installed, replace `crtk <command> ...` with `java -cp out application.Main <command> ...` after building. See [Build and install](build-and-install.md). Start Here [Getting started](getting-started.md) - build, install, verify, and run your first commands. [Build and install](build-and-install.md) - compile with `javac --release 17` (no Maven or Gradle) and set up the launcher. [Configuration](configuration.md) - config files, engine protocol TOML, model and weights paths. [FAQ](faq.md) - short, practical answers for new users. [Use cases](use-cases.md) - workflow paths grouped by role and job. [Troubleshooting](troubleshooting.md) - common setup and runtime failures with fixes. [Support](support.md) - where to get help and how to report issues. Commands [Command reference](command-reference.md) - the full grouped CLI surface, areas, actions, and options. [CLI command guide](cli-command-guide.md) - contributor guide for adding commands, flags, help, docs, and regression tests. [Command cheatsheet](command-cheatsheet.md) - common tasks mapped to exact commands. [Example commands](example-commands.md) - copy-pasteable command recipes. [Local JSON-RPC daemon](serve.md) - localhost-only command dispatch for agents and editors with `crtk serve`. [Glossary](glossary.md) - defined terms used across the toolkit and wiki. Workbench [Desktop workbench](workbench.md) - the native Swing GUI: board, play-vs-engine, command forms, batch jobs, datasets, logs, publishing previews, puzzles, and neural-net visualizers. Launch it with `workbench` (alias `gui`). [Workbench design guide](workbench-design-guide.md) - visual direction, layout rules, shared UI primitives, copy, accessibility, and verification for Workbench contributors. Engines & Models [Running engines](in-house-engine.md) - the built-in engine (`engine builtin` / `engine java`) and the forced-mate prover (`engine mate`). [LC0 and the Java evaluator](lc0.md) - the LC0 UCI engine path plus the in-process LC0 CNN evaluator. Networks are usable evaluators, not bit-exact LC0/BT4 reproductions. [GPU backends](gpu.md) - native CUDA, ROCm, and oneAPI backends for perft and OTIS, with automatic CPU fallback (`engine gpu`, `engine perft --gpu`). Workflows [Mining puzzles](mining.md) - generate and verify puzzles with `puzzle mine`, writing `*.puzzles.json` / `*.nonpuzzles.json`. [Filter DSL](filter-dsl.md) - the gating language that selects records and decides what counts as a puzzle. [Piece and position tags](piece-tags.md) - deterministic position, tactic, and theme tagging via `fen tags` and `puzzle tags`. [Tag reference](tag-reference.md) - the catalog of tag names and their meanings. [Datasets](datasets.md) - ML dataset export with `record dataset npy|lc0|classifier` and `record export ...-jsonl`. [Review to study](review-to-study.md) - review PGN games into schema-pinned review rows, study units, and Record handoff files. [T5 tag-to-text pipeline](t5.md) - natural-language position and puzzle summaries with `fen text` and `puzzle text`. [Book publishing](book-publishing.md) - native PDF books, covers, collections, and studies (`book render`, `book cover`, `book collection`, `book study`, `book pdf`) - no LaTeX. [AI agents and automation](ai-agents.md) - deterministic, scriptable commands for agents and batch pipelines (`batch run`, `config validate`, `doctor`). Project [Architecture](architecture.md) - the one-shared-core design and how it powers the CLI, search, tagging, datasets, rendering, and GUI. [Quality and testing](quality-and-testing.md) - regression suites, perft validation, and determinism checks. [Outputs and logs](outputs-and-logs.md) - where artifacts land and how session cache/logs work (`clean`). [Development notes](development-notes.md) - internals, conventions, and contributor notes. [CLI command guide](cli-command-guide.md) - exact implementation workflow for new CLI commands. [Releasing](releasing.md) - how releases are cut and versioned. Command Areas At A Glance Quick Verification Before trusting anything else, prove the core is sound. Move generation underpins every command, so a clean regression run plus a couple of spot checks tells you the foundation holds: When you want more than a smoke test: See [Quality and testing](quality-and-testing.md) for the full suite. Project Policy Notes Source and shell scripts are tracked; everything generated is not. Analysis dumps, model weights, PDFs, and build output are local artifacts and stay out of git. UCI engines and model weights are optional. FEN, move, perft, Workbench Play, and the classical built-in search all run in-process, so a fresh checkout does useful work before you download a single weight. The neural-network paths are honest evaluators, not impersonations: no claim of bit-exact LC0 or BT4 parity, and the BT4 path remains simplified and experimental. Determinism and the single shared core are design goals, not accidents. One legality-and-notation implementation answers for the CLI, search, tagging, datasets, rendering, and GUI alike, which is why a result reproduces wherever you ask for it."
  },
  {
    "title": "Engine Strength Tuning \u2014 What Worked, What Didn't",
    "href": "engine-strength-tuning.html",
    "category": "Reference",
    "text": "Engine Strength Tuning \u2014 What Worked, What Didn't A running log of attempts to make the in-house `AlphaBeta` engine stronger, with the measured outcome of each. The point is to **not relearn the same lessons**: which techniques paid off, which didn't, and \u2014 more importantly \u2014 the *methodology and gotchas* that decide whether a measurement can be trusted at all. The one rule that produced everything below: **measure with self-play, never assert.** Several textbook techniques that \"should\" help measured neutral or negative here; several that looked neutral at first were just under-sampled. Ship a change only when a gauntlet says it's non-negative. How to measure (the harnesses) **`src/testing/SelfPlayGauntlet.java`** \u2014 in-process A/B. Plays candidate vs baseline from a set of openings, **each opening from both colors** (so identical engines score exactly 50.0% \u2014 verified), at an **equal fixed budget** (`--nodes N` or `--movetime MS`). Prints W-D-L + a point Elo. Flags: `--a/--b CSV|all|none` (feature sets), `--threadsA/--threadsB`, `--workers N` (parallel opening-pair workers), `--eval classical|nnue`, `--openings N` (seeded reproducible generation), `--seed S`. **`src/testing/NnueBench.java`** \u2014 NNUE correctness + speed. Prints a **golden** centipawn vector (must be byte-identical across builds \u2192 no eval change), a **parity** walk (incremental search-state eval == full eval at every make/undo node), a **concurrent parity** (8 threads, proves thread-safety), and an **nps** benchmark. **`AlphaBeta.Feature`** enum + the 3-arg constructor let each technique be toggled independently, so the gauntlet can A/B one change in isolation, in one process. Build and run from a **private output dir** (see the gotcha about `out/` below), e.g.: What worked (shipped) The shipped alpha-beta default feature set is `{SEE_PRUNING, SEARCH_REPETITION, LMR_TABLE, IMPROVING, CONT_HISTORY, CHECK_EXTENSION, IIR}`. CLI searches stay single-threaded by default for reproducibility, but `engine builtin --threads N` opts into Lazy SMP; `engine builtin --max-strength` selects the time-bound alpha-beta profile and automatic Lazy SMP thread count used by the Workbench Max setting. Features that measured neutral or negative remain off by default behind `AlphaBeta.Feature`. What did NOT work (rejected \u2014 kept as off-by-default toggles) These remain in the code behind `AlphaBeta.Feature` so they can be re-measured (e.g. after a future depth increase) without reimplementing them. The lessons that will save the most time **Sample size dominates.** A 48-game gauntlet has roughly **\u00b1100 Elo** of noise \u2014 enough to flip a true +73 to \"+22\" or a true 0 to \"\u221229.\" Several techniques read \"neutral\" or \"negative\" at 48 games and then settled at a different value by 300. **Use \u2265300 games before believing a ship/no-ship call**, and confirm a marginal result with a second seed. **Depth-scaling techniques are invisible at shallow depth.** LMR, check extensions, history, singular extensions, etc. pay off *more as search deepens*. This engine searches shallowly (NNUE ~12\u201328k nps; classical reaches ~depth 6\u201310 at a few-thousand-node budget), so they read neutral/negative on a shallow gauntlet. They flipped clearly positive at 20\u201330k nodes. **Don't reject a depth-scaling heuristic on a shallow measurement \u2014 re-test it deep**, and gate it to the deep (Max) regime if it hurts when shallow. **Lazy SMP must diversify.** N identical deterministic threads \u2248 one search + overhead = *negative*. Helpers need a depth-skip (or move-order) scheme so they explore different parts of the tree. **Measure SMP at fixed *time*, not fixed nodes** (more threads = more nodes per unit time is the entire point). **The NNUE \"incremental accumulator\" can be dead code.** For BIG nets it returned `null`, so the AB path fell back to a full forward pass per leaf \u2014 the real reason NNUE was slow. **Before optimizing NNUE, confirm which path actually runs.** An output-preserving speedup (verified bit-identical via the parity walk) needs *no* Elo measurement \u2014 more nps at identical eval is a pure win (deeper search in the same time). **Elo is not additive.** Per-change gains were each measured against the *previous* config under different conditions; you can't sum them. For a real total, run the **full new config vs the original engine head-to-head** at fixed time. Environment / tooling gotchas **The IDE clobbers `out/`.** The VS Code `redhat.java` (Eclipse JDT) language server auto-builds the project on every file edit and intermittently rewrites/empties `out/`, causing flaky `ClassNotFoundException` mid-run. **Compile and run from a private dir** (`/tmp/crtk-\u2026`) the IDE doesn't manage. (The running workbench GUI from `crtk.jar` can also hold the tree.) **Use game-level workers for fixed-node gauntlets.** `SelfPlayGauntlet --workers N` runs independent opening pairs concurrently and collects them in opening order, so fixed-node results stay deterministic while using more cores. Keep `--threadsA 1 --threadsB 1` for these screens unless you are explicitly measuring Lazy SMP. **Don't parallelize fixed-time gauntlets casually.** Multiple workers contend for cores and skew wall-clock budgets. For Lazy SMP strength tests, prefer `--movetime` with `--threadsA/--threadsB` and `--workers 1`. **Build with `javac --release 17`.** Bare JDK-21 emits false errors; the project targets 17. The Java Vector API (`jdk.incubator.vector`) is **not** available under the stock `--release 17` launch, so SIMD NNUE is out without a build/launch change. **Allocation cleanups are not Elo claims.** Reusing a scratch `Position.State` for checking-move probes and per-ply move-order score rows removes hot-path allocation, but fixed-node tests cannot show speed gains directly. Verify output stability (for example same-engine fixed-node self-play at 50.0%) and use longer fixed-time gauntlets before calling it Elo. **Determinism is a feature here.** Anything non-deterministic (Lazy SMP, sampling) is gated so the CLI, tests, and lower Play strengths stay reproducible. Open levers (not yet attempted \u2014 risk noted) **Incremental NNUE *threats*** \u2014 threats ray-walk sliders and depend non-locally on occupancy, so a per-move delta risks silently changing eval. Only pursue behind the bit-exact parity harness; treat any divergence as a bug, not a tradeoff. Estimated *modest* (the threat rebuild is ~40% of the per-node cost). **Broaden SMP / deep-heuristics below the Max tier** \u2014 a calibration tradeoff (it strengthens settings beyond their nominal Elo); make it an explicit Play \"use all cores\" option rather than silently shifting the slider's meaning. **Re-test the shelved heuristics now that search is deeper** (faster NNUE + SMP) \u2014 the depth-scaling lesson predicts some may now help."
  },
  {
    "title": "Local JSON-RPC Daemon",
    "href": "serve.html",
    "category": "Reference",
    "text": "Local JSON-RPC Daemon `crtk serve` starts a localhost-only HTTP wrapper around the existing command dispatcher. It is for agents, editors, and scripts that need many small `crtk` calls without paying JVM startup every time. It is not an online service, not a play server, and not a social or matchmaking endpoint. Start It The daemon binds `127.0.0.1` by default and refuses non-loopback addresses. Use `--port 0` when embedding or testing and let the OS choose an ephemeral port. Endpoints Unknown paths return 404. There are no game-play, matchmaking, account, network-play, or remote-analysis endpoints. JSON-RPC Run Send `method: \"run\"` with `params.argv` as the exact CLI argv tokens after `crtk`. The response keeps stdout and stderr separate and reports the CLI exit code. Commands still run through the normal `application.Main` path, so JSON shapes, exit codes, and diagnostics match the CLI. Because stdout and stderr are process-wide streams, the daemon serializes requests through one dispatcher thread. Discovery Fetch the catalog instead of scraping help text: That response is the same `crtk.cli.catalog.v1` object emitted by: Python SDK The Python client is generated from the command catalog. Regenerate it after a command is added, removed, or renamed: The default output is `sdk/python/crtk_client.py`. The generator can also read a saved catalog, which is useful for tests and release checks: Use the client against a running daemon: Safety Boundaries The server binds only loopback addresses. The daemon is off by default and exists only when `crtk serve` is running. Engine work is still bounded by the command flags you pass, such as `--max-nodes`, `--max-duration`, and `--multipv`. Nested `serve` calls are rejected through the daemon to avoid blocking the dispatcher with another long-lived server."
  }
];
