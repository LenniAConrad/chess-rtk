window.CRTK_SEARCH_INDEX = [
  {
    "title": "Home",
    "href": "index.html",
    "category": "Start Here",
    "text": "ChessRTK (`crtk`) is a Java 17 chess research toolkit for deterministic chess workflows: FEN and SAN handling, legal move generation, perft validation, engine analysis, puzzle mining, dataset export, board rendering, and native PDF book publishing. The project is organized around one shared position model. That means the same rules implementation drives command-line move lists, built-in search, UCI analysis, tags, datasets, diagrams, GUI views, and book output. Pick Your Path Copy-Paste Smoke Test If the launcher is not installed, use: Documentation Surfaces What ChessRTK Covers Most Common Workflows Study One Position Validate The Core Mine And Export Puzzles Architecture At A Glance ![ChessRTK position toolbox](../assets/diagrams/crtk-position-toolbox.png) The main design rule is simple: avoid separate chess implementations for separate tools. FEN parsing, SAN conversion, legality, make/undo, perft, search, rendering, tagging, and export paths all meet at the same Java core. Core Documentation Website Version GitHub Wiki pages use GitHub's built-in Markdown renderer. For the website-style version with the custom sidebar, page table of contents, and generated HTML layout, use: For a single printable document, use: Support Start with [Troubleshooting](troubleshooting). If you are reporting a bug, include the exact command, input FEN or file, Java version, operating system, engine protocol TOML if relevant, and whether this passes:"
  },
  {
    "title": "Getting Started",
    "href": "getting-started.html",
    "category": "Start Here",
    "text": "Getting Started This page gets a fresh checkout from source code to useful commands. Requirements Java 17+ JDK with `javac` Optional: `stockfish` or another UCI engine on `PATH` Optional: local model files under `models/` for NNUE, LC0, and T5 workflows Core FEN, move, perft, and classical built-in search commands do not require an external chess engine. Build From Source Install The Launcher If you do not install the launcher, use: Verify The Checkout Recommended broader pass: Learn The Command Shape ChessRTK commands use a noun group plus an action: Examples: Help works at each level: First Position Workflow First Engine Workflow Use the in-process engine when you want no external process: Use a configured UCI engine when strength matters: First Publishing Workflow For full books and covers, continue with [Book publishing](book-publishing.md). Next Pages [Configuration](configuration.md) [Command reference](command-reference.md) [Example commands](example-commands.md) [FAQ](faq.md) [Troubleshooting](troubleshooting.md)"
  },
  {
    "title": "Use Cases",
    "href": "use-cases.html",
    "category": "Start Here",
    "text": "Use Cases ChessRTK is broad, so the easiest way to learn it is to start from the job you want done. I Want To Study One Position Use the position and move primitives first. They run in-process and do not need a configured UCI engine. Then ask an engine: Read next: [Command Cheatsheet](command-cheatsheet) [Configuration](configuration) [In-House Engine](in-house-engine) I Want To Verify Move Generation Use perft and the stored regression suite. These commands validate ChessRTK's Java move generator directly. Read next: [Quality and Testing](quality-and-testing) [Architecture](architecture) [Development Notes](development-notes) I Want To Mine Puzzles Start from PGN or FEN seeds, run bounded UCI analysis, then keep accepted and rejected records for later filtering or training. Read next: [Mining Puzzles](mining) [Filter DSL](filter-dsl) [Outputs and Logs](outputs-and-logs) I Want To Generate Filtered Position Sets Use `gen fens` when you need reusable FEN shards for training, test corpora, or targeted engine checks. Filters combine with AND, so every selected condition must match. Read next: [Command Reference](command-reference) [Example Commands](example-commands) [Datasets](datasets) I Want To Build Training Data Use `record` commands after mining or importing analysis records. Read next: [Datasets](datasets) [Piece and Position Tags](piece-tags) [Filter DSL](filter-dsl) I Want To Publish Diagrams Or Books Use `book pdf` for quick diagram sheets and `book render` / `book cover` for full book projects. Read next: [Book Publishing](book-publishing) [Example Commands](example-commands) I Want Agent-Friendly Chess Primitives Prefer deterministic command shapes with explicit formats and bounded engine work. Read next: [AI Agents and Automation](ai-agents) [Command Cheatsheet](command-cheatsheet) I Want To Work On ChessRTK Itself Run focused checks for your edit area and the recommended suite before pushing. Read next: [Quality and Testing](quality-and-testing) [Architecture](architecture) [Releasing](releasing)"
  },
  {
    "title": "Command Cheatsheet",
    "href": "command-cheatsheet.html",
    "category": "Start Here",
    "text": "Command Cheatsheet This page is the shortest path from \"what do I want?\" to the command to run. For complete options, use `crtk help --full` or open [Command Reference](command-reference). One-Minute Demo Setup And Health FEN And Board Inspection Moves Engine Work Move-Generation Verification Puzzle Mining Datasets Publishing Agent-Friendly Commands"
  },
  {
    "title": "FAQ",
    "href": "faq.html",
    "category": "Start Here",
    "text": "FAQ What is ChessRTK? ChessRTK is a Java 17 command-line toolkit for chess research, automation, engine workflows, puzzle mining, dataset export, rendering, and book publishing. Is this a chess engine? Partly. ChessRTK includes `engine builtin`, a small in-process Java alpha-beta searcher, but the project is broader than an engine. It also provides FEN, move, perft, record, dataset, rendering, GUI, and publishing tools. For strength-sensitive analysis, use an external UCI engine through `engine analyze`, `engine bestmove`, or `puzzle mine`. Do I need Stockfish or LC0? No for core commands. These run in-process: `fen normalize`, `fen validate`, `fen print` `move list`, `move to-san`, `move to-uci`, `move after`, `move play` `engine perft`, `engine perft-suite` `engine static` `engine builtin --classical` You need a UCI engine for UCI-backed analysis, best-move, threat, smoke-test, and mining workflows. Why not use Maven or Gradle? The repo is intentionally build-tool light. A JDK is enough: The regression runner wraps the common local checks. Where do model files go? Put local model files under `models/`. They are intentionally ignored by git. Use `./install.sh --models` for the default model artifacts, or pass explicit paths with options such as `--weights` and `--model`. What is a `.record` file? It is ChessRTK's reusable analysis-record JSON shape. Record commands can filter, merge, summarize, export, and convert those records into PGN, CSV, JSONL, NumPy tensors, LC0-style tensors, classifier tensors, and diagnostics. How do I know move generation is correct? Run: The suite compares stored reference positions against the Java move generator and reports whether every calculated node count matches the expected truth. What should I run before opening a PR or publishing a change? If you touched move generation, FEN, SAN, Chess960, or make/undo logic, also run a deeper perft smoke pass: Where should generated files go? Use local artifact directories such as `dump/`, `data/`, `dist/`, `models/`, and `out/`. Do not commit generated PDFs, model weights, build output, or large analysis dumps unless a specific small fixture is required by a regression test."
  },
  {
    "title": "Command Reference",
    "href": "command-reference.html",
    "category": "Start Here",
    "text": "Command reference All commands are subcommands of `application.Main`. Installed launcher: `crtk <area> <action> [options] [args]` From classes: `java -cp out application.Main <area> <action> [options] [args]` Built-in usage text: `crtk help --full` Use grouped commands for scripts and automation: `record`, `fen`, `move`, `engine`, `book`, and `puzzle`. Command style: Prefer the full grouped form in scripts: `crtk record export ...`, `crtk record dataset ...`, `crtk move list ...`, `crtk engine bestmove ...`. Use named flags for structured values such as `--fen`, `--input`, `--output`, and `--format`. Put options before free-form args when scripting. Use `--` or `--end-of-options` if a value could be parsed as an option. Contextual help works with either `crtk help move list` or `crtk move list --help`. The Java-native commands (`fen`, `move`, `engine perft`, `engine perft-suite`, `engine builtin --classical`, and `engine static`) do not require an external engine. External UCI configuration is only needed for UCI-backed analysis, best-move, threat, smoke-test, and mining workflows. Capability Overview `record` Grouped record workflows. This keeps export, dataset, stats, and file-management operations under one entry point. Usage: `crtk record export plain ...`: convert `.record` JSON to `.plain` `crtk record export csv ...`: convert `.record` JSON to CSV `crtk record export pgn ...`: convert `.record` JSON to PGN games `crtk record export puzzle-jsonl ...`: export LC0-policy-aware puzzle JSONL rows `crtk record export training-jsonl ...`: export coarse/fine FEN labels `crtk record dataset npy ...`: export eval-regression tensors `crtk record dataset lc0 ...`: export LC0-style tensors `crtk record dataset classifier ...`: export binary classifier tensors `crtk record files ...`: merge/filter/split record files `crtk record stats ...`: summarize record files `crtk record tag-stats ...`: summarize tag distributions `crtk record analysis-delta ...`: export evaluation stability metrics Use the full `record export ...` and `record dataset ...` forms in scripts. `fen` Grouped FEN and position workflows. Usage: `crtk fen normalize ...`: normalize and validate a FEN `crtk fen validate ...`: validate a FEN `crtk fen after ...`: apply one move and print the resulting FEN `crtk fen line ...`: apply a move line and print the resulting FEN `crtk fen generate ...`: generate random legal FEN shards `crtk fen pgn ...`: convert PGN games to FEN lists `crtk fen chess960 ...`: print Chess960 starting positions `crtk fen print ...`: pretty-print a FEN `crtk fen display ...`: render a board image in a window `crtk fen render ...`: save a board image to disk `crtk fen tags ...`: generate tags for FENs, PGNs, or variations `crtk fen text ...`: summarize position tags with T5 `move` Grouped move-listing, notation, and application workflows. Usage: `crtk move list ...`: list legal moves for a FEN `crtk move uci ...`: list legal moves in UCI `crtk move san ...`: list legal moves in SAN `crtk move both ...`: list legal moves in UCI and SAN `crtk move to-san ...`: convert one UCI move to SAN `crtk move to-uci ...`: convert one SAN move to UCI `crtk move after ...`: apply one move and print the resulting FEN `crtk move play ...`: apply a move line and print the resulting FEN `engine` Grouped engine, evaluator, and move-generation workflows. Usage: `crtk engine analyze ...`: analyze a FEN with the configured engine `crtk engine bestmove ...`: print the best move for a FEN `crtk engine bestmove-uci ...`: print the best move in UCI `crtk engine bestmove-san ...`: print the best move in SAN `crtk engine bestmove-both ...`: print the best move in UCI and SAN `crtk engine builtin ...`: search with the in-house Java engine `crtk engine java ...`: run the same built-in Java engine `crtk engine threats ...`: analyze opponent threats `crtk engine eval ...`: evaluate a FEN with LC0 or classical heuristics `crtk engine static ...`: evaluate a FEN with the classical backend `crtk engine perft ...`: run perft on a position `crtk engine perft-suite ...`: run the perft regression suite `crtk engine gpu ...`: print GPU JNI backend status `crtk engine uci-smoke ...`: start the engine and run a tiny bounded search `engine builtin` Search a position with ChessRTK's in-house Java engine. `engine java` runs the same implementation. The built-in engine is an in-process search and benchmarking engine. It is useful for deterministic local search, smoke tests, puzzle-solve timing, and automation that should not depend on spawning a UCI engine. It is not intended to be a top-tier engine competing with mature UCI engines. Examples: Options: `--input|-i <path>`: FEN list file (optional) `--fen \"<FEN...>\"`: FEN string (or pass it positionally) `--evaluator <classical|nnue|lc0>`: static evaluator family (default `classical`) `--classical`: shortcut for `--evaluator classical` `--nnue`: shortcut for `--evaluator nnue` `--lc0`: shortcut for `--evaluator lc0` `--weights <path>`: NNUE or LC0 evaluator weights path `--depth|-d <n>`: maximum iterative-deepening depth in plies (default `3`) `--max-nodes|--nodes <n>`: node budget; `0` means unlimited. If omitted, the default is `250000` without `--depth`, and unlimited when `--depth` is explicit. `--max-duration <dur>`: wall-clock budget; `0` means unlimited. If omitted, the default is `5s` without `--depth`, and unlimited when `--depth` is explicit. `--format <uci-info|uci|san|both|summary>`: output format (default `uci-info`) `--verbose|-v`: print stack traces on failure Evaluator notes: `classical`: handcrafted evaluator with no model dependency. `nnue`: pure-Java NNUE evaluator using `models/crtk-halfkp.nnue` by default, or an explicit `--weights` path. If no NNUE weights are available, the command fails with a missing-weights error instead of silently using the smoke-test fallback. `lc0`: pure-Java LC0 value evaluator using the configured LC0J model path by default, or an explicit `--weights` path. Search notes: The engine uses alpha-beta search, not LC0-style MCTS. The search is single-threaded and owns an internal transposition table plus a static-evaluation cache. The table size is fixed in code; `--threads` and `--hash` are UCI-engine options, not built-in-engine options. The default `uci-info` format prints one UCI-style `info depth ... pv ...` line for each completed depth, then a final `bestmove ...` line. If a slow evaluator exhausts the budget before depth 1 completes, `uci-info` prints the static root fallback as `info depth 0 ... pv ...`. Use `--format uci` for best-move-only UCI coordinate output. `--format summary` prints the evaluator, best move, score, completed depth, nodes, elapsed time, stopped flag, and principal variation. `book` Grouped book and diagram PDF workflows. Usage: `crtk book collection ...`: build a dense puzzle-collection book manifest from record JSON/JSONL `crtk book study ...`: render annotated puzzle studies to PDF, cover, and optional normalized TOML `crtk book render ...`: render a chess-book JSON/TOML file to a native PDF `crtk book cover ...`: render a native PDF cover for a chess-book file `crtk book pdf ...`: export chess diagrams to a PDF Compatibility aliases: `book ilovechess` -> `book collection` `book artofchess` / `book art` -> `book study` `puzzle` Grouped puzzle mining, conversion, tag, and text workflows. Usage: `crtk puzzle mine ...`: mine chess puzzles `crtk puzzle pgn ...`: convert mixed puzzle dumps to PGN games `crtk puzzle tags ...`: generate per-move tags for puzzle PVs `crtk puzzle text ...`: run T5 over puzzle PVs `record export plain` Convert a `.record` JSON array into Leela-style `.plain` blocks. Options: `--input|-i <path>`: input `.record` (required) `--output|-o <path>`: output `.plain` (optional; default derived from input) `--filter|-f <dsl>`: Filter DSL to select which records are exported `--sidelines|--export-all|-a`: include sidelines / export additional PVs when present `--csv`: also emit"
  },
  {
    "title": "Example Commands",
    "href": "example-commands.html",
    "category": "Start Here",
    "text": "Example commands Examples assume you installed the launcher (`crtk`). If you run from classes, replace `crtk` with `java -cp out application.Main`. Quick sanity checks `crtk help` \u2014 show all commands + flags. `crtk help --full` \u2014 show subcommands and detailed option groups. `crtk doctor` \u2014 check Java, config, protocol, engine discovery, and local artifact paths. `crtk doctor --strict` \u2014 treat setup warnings as failures. `crtk config validate` \u2014 validate config + protocol file paths. `crtk engine uci-smoke --nodes 1 --max-duration 5s` \u2014 start the configured engine and run a tiny bounded search. `crtk engine gpu` \u2014 check whether the optional GPU JNI backends are usable (CUDA/ROCm/oneAPI). `crtk engine perft-suite --depth 6 --threads 4` \u2014 validate critical stored-truth positions with the Java move generator. `crtk fen print --fen \"rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1\"` \u2014 print the starting position. `crtk config show` \u2014 dump resolved config values. Convert `.record` \u2192 `.plain` / CSV `crtk record export plain -i data/input.record` \u2014 writes `data/input.plain`. `crtk record export plain -i data/input.record --sidelines --csv` \u2014 also writes `data/input.csv`. `crtk record export csv -i data/input.record -o dump/input.csv` \u2014 CSV only. `crtk record export plain -i data/input.record -f \"gate=AND;eval>=300\"` \u2014 export only records matching a Filter DSL. `crtk record files -i dump/ -o dump/merged.json` \u2014 merge many record files into one JSON array. `crtk record files -i dump/ -o dump/merged.json --max-records 100000` \u2014 merge and split into 100k-sized parts. `crtk record files -i dump/mixed.json -o dump/puzzles.json --puzzles` \u2014 keep only puzzle records. Mine puzzles `crtk puzzle mine --random-count 50 --output dump/` \u2014 mine 50 random seeds into timestamped outputs under `dump/`. `crtk puzzle mine --input seeds/fens.txt --output dump/fens.json` \u2014 mine from a `.txt` file; writes `dump/fens.puzzles.json` + `dump/fens.nonpuzzles.json`. `crtk puzzle mine --input games.pgn --output dump/pgn.json --engine-instances 4 --max-duration 60s` \u2014 mine from PGN. `crtk puzzle mine --chess960 --random-count 200 --output dump/` \u2014 Chess960 random mining. `crtk puzzle pgn -i dump/fens.puzzles.json -o dump/fens.pgn` \u2014 convert accepted puzzle records to PGN. `crtk record files -i dump/ -o dump/filtered.json --recursive --puzzles` \u2014 collect puzzle records from a directory tree. Generate random FEN shards `crtk fen generate --output shards/ --files 2 --per-file 20 --chess960-files 1` \u2014 writes 2 shard files (first one Chess960). `crtk gen fens --output endgames/ --files 2 --per-file 500 --endgame --max-material-imbalance 300` \u2014 generate broad queenless endgame seeds with bounded material imbalance. `crtk gen fens --output endgames/ --files 1 --per-file 100 --rook-endgame --rooks 2 --max-material-imbalance 200` \u2014 generate balanced rook endgame seeds. `crtk gen fens --output specials/ --files 1 --per-file 25 --en-passant --max-attempts 250000` \u2014 keep only positions where en passant is legally available. `crtk gen fens --output promotions/ --files 1 --per-file 50 --promotion --capture --max-attempts 1000000` \u2014 keep positions with a legal promotion that also have at least one legal capture. `crtk gen fens --output quiet-black/ --files 1 --per-file 100 --side black --not-in-check --min-pieces 10 --max-pieces 20` \u2014 keep quiet-ish Black-to-move middlegame/endgame positions by piece count. `crtk fen chess960 518` \u2014 print the standard start position by its Chess960 index. `crtk fen chess960 --all --format both > chess960.tsv` \u2014 export `index`, back-rank layout, and FEN for all 960 starts. Export datasets (NumPy) `crtk record dataset npy -i dump/fens.puzzles.json -o training/pytorch/data/puzzles` \u2014 writes `puzzles.features.npy` + `puzzles.labels.npy`. `crtk record dataset lc0 -i dump/fens.puzzles.json -o training/lc0/puzzles --weights models/leela_112planes-10blocksx128-policyhead80-valuehead32-policy4672-wdl3.bin` \u2014 write LC0-style input, policy, value, and metadata tensors. `crtk record dataset classifier -i dump/fens.puzzles.json -i dump/fens.nonpuzzles.json -o training/classifier/fens` \u2014 writes 21-plane classifier inputs + 0/1 labels. `crtk record export training-jsonl -i dump/fens.puzzles.json -i dump/fens.nonpuzzles.json -o training/fens.training.jsonl` \u2014 writes coarse/fine JSONL labels. `crtk record export puzzle-jsonl -i dump/fens.puzzles.json -o training/fens.puzzle.jsonl --weights models/leela_112planes-10blocksx128-policyhead80-valuehead32-policy4672-wdl3.bin --puzzles` \u2014 write JSONL puzzle rows with LC0 policy indices. `crtk record analysis-delta -i dump/fens.puzzles.json -o dump/fens.analysis-delta.jsonl` \u2014 writes evaluation stability metrics. Publish diagrams and books `crtk book collection -i dump/mate1.json -o books/mate1.book.toml --subtitle \"4,000 Mate in 1 Puzzles\" --pdf-output dist/mate1.pdf --cover-output dist/mate1-cover.pdf --binding paperback --interior white-bw` \u2014 build a puzzle-collection manifest from analyzed records and render the matching interior + cover. `crtk book study -i books/puzzle-studies.json --manifest-output books/puzzle-studies.toml -o dist/puzzle-studies.pdf --cover-output dist/puzzle-studies-cover.pdf --binding paperback --interior white-bw` \u2014 render richer annotated puzzle studies and optionally normalize them to TOML. `crtk book pdf --fen \"<FEN>\" -o dump/position.pdf` \u2014 export one diagram to PDF. `crtk book pdf -i seeds.txt -o dump/sheet.pdf --title \"Training Sheet\"` \u2014 export a FEN list to a diagram sheet PDF. `crtk book pdf --pgn games.pgn -o dump/games.pdf --page-size a5 --diagrams-per-row 1` \u2014 export PGN mainlines to PDF. `crtk book render -i books/puzzles.toml --check` \u2014 validate book layout, FENs, and solution lines without writing. `crtk book render -i books/puzzles.toml -o dist/puzzles.pdf` \u2014 render a book manifest to a native vector PDF. `crtk book cover -i books/puzzles.toml --pdf dist/puzzles.pdf --check --binding paperback --interior white-bw` \u2014 verify cover dimensions from the rendered interior PDF before rendering. `crtk book cover -i books/puzzles.toml --pdf dist/puzzles.pdf -o dist/puzzles-cover.pdf --binding paperback --interior white-bw` \u2014 render a matching paperback cover from the interior PDF geometry. Display a position (GUI) `crtk fen display --fen \"r1bqk2r/ppppbppp/3n4/4R3/8/8/PPPP1PPP/RNBQ1BK1 b kq - 2 8\" --special-arrows --arrow e5e1 --legal d6` \u2014 open a window. `crtk fen display --fen \"<FEN>\" --arrow e2e4 --circle e4 --legal g1` \u2014 overlays for quick inspection. `crtk fen display --fen \"<FEN>\" --ablation --show-backend` \u2014 show per-piece ablation (uses LC0 if available; otherwise classical). `crtk fen render --fen \"<FEN>\" -o dump/position.png --arrow e2e4 --circle e4 --size 1200` \u2014 save a board image. `crtk fen render --fen \"<FEN>\" -o dump/position.svg --format svg --flip` \u2014 save a vector board from Black's perspective. `crtk gui-web --fen \"r1bqk2r/ppppbppp/3n4/4R3/8/8/PPPP1PPP/RNBQ1BK1 b kq - 2 8\" --dark` \u2014 launch the chess-web-inspired desktop GUI. `crtk gui-next --fen \"<FEN>\" --dark` \u2014 launch the Studio research workbench. Analyze positions / best move with a UCI engine `crtk engine analyze --fen \"<FEN>\" --max-duration 5s` \u2014 print PV summaries for a single position. `crtk engine analyze -i positions.txt --max-nodes 1000000 --multipv 3 --threads 4 --hash 256` \u2014 analyze a FEN list with bounded UCI engine settings. `crtk engine threats --fen \"<FEN>\" --max-duration 2s` \u2014 analyze opponent threats via a null move (MultiPV). `crtk engine bestmove --fen \"<FEN>\"` \u2014 print the best move (UCI). `crtk engine bestmove --fen \"<FEN>\" --format san` \u2014 print the best move (SAN). `crtk engine bestmove-uci --fen \"<FEN>\"` \u2014 best move (UCI shortcut). `crtk engine bestmove-san --fen \"<FEN>\"` \u2014 best move (SAN shortcut). `crtk engine bestmove-both --fen \"<FEN>\"` \u2014 best move (UCI + SAN). Tags / moves `crtk fen tags --fen \"<FEN>\"` \u2014 emit tags as JSON. `crtk fen tags --fen \"<FEN>\" --analyze --max-duration 2s --multipv 3 --wdl` \u2014 add en"
  },
  {
    "title": "Troubleshooting",
    "href": "troubleshooting.html",
    "category": "Start Here",
    "text": "Troubleshooting Mining fails to start an engine Verify your engine binary exists and is executable (check `path = ...` in your engine protocol TOML). Run `crtk help` and confirm you\u2019re passing `--protocol-path` to the right file. If your engine is on `PATH`, try `which stockfish` / `which lc0`. Commands such as `move list`, `engine perft`, `engine perft-suite`, `engine static`, and `engine builtin --classical` do not start a UCI engine; use them to isolate whether a failure is in the chess core or in external engine configuration. Perft validation fails `engine perft-suite` compares stored truth values against the Java core move generator. It does not call Stockfish or any other external process. Re-run a failing row directly with `engine perft --fen \"<FEN>\" --depth <n> --divide` to inspect per-root-move counts. Recent edits to FEN parsing, Chess960 castling metadata, en-passant handling, SAN move application, make/undo, or attack detection can all affect perft. Filters don\u2019t seem to \u201cstick\u201d If a Filter DSL string is invalid, the CLI logs an error and falls back to the defaults. Prefer editing `config/cli.config.toml` first (then override per-run once you\u2019re happy). Mining is too slow Lower `--max-nodes` / `--max-duration`. Reduce `--engine-instances`. Relax the quality gate (`puzzle-quality`) and/or the accelerate prefilter (`puzzle-accelerate`). `fen display --ablation` is slow or uses the classical backend The evaluator tries to load local `models/leela_112planes-10blocksx128-policyhead80-valuehead32-policy4672-wdl3.bin` weights and falls back to a classical heuristic when LC0 is unavailable. Fetch the default weights with `./install.sh --models`. If you want native GPU acceleration, build the matching backend under `native/cuda/`, `native/rocm/`, or `native/oneapi/` and run with `-Djava.library.path=...`. `engine builtin --nnue` says default weights are missing Run `./install.sh --models` to download `models/crtk-halfkp.nnue` and the bundled LC0 weights. Or pass `--weights /path/to/file.nnue` explicitly. Supported Stockfish NNUE files work here too. Cover dimensions do not match the upload form Pass the rendered interior PDF to `book cover --pdf <interior.pdf>` so trim size and page count come from the actual file. Use `book cover --pages <n>` only when you need to override the PDF-derived spine count. Use a current interior token: `white-bw`, `cream-bw`, `white-standard-color`, or `white-premium-color`. Compare the dimensions printed by `book cover` with the publishing service's own cover calculator before upload. T5 commands cannot find a model Pass `--model /path/to/t5.bin`, or set `t5-model-path` in `config/cli.config.toml`. Model binaries are local artifacts and are ignored by git; they are not bundled with a fresh clone."
  },
  {
    "title": "Build & Install",
    "href": "build-and-install.html",
    "category": "User Guides",
    "text": "Build & install Requirements Java 17+ JDK (needs `javac` to build) Optional: a UCI chess engine either on `PATH` or configured via `config/default.engine.toml` for `engine analyze`, `engine bestmove`, `engine threats`, `engine uci-smoke`, and puzzle mining Optional: local model weights under `models/` for LC0, NNUE, and T5-backed evaluator/text workflows Debian/Ubuntu packages Minimal install (build + run): Install a UCI engine only if you want external-engine analysis or mining: Optional (recommended if you use `./install.sh` and/or build the CUDA JNI backend): Notes: `git` is included so a fresh machine can `git clone` this repo (and `ca-certificates` enables HTTPS downloads). `nvidia-cuda-toolkit` is large and only needed to build the optional CUDA JNI backend under `native/cuda/`. Build (no Maven/Gradle) Focused regression check Run the zero-dependency `Position` regression harness after a build: Local regression runner Use the shared runner to keep local checks aligned with CI: Useful focused suites: Quality checks The VS Code workspace enables SonarLint automatic analysis and excludes local artifact directories such as `data/`, `dump/`, `models/`, `out/`, and `session/`. There is no required SonarQube server for local development. Use these local checks before committing Java changes: If the workflow depends on a configured external UCI engine, also run: For rendering and PDF checks on a headless machine, pass the AWT headless flag: Run the full internal perft validation when changing move generation, FEN validation, SAN, make/undo, or Chess960 setup: The perft suite uses stored truth values and the Java core move generator. It does not launch Stockfish or any other external process. Run Package a runnable JAR (optional) Linux installer (Debian/Ubuntu) `./install.sh` is a convenience installer that: optionally installs OpenJDK 17 and Stockfish via `apt-get` optionally downloads NNUE and LC0 model weights into `models/` compiles sources and builds `crtk.jar` installs a launcher at `/usr/local/bin/crtk` that runs from this repo optionally builds the CUDA JNI backend under `native/cuda/` (if you have the CUDA toolkit) Downloaded `models/*.bin` files are local artifacts and are ignored by git. Fetch optional model weights without prompting: Skip model weight downloads: Skip the CUDA backend build: Force the CUDA backend build (installs missing CUDA build deps on Debian/Ubuntu): Update Update the current git checkout with a fast-forward pull and rerun the installer: Rebuild and reinstall the current checkout without pulling new commits: Notes: `scripts/update.sh` refuses to pull if the git worktree has local changes. Any remaining flags are passed through to `./install.sh`, for example `./scripts/update.sh --no-cuda`. Uninstall Remove the launcher and local build artifacts: Also remove data directories created by the app (`dump/`, `session/`):"
  },
  {
    "title": "Configuration",
    "href": "configuration.html",
    "category": "User Guides",
    "text": "Configuration This repo uses TOML for configuration and keeps defaults in `config/`. Most core commands do not need any configured external program: FEN handling, move listing, SAN/UCI conversion, perft, perft-suite, tagging without `--analyze`, `engine static`, and `engine builtin --classical` all run in-process. CLI config: `config/cli.config.toml` Loaded on startup. If the file is missing, the CLI will create it with built-in defaults (see `src/application/Config.java`). Common keys: `protocol-path`: points to the UCI engine protocol file used by external engine commands (default `config/default.engine.toml`) `lc0-model-path`: default local LC0J `.bin` weights path for evaluator-backed commands/features (default `models/leela_112planes-10blocksx128-policyhead80-valuehead32-policy4672-wdl3.bin`; fetch with `./install.sh --models`) `t5-model-path`: default local T5 `.bin` path used by `fen text` / `puzzle text` when `--model` is omitted (default `models/t5.bin`) `output`: default output root for `puzzle mine` (default `dump/`) `engine-instances`: how many engine processes to run in parallel `max-nodes`: per-position node cap `max-duration`: per-position time cap (ms) `threat-min-cp`: minimum centipawn advantage in null-move analysis to emit a threat tag `threat-equalize-min-cp`: minimum disadvantage before considering equalizing threats `threat-equalize-target-cp`: maximum remaining disadvantage after an equalizing threat `puzzle-quality`, `puzzle-winning`, `puzzle-drawing`, `puzzle-accelerate`: Filter DSL strings for mining Notes: CLI flags override TOML values for a single run. Model binaries are not committed; local `models/*.bin` files are ignored by git. For `puzzle mine --output`, a directory produces timestamped outputs; a file-like root ending in `.json`/`.jsonl` produces `<stem>.puzzles.json` and `<stem>.nonpuzzles.json`. Switching to the included Lc0-tuned defaults The repo ships `config/cli.lc0.config.toml` as an alternative baseline. To use it, copy it over `config/cli.config.toml` (or move/swap the files). External UCI Engine Protocol: `config/*.engine.toml` Engine protocol files describe how to talk to a specific external engine via UCI: how to set options, how to send `position`, and how to issue `go` commands. They are used by `engine analyze`, `engine bestmove`, `engine threats`, `engine uci-smoke`, and puzzle mining. They are not used by `engine perft`, `engine perft-suite`, or the built-in Java search. Shipped files: `config/default.engine.toml`: Stockfish-style UCI defaults (expects `stockfish` on `PATH` unless `path` is changed) `config/lc0.engine.toml`: Lc0-style UCI defaults (expects `lc0` on `PATH` unless `path` is changed) Key fields (examples from `config/default.engine.toml`): `path`: engine executable (name on `PATH` or absolute path) `showUci` / `uciok`: initialization command and response used before readiness checks `setPosition`: template for `position fen %s` `searchNodes`: template for `go nodes %d` `setMultiPivotAmount`: template to set MultiPV (mining expects MultiPV \u2265 2) `setup = [ ... ]`: UCI commands sent after `uci`/`isready` Building engines locally (optional) This repo no longer ships helper scripts for building engines from source. Use your package manager (e.g. `apt-get install stockfish`) or build engines yourself, then point `path` in your `config/*.engine.toml` to the resulting executable. In-Process Evaluator Models The Java evaluators use model files directly and do not start UCI engines: `engine builtin --nnue` loads NNUE weights from `--weights`, or from the default NNUE model path; `./install.sh --models` populates that default. If neither exists, it fails with a missing-weights error. `engine builtin --lc0` loads LC0J `.bin` weights from `--weights`, or from `lc0-model-path`. `engine eval` prefers the Java LC0 evaluator and falls back to classical evaluation unless `--lc0` or `--classical` is specified. `engine static` is always the classical evaluator. LC0 as a UCI engine usually uses `.pb.gz` weights through the engine's own `WeightsFile` option. The Java LC0 evaluator uses ChessRTK's `.bin` model format. See [LC0](lc0.md) for the two workflows. Optional ECO book: `config/book.eco.toml` An ECO dictionary used for opening lookups (tags / printing). When present, the tagging subsystem (used by `tags`, `print`, and by mined records) will add: `eco: <code>` (e.g. `eco: A00`) `opening: <name>` (e.g. `opening: Amar Opening`) If the file is missing, opening tags are simply omitted."
  },
  {
    "title": "In-House Engine",
    "href": "in-house-engine.html",
    "category": "User Guides",
    "text": "In-house Java engine ChessRTK includes a small built-in Java engine exposed through `crtk engine builtin` and `crtk engine java`. The engine is an in-process search and benchmarking tool. It is not intended to be a top-tier engine competing with mature UCI engines. Use it when an in-process, zero-dependency search is valuable, when a workflow should continue without a configured UCI engine, or when you want to benchmark engine tasks such as puzzle solve speed. CLI usage Search one FEN with the default classical evaluator: Cap the search by nodes or time: When `--depth` is explicit and `--nodes`/`--max-duration` are omitted, the search tries to complete that depth without the default node/time caps. Add `--nodes` or `--max-duration` when you want depth to act as a maximum inside a budget. Read a file with one FEN per line: Select an evaluator: Shortcut flags are also available: `--classical`, `--nnue`, and `--lc0`. Defaults: If `--depth` is omitted, the command searches to depth `3` with a `250000` node budget and a `5s` wall-clock budget. If `--depth` is provided and no budget is provided, the command attempts to complete that depth without the default node/time caps. `--nodes` and `--max-nodes` are aliases. A value of `0` means unlimited. `--max-duration 0` means unlimited time. Evaluators `classical`: handcrafted material, piece-square, mobility, king safety, pawn structure, and terminal-position scoring. `nnue`: pure-Java NNUE evaluator using `models/crtk-halfkp.nnue` by default, or an explicit `--weights` path. `./install.sh --models` installs the default file. If the default file is absent, `--nnue` fails with a missing-weights error instead of silently using the smoke-test fallback. `lc0`: pure-Java LC0 value evaluator using the configured LC0J model path by default, or an explicit `--weights` path. The evaluator is used as the static score at the search frontier. The engine search itself is still a classical alpha-beta search; choosing `--lc0` does not turn the engine into an LC0-style MCTS engine. LC0 evaluation is much more expensive than the handcrafted evaluator, so high alpha-beta depths with `--lc0` can take a long time. NNUE architecture The Java NNUE path is intentionally small and explicit. The compact CRTK-format network uses HalfKP-style sparse features: `64 king buckets x 10 relative piece planes x 64 piece squares = 40,960` possible features. For each perspective, active sparse features are accumulated into one hidden vector, clipped ReLU is applied, and the side-to-move and opponent activations are projected through one linear output layer to a centipawn score. `FeatureEncoder` maps a `Position` into sparse feature indices, and `Accumulator` stores the hidden sums so the searcher can update deltas incrementally after moves instead of rebuilding from scratch each time. `Model.load(path)` auto-detects both the compact CRTK format and supported Stockfish `.nnue` files. The compact CRTK path exposes public incremental search-state helpers; the Stockfish path is compatibility-oriented inference. For source-level package docs, see `src/chess/nn/nnue/package-info.java`. Search behavior The search is deterministic and supports: iterative deepening negamax alpha-beta search quiescence search for noisy leaf positions internal transposition and static-evaluation caches principal variation output move ordering with principal variation, transposition-table, capture, promotion, killer-move, and history signals `--depth`, `--nodes`, and `--max-duration` limits The transposition table is built into the searcher and is not currently exposed as a user-tunable `--hash` option. External UCI commands still support `--threads` and `--hash` because those options are sent to the configured UCI engine; the in-house engine is single-threaded search with deterministic in-process state. Output formats: default / `--format uci-info`: UCI-style `info depth ... pv ...` lines, followed by `bestmove ...` if a slow evaluator exhausts its budget before depth 1 completes, `uci-info` emits the static fallback as `info depth 0 ... pv ...` `--format uci`: best move only, e.g. `e2e4` `--format san`: best move only, e.g. `e4` `--format both`: UCI and SAN separated by a tab `--format summary`: FEN, evaluator, best move, score, completed depth, nodes, elapsed time, stopped flag, and PV Practical Uses Use the built-in engine for: deterministic best-move output in scripts that should not spawn an external process small smoke tests in CI bounded puzzle-solve timing evaluator experiments with the same search shell local analysis when no UCI engine is configured quick comparisons between classical, NNUE, and LC0 value evaluators local workflows where process startup and protocol parsing would dominate the work Use a mature UCI engine for: strength-sensitive analysis long MultiPV analysis production puzzle mining large-scale tactical validation Benchmarking and Validation The built-in engine reports completed depth, nodes, elapsed time, stopped flag, score, and PV in `--format summary`: Compare move-generation correctness separately from search quality: `engine perft` and `engine perft-suite` use `chess.core` directly. The suite checks stored truth values for critical standard positions, Chess960 castling, en-passant, promotions, open castling lanes, and a knight-heavy stress position. It prints a progress bar first, then a table with `Truth`, `Calculated`, `Speed`, and `Match`. Use external engine commands when you want a reference move or PV: When comparing engines, keep the limits explicit. Depth, node budgets, time budgets, evaluator choice, thread count, hash size, and startup overhead all change what a result means."
  },
  {
    "title": "LC0",
    "href": "lc0.html",
    "category": "User Guides",
    "text": "LC0 (UCI Engine and Java Evaluator) ChessRTK supports two LC0-related workflows: LC0 as an external UCI engine for analysis and mining, usually with `.pb.gz` weights. A Java LC0 value evaluator for `engine eval`, `engine builtin --lc0`, board display, and ablation overlays, using LC0J `.bin` weights. Model weights are not checked into this repo. Local `models/*.bin` files are gitignored; fetch the default LC0J weights with `./install.sh --models` or download them manually from `models/README.md`. LC0 as a UCI Engine If your engine protocol points to `lc0` (see `config/lc0.engine.toml`), you typically also need to set: Recommended workflow: Download a network locally (gitignored): Update your engine protocol TOML to point `WeightsFile` at that path. (Optional) switch mining thresholds to the included `config/cli.lc0.config.toml` baseline (copy it over `config/cli.config.toml`). Guardrail (fail if weights are tracked by git): Use the configured UCI engine through the normal engine commands: Java LC0 Evaluator ![LC0 CNN evaluator flow](../assets/diagrams/crtk-lc0-cnn.png) Diagram source: `assets/diagrams/crtk-lc0-cnn.dot` (render with `dot -Tpng -Gdpi=160 -o assets/diagrams/crtk-lc0-cnn.png assets/diagrams/crtk-lc0-cnn.dot`). The Java evaluator lives under `src/chess/nn/lc0/` and is used by LC0-aware evaluation, display, and dataset paths. Defaults: weights path: `models/leela_112planes-10blocksx128-policyhead80-valuehead32-policy4672-wdl3.bin` (`chess.nn.lc0.Model.DEFAULT_WEIGHTS`) backend: `auto`, which uses an available native GPU backend before falling back to the pure Java CPU evaluator Backend selection (system properties): `-Dcrtk.lc0.backend=auto|cpu|cuda|rocm|amd|hip|oneapi|intel` (default `auto`) `-Dcrtk.lc0.threads=N` (CPU backend only) Evaluate a position: Use LC0 value evaluation at the frontier of the built-in alpha-beta search: Display evaluator ablation and backend information: Check native GPU backend availability: Native GPU Backends (Optional) The LC0 Java evaluator can run on the pure Java CPU backend with no native library. Optional JNI backends are available for CUDA, ROCm/HIP, and oneAPI. Use `-Djava.library.path=...` to point Java at the backend build directory and `-Dcrtk.lc0.backend=...` to force a backend. Leaving the backend as `auto` chooses the first available native backend and then falls back to CPU. CUDA build example: Build the JNI library: Then run Java with the library on `java.library.path`: See `native/cuda/README.md`, `native/rocm/README.md`, and `native/oneapi/README.md` for backend-specific build notes."
  },
  {
    "title": "Outputs & Logs",
    "href": "outputs-and-logs.html",
    "category": "User Guides",
    "text": "Outputs & logs Output directory (`dump/`) By default, outputs are written under `dump/` (configurable via `config/cli.config.toml` or `puzzle mine --output ...`). `puzzle mine --output` behavior: directory output: `standard-<timestamp>.puzzles.json` / `standard-<timestamp>.nonpuzzles.json` (or `chess960-...`) file-like root (`.json` or `.jsonl`): `<stem>.puzzles.json` / `<stem>.nonpuzzles.json` Mining outputs are single top-level JSON arrays (objects appended incrementally). Derived record outputs Common converters derive output names from the input stem when `--output` is omitted: `record export plain`: `<stem>.plain` `record export csv`: `<stem>.csv` `record export pgn`: `<stem>.pgn` `puzzle pgn`: `<stem>.pgn` `record analysis-delta`: `<stem>.analysis-delta.jsonl` `record export training-jsonl`: `<stem>.training.jsonl` Tensor exporters write multiple files from one output stem: `record dataset npy`: `<stem>.features.npy`, `<stem>.labels.npy` `record dataset lc0`: `<stem>.lc0.inputs.npy`, `<stem>.lc0.policy.npy`, `<stem>.lc0.value.npy`, `<stem>.lc0.meta.json` `record dataset classifier`: `<stem>.classifier.inputs.npy`, `<stem>.classifier.labels.npy`, `<stem>.classifier.meta.json` Publishing commands follow the same convention: `book pdf`: defaults to the input stem plus `.pdf` when an input path exists. `book render`: defaults to the manifest stem plus `.pdf`. `book cover`: defaults to the manifest stem plus `-cover.pdf`. Session logs (`session/`) The CLI writes logs under `session/` via `chess.debug.LogService`. To clear session artifacts:"
  },
  {
    "title": "Support",
    "href": "support.html",
    "category": "User Guides",
    "text": "Support This page describes the information that makes ChessRTK issues actionable. Before Reporting Run the local health checks that match the failure: For UCI engine problems: For move-generation or notation problems: Include This In Bug Reports exact command line full FEN, PGN snippet, or input file shape expected behavior actual output or stack trace Java version from `java -version` operating system whether the launcher or `java -cp out application.Main` was used external engine path and protocol TOML when UCI is involved whether `./scripts/run_regression_suite.sh recommended` passes Common Places To Start Setup failure: [Build and install](build-and-install.md) Engine failure: [Configuration](configuration.md) Command syntax: [Command reference](command-reference.md) Model files: [LC0](lc0.md), [T5](t5.md), and `models/README.md` Generated outputs: [Outputs and logs](outputs-and-logs.md) Known operational issues: [Troubleshooting](troubleshooting.md) Security Notes Do not paste private engine paths, private dataset paths, unpublished book manuscripts, or proprietary model locations unless they are needed and safe to share. Replace them with equivalent local examples where possible."
  },
  {
    "title": "Mining Puzzles",
    "href": "mining.html",
    "category": "Workflows",
    "text": "Mining Puzzles `puzzle mine` drives a configured UCI engine across many seed positions, applies Filter DSL gates, and writes accepted puzzles and rejected/non-puzzle examples as reusable record dumps. The command is built for long-running data work: seed positions can come from random legal generation, FEN lists, FEN parent/child pairs, PGN mainlines, PGN variations, or Chess960 starts UCI analysis is bounded by node and wall-clock limits quality, winning-resource, drawing-resource, and accelerate gates are explicit Filter DSL programs accepted and rejected rows are both preserved, which makes classifier and ablation datasets easier to build outputs are JSON arrays that downstream `record` commands can merge, filter, split, summarize, and export Basic Runs Mine random standard positions: Mine from a FEN list: Mine from PGN: Mine Chess960 positions: Seed Sources Random Seeds If `--input` is omitted, ChessRTK generates random legal positions. Use `--random-count <n>` for a finite batch or `--random-infinite` for continuous generation. Useful caps: `--max-waves <n>` limits expansion waves `--max-frontier <n>` limits the frontier size per wave `--max-total <n>` limits total processed positions The infinite mode ignores wave and total caps because it is designed for continuous runs. FEN Lists `--input seeds.txt` accepts one or two FENs per line: one FEN: the row's `position` two or more FENs: the first is treated as `parent`, the second as `position` Blank lines and lines starting with `#` or `//` are ignored. Parent/child pairs are useful when you want to preserve the move that created the candidate position. Create FEN lists from PGN: PGN Inputs `--input games.pgn` parses games and extracts positions from movetext. PGN variations are preserved by the parser, and `fen pgn` can be used separately when you want to inspect or reuse the extracted seed file before mining. Chess960 Use `--chess960` or `-9` to: generate Chess960 random starts during random mining toggle Chess960 mode in the UCI protocol when the protocol template supports it `fen chess960` is useful for deterministic seed generation: Engine Limits Each evaluated position is searched up to: `--max-nodes <n>`: UCI `go nodes <n>` `--max-duration <duration>`: wall-clock safety net, such as `5s`, `2m`, or `60000` `--engine-instances <n>`: number of UCI engine processes used by the miner Defaults are read from `config/cli.config.toml`; command-line flags override those values for the run. Run a smoke check before long jobs: Gates Mining uses Filter DSL programs to decide which positions deserve more work and which records count as puzzles. The acceptance check is: Override gates on the command line when you want a run-specific definition: See [Filter DSL](filter-dsl.md) for syntax and examples. Outputs `--output` accepts either a directory or a file-like root. Directory output: standard mining writes `standard-<timestamp>.puzzles.json` standard mining writes `standard-<timestamp>.nonpuzzles.json` Chess960 mining uses `chess960-<timestamp>...` stems File-like output: `--output dump/run.json` writes `dump/run.puzzles.json` `--output dump/run.json` writes `dump/run.nonpuzzles.json` `.jsonl` roots use the same stem behavior The files are JSON arrays that are appended incrementally while staying valid. Downstream commands can process them without loading every row into custom scripts. Follow-Up Commands Inspect and filter records: Convert accepted puzzles: Export tensors: Render study material: Reliability Checks Before trusting a large run: Run `crtk doctor --strict` on the target machine. Run `crtk engine uci-smoke` with the protocol, thread, and hash settings intended for mining. Run `crtk engine perft-suite --depth 6 --threads <n>` after core changes. Mine a small batch, inspect `record stats`, and open a few converted PGNs. Keep the config file, protocol TOML, and command line with the produced dumps so results can be reproduced."
  },
  {
    "title": "Filter DSL",
    "href": "filter-dsl.html",
    "category": "Workflows",
    "text": "Filter DSL Mining gates (and some converters) use a compact, round-trippable DSL implemented by `chess.uci.Filter`. If you only want to tweak mining behavior, you generally only need: the list of supported predicates how `gate=...`, `break=...`, `null=...`, `empty=...` interact Shape A filter is a tree of blocks. Each block: picks a MultiPV line to inspect (`break=<n>`, where 1 = PV1, 2 = PV2, \u2026) evaluates predicates against that PV output optionally evaluates nested blocks (`leaf[ ... ]`) combines all booleans using `gate=<...>` Separators: predicates/keys are delimited by `;` whitespace is ignored Core keys `gate=<AND|NOT_AND|OR|NOT_OR|XOR|X_NOT_OR|SAME|NOT_SAME>` `break=<n>`: which PV line to inspect (1 = best line) `null=<true|false>`: return value when PV data is missing `empty=<true|false>`: return value when the block has no predicates/leaves Predicates (keys) Predicates compare an output attribute using one of: `>`, `>=`, `=`, `<=`, `<`. Supported keys: `depth`, `seldepth`, `multipv`, `hashfull` `nodes`, `nps`, `tbhits`, `time` (ms) `eval` (centipawns or mate; decimals are allowed as pawns, e.g. `300` == `3.0`, `#-2`) `chances` (WDL), e.g. `790/200/10` or `1000/0/0` Examples \u201cQuality\u201d: demand enough effort on PV1 and PV2 \u201cWinning puzzle\u201d: best line clearly good, second line not \u201cAccelerate\u201d: cheap reject to avoid wasting time Where filters live Default mining filters are stored in `config/cli.config.toml` as TOML multi-line strings. You can override them per run via `puzzle mine --puzzle-quality <dsl>`, etc."
  },
  {
    "title": "Datasets",
    "href": "datasets.html",
    "category": "Workflows",
    "text": "Datasets ChessRTK exports machine-learning datasets from mined or imported analysis records. The dataset commands are dependency-free Java writers: they create NumPy `.npy`, JSONL, and metadata files directly from record dumps. The usual inputs are: `*.puzzles.json` and `*.nonpuzzles.json` from `puzzle mine` `.record` JSON arrays from analysis pipelines JSONL record streams accepted by `record` utilities filtered/merged files produced by `record files` Use `record stats`, `record tag-stats`, and `record analysis-delta` before exporting large datasets. They make it easier to catch unstable evaluations, unexpected tag distributions, and class imbalance. Export Inventory Eval Regression: `record dataset npy` Input: a `.record` JSON array. Output: `<stem>.features.npy` shaped `(N, 781)` float32 `<stem>.labels.npy` shaped `(N,)` float32 Labels are evaluations in pawns, clamped to `[-20, +20]`. Use this export when you want compact numeric features without a chess-engine specific plane layout. LC0-Style Tensors: `record dataset lc0` Input: a `.record` JSON array. Output: `<stem>.lc0.inputs.npy` shaped `(N, 112*64)` float32 `<stem>.lc0.policy.npy` shaped `(N, policySize)` float32 `<stem>.lc0.value.npy` shaped `(N,)` float32 in `[-1, 1]` `<stem>.lc0.meta.json` with policy encoding, compression, value scale, and source metadata Policy rows are one-hot. If `--weights` is supplied, ChessRTK reads the LC0J weights policy map and compresses policy targets to the network's policy size. Without weights, the raw 73-plane policy size (`4672`) is used. This export is useful when you want to verify LC0-style feature encoding, inspect policy targets, or train/evaluate tooling that expects LC0-shaped planes. Binary Classifier: `record dataset classifier` Input: one or more record files or directories. Output: `<stem>.classifier.inputs.npy` shaped `(N, 21*64)` float32 `<stem>.classifier.labels.npy` shaped `(N,)` float32 `<stem>.classifier.meta.json` with sources, filters, class counts, and positive-class weight Labels are resolved in this order: `--label-filter <dsl>` marks matching rows positive and non-matching rows negative. A record `kind` field of `puzzle` or `nonpuzzle` is used when present. The configured puzzle verification Filter DSL is used when `kind` is absent. Use `--filter <dsl>` to select eligible rows before labeling. Use `--max-positives` and `--max-negatives` to cap classes for balanced smoke datasets. JSONL Labels: `record export training-jsonl` This command writes one position per JSONL line. It is designed for pipelines that prefer structured text over tensor files. Label policy: `coarse_label=1`, `fine_label=2`: rows matching the puzzle DSL `coarse_label=1`, `fine_label=1`: rows sharing a parent FEN with a puzzle `coarse_label=0`, `fine_label=0`: remaining rows Engine/PV details are omitted from model input by default. Add `--include-engine-metadata` when you want those details retained as metadata. Puzzle JSONL: `record export puzzle-jsonl` This command writes puzzle rows with LC0 policy information. It requires LC0J weights because it uses the weights policy map. Use `--filter <dsl>` for row selection. Use `--nonpuzzles` to export negative examples through the same shape. Stability Features: `record analysis-delta` `record analysis-delta` writes one JSONL row per record with: initial and final evaluation delta type and delta magnitude fluctuation range depth/time to final value diagnostics useful for filtering unstable engine samples Recommended Export Flow Merge and filter source dumps with `record files`. Inspect counts with `record stats` and `record tag-stats`. Generate `record analysis-delta` and remove unstable rows if needed. Export one tensor or JSONL shape for the target training code. Keep the generated `*.meta.json` next to tensor files so model runs can be traced back to sources, filters, and label policy. Converting `.npy` to Framework Shards ChessRTK writes portable `.npy` files. Framework-specific shards can be built by loading those files in the training stack and saving the native format for that framework."
  },
  {
    "title": "Book Publishing",
    "href": "book-publishing.html",
    "category": "Workflows",
    "text": "Book publishing ChessRTK can produce native vector PDFs for five publishing jobs: `book collection`: build a dense puzzle-collection TOML manifest from record JSON/JSONL, with optional interior and cover PDF output. `book study`: render richer annotated puzzle studies with hints, analysis text, comments, and figure lists. `book pdf`: quick diagram sheets from FEN lists or PGN mainlines. `book render`: full puzzle books from JSON/TOML manifests. `book cover`: matching paperback, hardcover, or ebook covers from the same manifest. No LaTeX step is required for these commands. Legacy aliases `book ilovechess` and `book artofchess` still resolve to `book collection` and `book study`. The native book renderer keeps publishing-specific text helpers in `chess.book.render`. Solution SAN in captions and tables is converted to figurine algebraic notation, so moves such as `Qxe8+` render with chess figurines and a multiplication sign for captures. Minimal workflow For reviewer or free-edition copies, generate a traceable watermarked PDF: `--watermark-id` implies `--watermark`; it adds page-specific overlay text, corner marks, and matching PDF metadata. Pass the rendered interior PDF to `book cover --pdf <path>` so the cover command can read the actual trim width, trim height, and page count from the interior file. If `--pdf` is omitted, the cover command falls back to manifest `paperwidth` / `paperheight`, then manifest `pages`, then an estimate from the puzzle grid and solution-table cadence. `--pages` still overrides the inferred page count when you need a manual spine width. The `--check` flag is a dry run. For `book render`, it validates page geometry, FEN parsing, and solution moves. For `book cover`, it validates the same manifest and prints the calculated trim, spine, and full-cover dimensions. Puzzle collection workflow Use `book collection` when your source material is a record JSON/JSONL dump with a starting `position` and a PV in `analysis`. The command converts the first PV into move-numbered SAN, writes a TOML manifest, and can optionally render the interior and cover in one pass. If you omit `--subtitle`, the command falls back to a generic `\"<count> Chess Puzzles\"` subtitle. If you omit front-matter paragraphs, the builder supplies a compact default introduction and leaves the native how-to-read and afterword fallbacks in place. Puzzle study workflow Use `book study` when you already have richer annotated content and want to keep per-entry description text, comments, hints, and figure lists. This command accepts JSON or TOML input, can write a normalized TOML manifest, renders the composition-style interior PDF, and can build a matching cover in one pass. The manifest stays separate from the lean `book render` model on purpose: `book study` keeps the richer `Composition` fields instead of collapsing everything into `position + moves`. Example TOML: Important puzzle-study fields: `[[compositions]]` is the repeated annotated entry list. `description`, `comment`, `analysis`, and `hintLevel1..4` map directly to the rendered section blocks. `figureFens` is the diagram backbone; `figureMovesAlgebraic`, `figureMovesDetail`, and `figureArrows` stay aligned with it. `pageSize`, `margin`, `diagramsPerRow`, `boardPixels`, `whiteSideDown`, and `showFen` control the composition-PDF layout. Manifest shape Both JSON and TOML are accepted. TOML is usually easier to maintain: Important fields: `paperwidth` / `paperheight` are trim dimensions in centimeters. `pages` is used by cover generation for spine width. `puzzlerows` and `puzzlecolumns` control how many diagrams appear on each puzzle page. Each `[[elements]]` entry needs a starting FEN in `position` and a SAN solution line in `moves`. `blurb` is reused on the back cover. Cover dimensions `book cover` calculates dimensions from trim size, binding type, interior paper, and printed page count. Bindings: `paperback`: wraparound back + spine + front cover with 0.125 inch bleed on each outside edge. `hardcover`: case-laminate cover with 1.5 cm wrap and 1.0 cm hinge allowance. `ebook`: front-cover-only PDF; spine and back cover are omitted. Interior tokens: `white-bw`: white paper, black-and-white printing. `cream-bw`: cream paper, black-and-white printing. `white-standard-color`: white paper, standard color. `white-premium-color`: white paper, premium color. Spine width is `pages * paper-thickness`. The current constants are: Diagram sheets Use `book pdf` when you want a smaller document instead of a full puzzle book: Input choices are exclusive: use exactly one of `--fen`, `--input`, or `--pgn`. Checks After editing publishing code or manifests, these checks cover the native PDF paths: Use `-Djava.awt.headless=true` on machines without a reachable display server, including CI and many remote shells. For print-on-demand dimensions, compare the final values printed by `book cover --check` with the publishing service's own cover calculator before uploading. References: KDP print cover calculator: https://kdp.amazon.com/en_US/help/topic/G201953020 KDP paperback submission guidelines: https://kdp.amazon.com/en_US/help/topic/GDTKFJPNQCBTMRV6"
  },
  {
    "title": "Tags",
    "href": "piece-tags.html",
    "category": "Workflows",
    "text": "Position and Piece Tags ChessRTK emits compact, deterministic tags for positions, pieces, pawn structure, king safety, material, tactics, engine evaluations, and puzzle lines. Tags are used by: `fen tags` `puzzle tags` `record tag-stats` `fen text` `puzzle text` dataset and filtering workflows that consume tag strings ![Tagging pipeline](../assets/diagrams/crtk-tagging-flow.png) Diagram source: `assets/diagrams/crtk-tagging-flow.dot`. Tag Shape Tags use a stable family prefix followed by ordered key/value fields: Examples: Tags are strings, but they are intentionally structured so they can be sorted, deduplicated, filtered, counted, and converted into text. Tag Families Not every family appears for every position. Engine-derived families appear when analysis is requested or supplied. Commands Tag one position: Include the FEN in the output: Tag a FEN file: Tag PGN mainlines or sidelines: Tag puzzle PVs: Use engine-enriched tags: Use static tags only: Delta Tags `--delta` emits JSONL rows that describe how tags change from a parent position to a child position. This is useful for move explanations, puzzle-line summaries, and training rows that need \"what changed after this move\" evidence. For FEN pair input, each line can contain: For PGN input, parent/child relationships are created from the parsed movetext. Text Generation The T5 commands consume tags and generate short natural-language summaries: The text commands can use static tags or engine-enriched tags. Keep engine limits explicit when using them in batch jobs: Interpreting Piece Tags `PIECE` tags describe placement and activity. Common fields include: `tier`: placement tier such as `very_strong`, `strong`, `neutral`, `weak`, or `very_weak` `activity`: mobility or activity fact such as `low_mobility` `extreme`: strongest/weakest piece anchors for the whole board or one side `side`: `white` or `black` `piece`: `pawn`, `knight`, `bishop`, `rook`, `queen`, or `king` `square`: algebraic square Treat these tags as auditable hints. Tactical and king-safety tags should carry more weight than a generic placement tier when explaining a forcing sequence. Counting Tags Summarize tags in record dumps: Use this before training or publishing from mined data. It exposes class imbalance, missing families, and overly common generic tags."
  },
  {
    "title": "T5 Text",
    "href": "t5.html",
    "category": "Workflows",
    "text": "T5 tag-to-text pipeline ChessRTK includes a no-deps T5 runtime that turns tag lists into short prose summaries. It expects a custom `.bin` export and uses the same tag vocabulary as `crtk fen tags`. ![T5 tag-to-text pipeline](../assets/diagrams/crtk-t5-pipeline.png) Diagram source: `assets/diagrams/crtk-t5-pipeline.dot` (render with `dot -Tpng -Gdpi=160 -o assets/diagrams/crtk-t5-pipeline.png assets/diagrams/crtk-t5-pipeline.dot`). Inputs Tag lines as newline-separated text. Or a JSON array of strings (the default `crtk tags` output). A T5 `.bin` weights export passed via `--model`. Running position summaries Use `fen text` when you want a short summary for one FEN or a FEN file: Useful options: `--include-fen`: emit JSON with `fen` and `summary`. `--analyze`: enrich tags with engine analysis before summarization. `--max-new <n>`: cap generated tokens, default `128`. `--protocol|-p`, `--max-nodes`, `--max-duration`, `--multipv`, `--threads`, `--hash`, `--wdl`, `--no-wdl`: engine options used with `--analyze`. Running puzzle-line summaries Use `puzzle text` when you want summaries for the root PV and expanded puzzle continuations: Related command: `puzzle tags` emits JSONL with per-move tag deltas. `puzzle text` runs the same expanded positions through the T5 summarizer. Low-level runner Pipe tags directly: Or read from a file: Options: `--model <path>` optional (defaults to `t5-model-path` from `config/cli.config.toml`). `--input <path>` optional (reads stdin if omitted). `--max-new <n>` max generated tokens (default `128`). `--debug` prints token counts and top logits. Backends By default the runner uses the CPU path. You can override the backend selection with: `-Dcrtk.t5.backend=auto|cpu|cuda|rocm|oneapi` When optional native backends are present, they are tried in the requested order with CPU fallback."
  },
  {
    "title": "AI Agents",
    "href": "ai-agents.html",
    "category": "Workflows",
    "text": "AI Agents and Automation ChessRTK exposes deterministic chess primitives that work well in CI, scripts, and LLM tool workflows. The safest pattern is to use the narrowest command for the task: move conversion for notation, FEN commands for position validity, perft commands for move-generation checks, and bounded engine commands for analysis. These commands are worth using because they are built on the same correctness surface as the rest of the toolkit: one shared position model, deterministic output shapes, and regression-backed move-generation checks instead of ad hoc string parsing or GUI scraping. ![Agentic command contracts](../assets/diagrams/crtk-agentic-commands.png) Diagram source: `assets/diagrams/crtk-agentic-commands.dot`. Why Agents Can Trust This Machine-oriented commands prefer line-based formats, explicit flags, stable exit codes, and bounded search budgets. Core move generation is validated against stored perft truth positions and detailed counters, so legality-sensitive workflows do not depend on an external engine. The same core is reused for notation conversion, line application, perft checks, builtin search, exporters, and higher-level automation workflows. `./scripts/run_regression_suite.sh core` and `./scripts/run_regression_suite.sh recommended` provide repeatable regression entry points before longer CI or agent runs. Quick Verification Before using the commands in longer automated runs, a fast smoke pass is: Command Contracts Recommended Agent Workflow Run `crtk doctor` and `crtk config validate` before long jobs. Normalize input FENs with `fen normalize`. Use `move list --format both` to expose legal moves with both UCI and SAN. Use `move after` or `move play` to advance positions instead of editing FENs. Use `engine bestmove --format both` when a configured UCI engine is available. Use `engine builtin --format summary` when in-process bounded search is preferable. Run `engine perft-suite --depth 6 --threads <n>` after core move-generation changes. Deterministic Move Tasks List legal moves: Convert moves: Apply a SAN or UCI line: These commands are better for automation than parsing board diagrams because they have compact, line-oriented output. Engine Tasks External UCI engine: Built-in Java engine: For UCI engine commands, use explicit `--nodes`, `--max-duration`, `--threads`, and `--hash` values when you need reproducible behavior. For the built-in engine, use `--depth`, `--nodes`, `--max-duration`, and the evaluator flags; it does not spawn an engine process or accept UCI `--threads`/`--hash` options. Move-Generation Checks Single-position counters: Regression suite: The suite prints a progress bar while positions run. After the progress bar finishes, it prints a table with `No`, `Depth`, `FEN`, `Truth`, `Calculated`, `Speed`, and `Match`. Record and Dataset Plumbing Agents can keep data transformations explicit: Dataset exports: Practical Rules Prefer `--format uci|san|both|summary` flags over parsing prose. Prefer FEN and UCI/SAN commands over GUI or image output for agent decisions. Put engine budgets on every automated analysis command. Keep `--verbose` off in normal pipelines and enable it only for failures. Treat `doctor --strict`, `config validate`, and `engine uci-smoke` as setup gates for CI jobs."
  },
  {
    "title": "Architecture",
    "href": "architecture.html",
    "category": "Development",
    "text": "Architecture ChessRTK is organized around one shared Java position model. The goal is that a FEN accepted by one command means the same thing to every other command: notation conversion, legal move lists, perft, search, tags, datasets, rendering, and publishing all depend on the same chess rules implementation. High-Level Layers ![ChessRTK pipeline overview](../assets/diagrams/crtk-pipeline-overview.png) `chess.core` mutable position state legal move generation make/undo state attack and pin helpers FEN, SAN, UCI move helpers Chess960 setup and castling behavior `chess.debug` perft and divide tools reference-suite validation diagnostics and logging helpers `chess.engine` and `chess.eval` built-in alpha-beta search classical, NNUE, and LC0-facing evaluator hooks `chess.uci` external engine protocol orchestration analysis parsing MultiPV, WDL, node, time, thread, and hash options `application.cli` stable command contracts grouped command dispatch config resolution and validation Workflow layers puzzle mining record filtering and export dataset generation image and PDF rendering Studio GUI and other GUI entry points Why One Core Matters Chess workflows are coupled. If notation conversion and move generation use different assumptions, downstream results become hard to trust. ChessRTK avoids that by letting the same core answer these questions: Is the FEN legal? Which moves are legal? What is the SAN for this move? What position results after this line? Do perft counts match known reference positions? What should the renderer, tagger, dataset exporter, and book generator show? Command Flow Most commands follow this shape: Parse CLI flags with `utility.Argv`. Resolve defaults through `application.Config`. Build a `Position` from FEN, PGN, generated input, or record data. Execute the command against the shared chess core. Emit a deterministic text, JSON, image, tensor, or PDF output. Commands that need engine analysis either: call the in-process Java engine through `chess.engine`, or start a configured UCI engine through `chess.uci`. Data Flow Typical research pipeline: Important Design Constraints Keep core commands deterministic by default. Make node and time budgets explicit for engine work. Keep model files, engine binaries, dumps, and generated PDFs as local artifacts. Prefer line-based or structured outputs that are easy to script. Verify legality-sensitive changes with perft and SAN/FEN regressions. Related Pages [Development notes](development-notes.md) [Command reference](command-reference.md) [In-house Java engine](in-house-engine.md) [LC0 UCI engine and Java evaluator](lc0.md) [Datasets](datasets.md)"
  },
  {
    "title": "Quality & Testing",
    "href": "quality-and-testing.html",
    "category": "Development",
    "text": "Quality And Testing ChessRTK is correctness-sensitive. FEN parsing, SAN conversion, Chess960 castling, en-passant, promotion, make/undo state, and move generation all feed engine analysis, datasets, rendering, and publishing. Default Check Before Pushing This is the normal local confidence pass. It builds the project, runs lint, and executes the core regression groups used most often during development. Focused Suites Examples: Move-Generation Confidence Use perft whenever a change can affect legal moves or board state. `engine perft-suite` compares stored truth positions against the Java move generator. It does not start Stockfish or any other external engine. Engine And UCI Confidence Use UCI smoke tests when external engine setup or protocol handling changed. Use the built-in engine regression when `chess.engine` or evaluator integration changed: Publishing Confidence Use these checks for book, PDF, or rendering changes: Documentation Confidence The website-style docs are generated from the repository wiki source: The printable manual uses the same wiki source and writes both `docs/manual.html` and `docs/chessrtk-manual.pdf`: Then check links/assets in `docs/` before publishing. The GitHub Wiki is a separate repository, so changes pushed there should also pass a simple internal link check. Recommended Bug Report Baseline When reporting a bug, include: exact command full FEN or input file shape Java version and operating system UCI engine protocol TOML when relevant whether `./scripts/run_regression_suite.sh recommended` passes any failing perft row or regression test name"
  },
  {
    "title": "Development Notes",
    "href": "development-notes.html",
    "category": "Development",
    "text": "Development notes Project shape `src/application/` \u2014 CLI entry point, config loading, session/log plumbing `application.Main`: subcommand parsing + dispatch `application.Config`: loads `config/cli.config.toml` (auto-seeded if missing) `src/chess/` \u2014 chess core, UCI protocol, mining pipeline, render/display helpers `chess.core`: Java-native board state, FEN/SAN helpers, legal move generation, and make/unmake `chess.book.render`: native puzzle-book layout, table text, and figurine movetext formatting `chess.debug`: perft runners, perft validation suite, board/move printers, and logging helpers `chess.uci`: engine protocol + analysis parsing + Filter DSL `chess.io`: converters, readers/writers, dataset exporters `chess.engine`: in-process alpha-beta search used by `engine builtin` `chess.eval`: in-process evaluator facade used by `engine eval`, `engine static`, `engine builtin`, display ablation, and tag helpers `chess.nn.lc0`: pure-Java LC0 forward pass with optional CUDA, ROCm, and oneAPI native backends Core chess implementation The current chess rules implementation lives in `chess.core`. There is no separate public `bitboard` package: the optimized bitboard-backed code is the core implementation. Important classes: `Position`: mutable position state, FEN round-tripping, legal move helpers, `play(...)`, and `undo(...)`. `MoveGenerator`: pseudo-legal/legal move generation, attack tests, pinned piece detection, and node-only perft. `MoveList`: compact mutable list for encoded `short` moves. `Fen`: strict FEN parse/format helpers, including Chess960 castling metadata. `SAN`: SAN formatting, SAN parsing, and move-line application. `Setup`: standard starts, Chess960 starts, and random position helpers. Detailed perft is intentionally in `chess.debug.Perft`, while the stored-truth validation suite is in `chess.debug.PerftSuite`. Both use `chess.core` positions and move generation, and neither path depends on an external engine. Shared helpers Keep small cross-package helpers in `utility` when the behavior is genuinely generic. Current examples: `utility.Numbers`: inclusive numeric clamping, unit-interval clamping, and 8-bit color-channel clamping. `utility.Argv`: command-line option parsing. `utility.Json`, `utility.Toml`, and `utility.Svg`: dependency-free structured text and rendering helpers. Rendering-specific text belongs near the renderer. For example, `chess.book.render.MoveText` converts SAN-like solution text to figurine algebraic notation for book tables and PDF captions. Adding/changing CLI commands Add a new `case` in `application.Main.main(...)`. Implement a `runXxx(Argv a)` handler (use `utility.Argv` for parsing). Update the help text in `application.Main.help()`. Update docs: `wiki/command-reference.md` `wiki/example-commands.md` Docs policy The root `README.md` should carry the first-screen framing: what ChessRTK is, why it is technically hard, how to verify it quickly, and where the main system boundaries are. Longer explanations, workflows, and reference material live under `wiki/`. Optional tooling If you plan to iterate on ChessRTK regularly, consider adding a small layer of local tooling: `just` (or a `Makefile`) for common tasks like build/release/smoke tests `pre-commit` hooks for `shellcheck` (shell scripts) and `google-java-format` (Java) `jlink` / `jpackage` for optional self-contained distributions Related: `wiki/roadmap.md`."
  },
  {
    "title": "Releasing",
    "href": "releasing.html",
    "category": "Development",
    "text": "Releasing ChessRTK (`crtk`) This repo is intentionally build-tool light: the CLI is a runnable Java 17 jar. Optional native GPU backends live under `native/cuda/`, `native/rocm/`, and `native/oneapi/`. The release script below packages the CUDA Linux artifact. Linux (x86_64) + CUDA release artifact Prerequisites: Java 17+ JDK (`javac`, `jar`) CMake 3.18+ CUDA toolkit (`nvcc`) NVIDIA driver (runtime) Build and package: To include local, gitignored model files in the bundle: Outputs: `dist/crtk-vX.Y.Z-linux-x86_64-cuda.tar.gz` `dist/SHA256SUMS` Quick smoke test (from the extracted artifact directory): GitHub release checklist Release readiness checklist Ensure the working tree is clean: `git status --short` Run a fresh compile and lint pass: `./scripts/run_regression_suite.sh build` `./scripts/run_regression_suite.sh lint` Run the recommended local regression suite: `./scripts/run_regression_suite.sh recommended` Run a deeper perft suite after move-generation or notation changes: `CRTK_PERFT_SUITE_DEPTH=6 CRTK_PERFT_THREADS=4 ./scripts/run_regression_suite.sh perft-smoke` Update docs that changed with the release: `README.md`, `wiki/command-reference.md`, `wiki/build-and-install.md`, and any new feature notes Build a fresh runnable jar: `./scripts/run_regression_suite.sh jar` Confirm release artifacts include the current license file: keep `LICENSE.txt` bundled with packaged releases and distributed archives GitHub release workflow Ensure the release-readiness checklist above is complete. Decide on version `vX.Y.Z` and tag it: `git tag -a vX.Y.Z -m \"vX.Y.Z\"` `git push origin vX.Y.Z` Run the release build: `scripts/make_release_linux_cuda.sh --version vX.Y.Z` or at minimum `./scripts/run_regression_suite.sh release` Create a GitHub Release for tag `vX.Y.Z` and upload: `dist/crtk-vX.Y.Z-linux-x86_64-cuda.tar.gz` `dist/SHA256SUMS`"
  },
  {
    "title": "Roadmap",
    "href": "roadmap.html",
    "category": "Development",
    "text": "Roadmap / ideas This page is a lightweight backlog of **proposed** additions. Anything listed here may change, and is not implemented unless it appears in `wiki/command-reference.md`. Implemented from this list `doctor`: sanity-check Java, config resolution, engine protocol, engine discovery, and local artifact paths. `engine uci-smoke`: engine health check suitable for CI and agents. Proposed CLI subcommands `puzzle pgn`: extend mined puzzle PGN export with richer solution tags and optional \u201cfail move\u201d annotations. `dump-filter` / `dump-grep`: apply Filter DSL to `.record` / puzzle dumps and emit JSONL/CSV subsets. `dump-merge` + `dump-dedupe`: merge shards and deduplicate by `(fen, bestmove[, pv])` with stable ordering. `uci-shell`: interactive UCI REPL (send commands, parse `info`, show PVs/WDL, quick presets like `go nodes` / `go movetime`). `arena`: engine-vs-engine matches (time controls, opening suite, PGN output, Elo summary). `engine perft-suite --suite <file>`: let the existing perft regression command read custom expected-node suites. `analyze-batch`: analyze many FENs into JSONL with strict limits (`--nodes`/`--max-duration`) and stable schemas. `eval-diff` / `bestmove-diff`: compare engines/evaluators over a position set and emit summary stats + CSV/JSON for plots. Developer tooling (optional) `just` (or a `Makefile`): one-liner entrypoints for `build`, `release`, `install`, and quick smoke tests. `pre-commit` hooks: run `shellcheck` on `install.sh` / `scripts/` and apply `google-java-format` consistently. `jlink` / `jpackage`: optional self-contained distributions so users can run without a system JDK."
  },
  {
    "title": "Glossary",
    "href": "glossary.html",
    "category": "Development",
    "text": "Glossary CRTK Short command name for ChessRTK. The installed launcher is `crtk`. FEN Forsyth-Edwards Notation. A single-line representation of a chess position. SAN Standard Algebraic Notation. Human-readable move notation such as `Nf3`, `exd5`, `O-O`, or `Qxe8+`. UCI Universal Chess Interface. The protocol used by engines such as Stockfish and LC0. ChessRTK can start and control UCI engines through protocol TOML files. Perft Performance test. Counts legal move-tree nodes to a fixed depth. Perft is a standard way to validate move generation because every legal move branch must be counted exactly. Divide A per-root-move perft report. Divide output shows which first move contributes which node count, making move-generation bugs easier to isolate. Record ChessRTK's reusable JSON analysis-record format. Record commands can filter, merge, summarize, export, and convert records into other data formats. Filter DSL Small expression language used to select records or puzzle candidates based on fields such as evaluation, PV shape, move type, tags, and metadata. NNUE Efficient neural-network evaluator architecture used by modern alpha-beta chess engines. ChessRTK can load NNUE weights for the built-in search evaluator. LC0 Leela Chess Zero. In ChessRTK docs this can mean either an external LC0 UCI engine or the Java LC0-style evaluator path, depending on context. WDL Win/draw/loss probabilities or estimates. Some UCI engines can report WDL alongside centipawn or mate scores. MultiPV Multiple principal variations from an engine search. Useful for analysis, threat detection, and puzzle mining. PV Principal variation. The engine's preferred line from the current position. NAG Numeric annotation glyph in PGN, such as `$1` for a good move."
  },
  {
    "title": "Tagging Plan",
    "href": "tagging-implementation-plan.html",
    "category": "Development",
    "text": "Tagging Implementation Plan This document is the implementation plan for completing ChessRTK's position, move, tactic, and puzzle-theme tags. The goal is not just to add many strings. The goal is to build a deterministic, auditable tagging layer that can serve three consumers: CLI users running `fen tags`, `puzzle tags`, `record tag-stats`, and `record analysis-delta`. Dataset builders using tags as labels, filters, or training text inputs. LLM/agent workflows that need compact, stable chess facts instead of prose. Current State There is one public tag implementation: `chess.tag`: canonical tag pipeline used by CLI, GUI, records, tests, and engine analysis workflows. No duplicate tag pipeline is part of the documented CLI surface. Guiding Rules Tags must be deterministic for the same position, analysis input, and config. Heuristic tags must not require an engine, neural model, or network access. Engine-derived tags must be clearly separated and only emitted when analysis was requested or supplied. Tags must be stable strings. Renames need migration notes because users may train models or write filters against them. Prefer exact board predicates over vague names. A tag should explain why it was emitted or encode enough fields to reconstruct the reason. Avoid duplicate concepts. Chess.com, Lichess, and ChessFox labels should map into canonical tags where possible. Each additional tag family needs positive and negative regression positions. Keep tag generation allocation-conscious. It will be used in batch pipelines. Canonical Tag Shape Use the existing family style: Examples: Keep the canonical internal output structured enough for `Line`, `Identity`, and `Sort`; compatibility converters should live outside the tag pipeline. Tag Families The complete family set should be: `META`: side to move, phase, source, evaluator metadata, difficulty, ECO/opening metadata. `FACT`: status, check state, checkers, castling, en-passant, legal state facts. `MATERIAL`: material totals, imbalance, piece counts, bishop pair, exchange state. `MOVE`: legal move counts and one-ply tactical move counts. `CAND`: candidate moves from engine or bounded tactical search. `PV`: principal variation tags from engine analysis. `IDEA`: concise strategic idea tags derived from facts or analysis. `THREAT`: threats from engine or bounded reply search. `KING`: king safety, exposure, flight squares, pawn shield, back-rank issues. `PAWN`: pawn structure and promotion facts. `PIECE`: piece activity, outposts, trapped pieces, hanging pieces, overloaded defenders. `TACTIC`: tactical motifs and forcing mechanisms. `CHECKMATE`: checkmate status and named mate patterns. `ENDGAME`: material endgame classes and practical endgame themes. `OPENING`: ECO code and opening name. `SPACE`: center/side space information. `DEVELOPMENT`: undeveloped pieces and development lead. `MOBILITY`: side and piece mobility. `INITIATIVE`: forcing-move and tempo indicators. `Sort` and `Identity` must understand every family and every identity-relevant field. Any additional family gets added there before it is exposed by CLI commands. Target Architecture Package Layout Keep `chess.tag` as the public API. Split implementation into focused modules: The exact filenames can change, but the rule is simple: one large `Tagging` class should not accumulate every motif. Tag Context Create a reusable context object for one position: `Position position` `byte[] board` `MoveList legalMoves` side to move king squares material counts and material phase attack maps by side defenders by square attackers by square pinned pieces by side checkers pawn file counts optional `Analysis` optional `Evaluator` The context should compute expensive values lazily and cache them for all detectors. This avoids repeated legal move generation and repeated attack scans. Tag Emitter Introduce a small helper for canonical tags: This reduces typo risk and makes field ordering consistent. It also makes it easy to enforce escaping rules if a field value ever contains spaces. Detector Contract Each detector should follow this shape: No detector should call the engine or load config unless it is explicitly an engine/config detector. Heuristic detectors should be pure functions over the position and cached context. Implementation Phases Phase 0: Stabilize The Contract Deliverables: Document canonical tag grammar in `wiki/piece-tags.md` or a dedicated tag reference. Add a `TagEmitter`. Add `TagContext`. Add `TagDetector` or an equivalent internal pattern. Update `Sort.FAMILY_ORDER` with the final family set. Update `Identity` to handle all planned family keys gracefully. Add a no-engine golden test harness for tag fixtures. Definition of done: Existing `fen tags` output is unchanged except for intentional normalizations. `TaggingRegressionTest` still passes. Parser tests prove every planned family can be parsed and sorted. Phase 1: Canonical Tag Consolidation Keep all additional detectors in `chess.tag` modules. Port first: `META: to_move` `META: phase` `FACT: status` `FACT: in_check` `FACT: castling` `FACT: en_passant` `FACT: checker` `MATERIAL: cp_white` `MATERIAL: cp_black` `MATERIAL: balance` `MATERIAL: queens` `MATERIAL: rooks` `MATERIAL: bishop_pair` `MOVE: legal` `MOVE: captures` `MOVE: checks` `MOVE: mates` `MOVE: promotions` `MOVE: castles` `KING: attackers` `KING: exposed` `PAWN: doubled` `PAWN: isolated` `PAWN: advanced` `PAWN: promotion_ready` `PAWN: passed` `TACTIC: pin` `TACTIC: hanging` `TACTIC: fork` `CHECKMATE: winner` `CHECKMATE: defender` `CHECKMATE: delivery` `CHECKMATE: pattern=double_check` `CHECKMATE: pattern=back_rank_mate` `CHECKMATE: pattern=smothered_mate` `CHECKMATE: pattern=support_mate` `CHECKMATE: pattern=corner_mate` Definition of done: `chess.tag.Tagging.tags(position)` covers the fixture categories used by the regression tests. No duplicate tag package exists in `src`. Phase 2: Core Facts And Status Add robust position-state facts: Notes: Do not emit noisy clock tags by default if they hurt tag stability. Consider a `--include-counters` flag later. `castled` inference should be conservative. If uncertain, omit it. Phase 3: Material And Imbalance Tags Complete material facts: Implementation details: Material code should use `Position.pieceCount`-style helpers or bitboards, not repeated board scans in every detector. For `insufficient`, keep the rule conservative: K vs K K+B vs K K+N vs K K+B vs K+B with bishops on same color, if implemented confidently Phase 4: Move Facts And Forcing Move Counts Add move-level aggregate facts: Implementation details: Use one `MoveList` generated in `TagContext`. Use `Position.State` and `play/undo` for low-allocation move consequence checks. Do not copy the position once per move in hot paths. Phase 5: Pawn Structure Add complete pawn themes: Implementation details: Build file/rank pawn masks once per side. Distinguish structural pawn tags from move tags. A pawn on the 7th rank gets `promotion_ready`; a legal promotion move gets `MOVE: promotions`. Keep candidate-passer rules conservative until tests are strong. Phase 6: King Safety Add king safety facts and themes: Implementation details: Use attack maps for enemy attacks and pawn maps for pawn shield. Avoid over-tagging every small weakness. Emit only meaningful thresholds. Keep \"vulnerable king\" and \"attacking castled king\" derived from this module. Phase 7: Piece Activity And Pressure Complete piece-specific placement tags: Implementation details: Keep the piece activity score explainable. Use piece-square heuristics, mobility, attacks, defended status, and king proximity, but do not hide a huge opaque formula in one method. Existing `wiki/piece-tags.md` should become the reference for this family. Phase 8: Space, Development, Mobility, Initiative Add strategic tags: Implementation details: `MOBILITY` for the side not to move requires generating moves after flipping side to move. This must be cached and should not be done unless requested by "
  },
  {
    "title": "Wiki Index",
    "href": "wiki-index.html",
    "category": "Development",
    "text": "ChessRTK Wiki Welcome to the ChessRTK documentation. This wiki is the project handbook: it starts with installation and first commands, then moves into configuration, engine workflows, mining, datasets, publishing, architecture, and maintenance. For a website-style version with sidebar navigation and readable page layout, open `https://LenniAConrad.github.io/chess-rtk/`. If the launcher is not installed, replace `crtk <command> ...` with `java -cp out application.Main <command> ...` after building the project. Start Here [Getting started](getting-started) - build, install, verify, and run the first useful commands [FAQ](faq) - practical answers for new users [Use cases](use-cases) - workflow paths by role or job [Command cheatsheet](command-cheatsheet) - fastest path from goal to command [Command reference](command-reference) - grouped CLI surface and options [Example commands](example-commands) - copyable command recipes [Troubleshooting](troubleshooting) - common setup and runtime failures Guides For Users [Build and install](build-and-install) [Configuration](configuration) [Running engines](in-house-engine) [LC0 UCI engine and Java evaluator](lc0) [Quality and testing](quality-and-testing) [Outputs and logs](outputs-and-logs) [Support](support) Workflows [Mining puzzles](mining) [Filter DSL](filter-dsl) [Datasets](datasets) [Book publishing](book-publishing) [Piece and position tags](piece-tags) [T5 tag-to-text pipeline](t5) [AI agents and automation](ai-agents) Developer Documentation [Architecture](architecture) [Quality and testing](quality-and-testing) [Development notes](development-notes) [Releasing](releasing) [Roadmap and ideas](roadmap) [Glossary](glossary) Capability Map Architecture At A Glance ![ChessRTK position toolbox](../assets/diagrams/crtk-position-toolbox.png) ChessRTK is built around one shared Java position model. The same legality and notation code drives the CLI, perft validation, built-in search, tagging, dataset export, rendering, GUI tools, and publishing output. Quick Verification For a broader pass: Project Policy Notes Java source and shell scripts are tracked. Local analysis dumps, models, generated PDFs, and build output are local artifacts and should stay out of git. UCI engines and model weights are optional. Core FEN, move, perft, and classical built-in search commands run in-process. Wiki pages describe implemented behavior unless a page is explicitly marked as roadmap or planning material."
  }
];
